{{- if .Values.telegram.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "incidentrelay.fullname" . }}-telegram
  labels:
    {{- include "incidentrelay.labels" . | nindent 4 }}
    app.kubernetes.io/component: telegram
spec:
  replicas: 1
  {{- with .Values.telegram.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "incidentrelay.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: telegram
  template:
    metadata:
      annotations:
        {{- include "incidentrelay.configChecksum" . | nindent 8 }}
        {{- with .Values.telegram.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "incidentrelay.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: telegram
        {{- with .Values.telegram.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "incidentrelay.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: telegram
          image: {{ include "incidentrelay.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: INCIDENTRELAY_CONFIG_FILE
              value: /etc/incidentrelay/incidentrelay.conf
            - name: INCIDENTRELAY_SERVICE
              value: telegram
            - name: INCIDENTRELAY_RUN_MIGRATIONS
              value: "0"
            - name: PYTHONUNBUFFERED
              value: "1"
            {{- with .Values.telegram.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.telegram.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            {{- include "incidentrelay.volumeMounts" . | nindent 12 }}
      volumes:
        {{- include "incidentrelay.volumes" . | nindent 8 }}
      {{- with .Values.telegram.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.telegram.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.telegram.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
