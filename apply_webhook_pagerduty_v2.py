#!/usr/bin/env python3
"""Add PagerDuty Events API v2 compatibility to the existing Webhook source."""

from pathlib import Path
import hashlib
import json

ROOT = Path.cwd()

FILE_VARIANTS = {'app/api/schemas/integrations.py': [('e6de01a35aeb7b74b513a9bd731cf37fa944fdd138bf598b6b016344616f452b', '317cc7b0bdfe6e7fdc8f1ddafb5ff2efdd06f30d8b28c3c45daf8a596766d0d9', 'from typing import Any, Dict, List\n\nfrom pydantic import ConfigDict, Field, model_validator\n\nfrom app.api.schemas.base import ApiModel\n\n\nclass AlertmanagerAlertSchema(ApiModel):\n    """\n    Validate one Alertmanager alert.\n    """\n\n    model_config = ConfigDict(extra="ignore")\n\n    status: str = "firing"\n    labels: dict[str, str] = Field(default_factory=dict)\n    annotations: dict[str, str] = Field(default_factory=dict)\n    startsAt: str | None = None\n    endsAt: str | None = None\n    generatorURL: str | None = None\n    fingerprint: str | None = None\n\n\nclass AlertmanagerWebhookSchema(ApiModel):\n    """\n    Validate Alertmanager webhook payload.\n    """\n\n    model_config = ConfigDict(extra="ignore")\n\n    receiver: str | None = None\n    status: str = "firing"\n    alerts: list[AlertmanagerAlertSchema]\n    groupLabels: dict[str, str] = Field(default_factory=dict)\n    commonLabels: dict[str, str] = Field(default_factory=dict)\n    commonAnnotations: dict[str, str] = Field(default_factory=dict)\n    externalURL: str | None = None\n    version: str | None = None\n    groupKey: str | None = None\n    truncatedAlerts: int | None = None\n    generatorURL: str | None = None\n    silenceURL: str | None = None\n    dashboardURL: str | None = None\n    panelURL: str | None = None\n\n\nclass GrafanaAlertSchema(ApiModel):\n    """Validate one Grafana Alerting alert instance."""\n\n    model_config = ConfigDict(extra="allow")\n\n    status: str = "firing"\n    labels: Dict[str, Any] = Field(default_factory=dict)\n    annotations: Dict[str, Any] = Field(default_factory=dict)\n\n    startsAt: str | None = None\n    endsAt: str | None = None\n\n    generatorURL: str | None = None\n    fingerprint: str | None = None\n    silenceURL: str | None = None\n    dashboardURL: str | None = None\n    panelURL: str | None = None\n\n    values: Dict[str, Any] = Field(default_factory=dict)\n    valueString: str | None = None\n\n\nclass GrafanaWebhookSchema(ApiModel):\n    """Validate a Grafana Alerting webhook payload."""\n\n    model_config = ConfigDict(extra="allow")\n\n    receiver: str | None = None\n    status: str = "firing"\n    orgId: int | str | None = None\n\n    alerts: List[GrafanaAlertSchema] = Field(min_length=1)\n\n    groupLabels: Dict[str, Any] = Field(default_factory=dict)\n    commonLabels: Dict[str, Any] = Field(default_factory=dict)\n    commonAnnotations: Dict[str, Any] = Field(default_factory=dict)\n\n    externalURL: str | None = None\n    version: str | None = None\n    groupKey: str | None = None\n    truncatedAlerts: int | None = None\n\n    title: str | None = None\n    state: str | None = None\n    message: str | None = None\n\n\nclass ZabbixWebhookSchema(ApiModel):\n    """Validate Zabbix webhook payload at envelope level."""\n\n    # Zabbix media types are often customized with additional macro fields.\n    # Keep known fields typed, but allow extra parameters to pass through\n    # to the normalizer.\n    model_config = ConfigDict(extra="allow")\n\n    event_id: str | None = None\n    eventid: str | None = None\n\n    trigger_id: str | None = None\n    triggerid: str | None = None\n\n    title: str | None = None\n    subject: str | None = None\n    name: str | None = None\n    event_name: str | None = None\n    trigger_name: str | None = None\n    problem_name: str | None = None\n\n    message: str | None = None\n    description: str | None = None\n    opdata: str | None = None\n\n    severity: str | None = None\n    event_severity: str | None = None\n    trigger_severity: str | None = None\n\n    status: str | None = None\n    event_status: str | None = None\n\n    team: str | None = None\n\n    host: str | None = None\n    host_name: str | None = None\n    hostname: str | None = None\n\n    labels: Dict[str, Any] = Field(default_factory=dict)\n\n    # Can be a dict, a Zabbix EVENT.TAGSJSON array, or sometimes a raw string.\n    tags: Any = None\n    event_tag: Any = None\n    event_tags: Any = None\n    tags_json: Any = None\n\n    fingerprint: str | None = None\n\n    event_link: str | None = None\n    event_url: str | None = None\n    problem_url: str | None = None\n    trigger_url: str | None = None\n    zabbix_url: str | None = None\n\n    @model_validator(mode="after")\n    def validate_not_empty(self):\n        has_identity = bool(\n            self.event_id\n            or self.eventid\n            or self.trigger_id\n            or self.triggerid\n            or self.fingerprint\n        )\n\n        has_content = bool(\n            self.title\n            or self.subject\n            or self.name\n            or self.event_name\n            or self.trigger_name\n            or self.problem_name\n            or self.message\n            or self.description\n            or self.opdata\n            or self.event_link\n            or self.event_url\n            or self.problem_url\n            or self.trigger_url\n        )\n\n        has_labels = bool(self.labels or self.tags)\n\n        if not (has_identity or has_content or has_labels):\n            raise ValueError(\n                "Zabbix webhook payload must contain at least one of: "\n                "event_id, trigger_id, fingerprint, title, subject, "\n                "event_name, trigger_name, problem_name, message, description, "\n                "opdata, labels or tags"\n            )\n\n        return self\n\n\nclass GenericWebhookSchema(ApiModel):\n    """Validate generic or PagerDuty Events API v2-compatible payloads."""\n\n    # Generic senders frequently add custom fields. PagerDuty Events API v2\n    # also has nested payload, links, images and client metadata. Preserve all\n    # of them in the raw alert payload instead of silently dropping them.\n    model_config = ConfigDict(extra="allow")\n\n    # IncidentRelay generic webhook fields.\n    title: str | None = Field(default=None, min_length=1, max_length=255)\n    message: str | None = None\n    severity: str | None = None\n    status: str | None = None\n    team: str | None = None\n    labels: Dict[str, Any] = Field(default_factory=dict)\n    fingerprint: str | None = None\n    external_id: str | None = None\n\n    event_link: str | None = None\n    event_url: str | None = None\n    alert_url: str | None = None\n    source_url: str | None = None\n    dashboard_url: str | None = None\n    runbook_url: str | None = None\n\n    # PagerDuty Events API v2-compatible fields. The route intake token is\n    # supplied as routing_key when this format is used.\n    routing_key: str | None = None\n    event_action: str | None = None\n    dedup_key: str | None = None\n    payload: Dict[str, Any] | None = None\n    links: List[Dict[str, Any]] = Field(default_factory=list)\n    images: List[Dict[str, Any]] = Field(default_factory=list)\n    client: str | None = None\n    client_url: str | None = None\n\n    @model_validator(mode="after")\n    def validate_supported_format(self):\n        action = str(self.event_action or "").strip().lower()\n\n        if action:\n            if action not in {"trigger", "acknowledge", "resolve"}:\n                raise ValueError(\n                    "event_action must be trigger, acknowledge or resolve"\n                )\n\n            if not str(self.routing_key or "").strip():\n                raise ValueError(\n                    "routing_key is required for PagerDuty Events API v2 payloads"\n                )\n\n            if action in {"acknowledge", "resolve"}:\n                if not str(self.dedup_key or "").strip():\n                    raise ValueError(\n                        "dedup_key is required for acknowledge and resolve events"\n                    )\n                return self\n\n            event_payload = self.payload or {}\n            missing = [\n                key\n                for key in ("summary", "source", "severity")\n                if not str(event_payload.get(key) or "").strip()\n            ]\n\n            if missing:\n                raise ValueError(\n                    "PagerDuty trigger payload is missing required fields: "\n                    + ", ".join(missing)\n                )\n\n            severity = str(event_payload.get("severity") or "").strip().lower()\n            if severity not in {"critical", "error", "warning", "info"}:\n                raise ValueError(\n                    "PagerDuty payload.severity must be critical, error, warning or info"\n                )\n\n            return self\n\n        if not str(self.title or "").strip():\n            raise ValueError(\n                "Generic webhook payload must contain title, or use "\n                "PagerDuty Events API v2 event_action"\n            )\n\n        return self\n\n\nclass RmonWebhookSchema(ApiModel):\n    """Validate an RMON alert payload."""\n\n    model_config = ConfigDict(extra="allow")\n\n    title: str = Field(min_length=1, max_length=255)\n    message: str | None = None\n    severity: str | None = None\n    status: str | None = None\n\n    fingerprint: str | None = None\n    external_id: str | int | None = None\n\n    team: str | None = None\n    labels: Dict[str, Any] = Field(default_factory=dict)\n\n    runbook: str | None = None\n    runbook_url: str | None = None\n    event_link: str | None = None\n\n    rmon_name: str | None = None\n\n    check_id: str | int | None = None\n    multi_check_id: str | int | None = None\n    state_id: str | int | None = None\n\n    check_name: str | None = None\n    check_type: str | None = None\n    target: str | None = None\n\n    agent: str | None = None\n    region: str | None = None\n    country: str | None = None\n\n\nclass SentryWebhookSchema(ApiModel):\n    """Validate Sentry integration webhook payload."""\n\n    model_config = ConfigDict(extra="allow")\n\n    action: str | None = None\n    actor: Dict[str, Any] | None = None\n    installation: Dict[str, Any] | None = None\n    data: Dict[str, Any] = Field(default_factory=dict)\n\n\nclass LibreNMSWebhookSchema(ApiModel):\n    """Validate LibreNMS API transport payload."""\n\n    model_config = ConfigDict(extra="allow")\n\n    id: str | int | None = None\n    uid: str | None = None\n    alert_id: str | int | None = None\n    alert_uid: str | None = None\n    external_id: str | int | None = None\n\n    title: str | None = None\n    subject: str | None = None\n    name: str | None = None\n    rule: str | None = None\n\n    message: str | None = None\n    msg: str | None = None\n    description: str | None = None\n    alert_notes: str | None = None\n\n    state: str | int | None = None\n    status: str | None = None\n    severity: str | None = None\n\n    hostname: str | None = None\n    display: str | None = None\n    sysName: str | None = None\n    device_id: str | int | None = None\n    ip: str | None = None\n    os: str | None = None\n    type: str | None = None\n    hardware: str | None = None\n    version: str | None = None\n    location: str | None = None\n\n    timestamp: str | None = None\n    elapsed: str | None = None\n    proc: str | None = None\n\n    team: str | None = None\n    labels: Dict[str, Any] = Field(default_factory=dict)\n\n    fingerprint: str | None = None\n    event_link: str | None = None\n    event_url: str | None = None\n    alert_url: str | None = None\n    source_url: str | None = None\n    device_url: str | None = None\n    librenms_url: str | None = None\n\n    faults: Any = None\n    contacts: Any = None\n    applications: Any = None\n\n    @model_validator(mode="after")\n    def validate_not_empty(self):\n        has_identity = bool(\n            self.external_id\n            or self.id\n            or self.uid\n            or self.alert_id\n            or self.alert_uid\n            or self.fingerprint\n        )\n        has_content = bool(\n            self.title\n            or self.subject\n            or self.name\n            or self.rule\n            or self.message\n            or self.msg\n            or self.description\n            or self.hostname\n            or self.display\n            or self.sysName\n        )\n        has_labels = bool(self.labels)\n\n        if not (has_identity or has_content or has_labels):\n            raise ValueError(\n                "LibreNMS webhook payload must contain at least one of: "\n                "id, uid, alert_id, title, name, rule, message, msg, "\n                "description, hostname, display, sysName, fingerprint or labels"\n            )\n\n        return self\n\n\nclass AwsSnsEnvelopeSchema(ApiModel):\n    """Validate an Amazon SNS HTTP/HTTPS message envelope."""\n\n    model_config = ConfigDict(\n        extra="allow",\n        populate_by_name=True,\n    )\n\n    message_type: str = Field(\n        alias="Type",\n        pattern=(\n            r"^(Notification|SubscriptionConfirmation|"\n            r"UnsubscribeConfirmation)$"\n        ),\n    )\n    message_id: str = Field(\n        alias="MessageId",\n        min_length=1,\n    )\n    topic_arn: str = Field(\n        alias="TopicArn",\n        min_length=1,\n    )\n    message: str = Field(\n        alias="Message",\n    )\n    timestamp: str = Field(\n        alias="Timestamp",\n        min_length=1,\n    )\n    signature_version: str = Field(\n        alias="SignatureVersion",\n        pattern=r"^[12]$",\n    )\n    signature: str = Field(\n        alias="Signature",\n        min_length=1,\n    )\n    signing_cert_url: str = Field(\n        alias="SigningCertURL",\n        min_length=1,\n    )\n\n    subject: str | None = Field(\n        default=None,\n        alias="Subject",\n    )\n    token: str | None = Field(\n        default=None,\n        alias="Token",\n    )\n    subscribe_url: str | None = Field(\n        default=None,\n        alias="SubscribeURL",\n    )\n    unsubscribe_url: str | None = Field(\n        default=None,\n        alias="UnsubscribeURL",\n    )\n    message_attributes: Dict[str, Any] = Field(\n        default_factory=dict,\n        alias="MessageAttributes",\n    )\n\n    @model_validator(mode="after")\n    def validate_confirmation_fields(self):\n        if self.message_type in {\n            "SubscriptionConfirmation",\n            "UnsubscribeConfirmation",\n        }:\n            if not self.token:\n                raise ValueError(\n                    "Token is required for an SNS confirmation message"\n                )\n\n            if not self.subscribe_url:\n                raise ValueError(\n                    "SubscribeURL is required for an SNS confirmation message"\n                )\n\n        return self\n', 'default')], 'app/services/integrations/auth.py': [('3f0f647ea1bdca660c8b68b104a816a46bbf3e31f865848e8ac51a6216ed0a42', '868edd3875c67185a9bd81c7d79c288ec0b1fcfe71d16f4bdcd5b32173d81007', 'import hashlib\nimport secrets\nfrom datetime import datetime\nfrom functools import wraps\n\nfrom flask import jsonify, request\n\nfrom app.middleware import load_jwt_user\nfrom app.modules.db import routes_repo, tokens_repo\nfrom app.settings import Config\n\n\ndef create_raw_token():\n    """\n    Create a random token.\n    """\n\n    return secrets.token_urlsafe(48)\n\n\ndef hash_token(raw_token):\n    """\n    Hash a token before storing it.\n    """\n\n    return hashlib.sha256(raw_token.encode("utf-8")).hexdigest()\n\n\ndef get_bearer_token():\n    """\n    Extract a bearer token from the Authorization header.\n    """\n\n    auth_header = request.headers.get("Authorization", "")\n\n    if not auth_header.startswith("Bearer "):\n        return None\n\n    return auth_header.split(" ", 1)[1].strip()\n\n\ndef get_json_routing_key():\n    """Return PagerDuty-compatible routing_key from a JSON request body."""\n\n    payload = request.get_json(silent=True)\n\n    if not isinstance(payload, dict):\n        return None\n\n    routing_key = payload.get("routing_key")\n\n    if routing_key is None:\n        return None\n\n    routing_key = str(routing_key).strip()\n    return routing_key or None\n\n\ndef authenticate_api_token(raw_token=None):\n    """\n    Authenticate a stored API token.\n    """\n\n    raw_token = raw_token or get_bearer_token()\n\n    if not raw_token:\n        request.current_api_token = None\n        return None\n\n    api_token = tokens_repo.get_active_token_by_hash(hash_token(raw_token))\n\n    if not api_token:\n        request.current_api_token = None\n        return None\n\n    if api_token.expires_at and api_token.expires_at <= datetime.utcnow():\n        request.current_api_token = None\n        return None\n\n    tokens_repo.mark_token_used(api_token)\n\n    request.current_api_token = api_token\n    request.current_auth_type = "api_token"\n\n    if api_token.user and api_token.user.active:\n        request.current_user = api_token.user\n\n    return api_token\n\n\ndef authenticate_route_token(raw_token=None):\n    """\n    Authenticate a route alert intake token.\n    """\n\n    raw_token = raw_token or get_bearer_token()\n\n    if not raw_token:\n        request.current_intake_route = None\n        return None\n\n    route = routes_repo.get_route_by_intake_hash(hash_token(raw_token))\n\n    if not route:\n        request.current_intake_route = None\n        return None\n\n    request.current_intake_route = route\n    request.current_auth_type = "route_token"\n\n    return route\n\n\ndef authenticate_request():\n    """\n    Authenticate the request using JWT first, then personal/API tokens.\n    """\n\n    user = load_jwt_user()\n\n    if user:\n        request.current_api_token = None\n        request.current_auth_type = "jwt"\n        return user\n\n    api_token = authenticate_api_token()\n\n    if api_token:\n        return api_token\n\n    request.current_auth_type = None\n    return None\n\n\ndef token_has_scope(api_token, required_scopes):\n    """\n    Return True when an API token has all required scopes.\n    """\n\n    if not required_scopes:\n        return True\n\n    token_scopes = api_token.scopes or []\n\n    if "*" in token_scopes:\n        return True\n\n    return all(scope in token_scopes for scope in required_scopes)\n\n\ndef require_api_token(scopes=None, required=None):\n    """\n    Require JWT or API token authentication for a view.\n    """\n\n    scopes = scopes or []\n    required = Config.API_AUTH_REQUIRED if required is None else required\n\n    def decorator(func):\n        @wraps(func)\n        def wrapper(*args, **kwargs):\n            principal = authenticate_request()\n\n            if not required and not principal:\n                return func(*args, **kwargs)\n\n            if not principal:\n                return jsonify({"error": "Authentication is required"}), 401\n\n            api_token = getattr(request, "current_api_token", None)\n\n            if api_token and not token_has_scope(api_token, scopes):\n                return jsonify({"error": "Missing API token scope", "missing_scopes": scopes}), 403\n\n            return func(*args, **kwargs)\n\n        return wrapper\n\n    return decorator\n\n\ndef require_alert_token(allow_json_routing_key=False):\n    """\n    Require a token that can submit incoming alerts.\n\n    Accepted tokens:\n    - personal/API token with alerts:write scope;\n    - route intake token created on the Routes page;\n    - PagerDuty-compatible JSON routing_key when explicitly enabled.\n\n    routing_key is checked before Authorization so it remains the routing\n    boundary for PagerDuty Events API v2-compatible requests.\n    """\n\n    def decorator(func):\n        @wraps(func)\n        def wrapper(*args, **kwargs):\n            if allow_json_routing_key:\n                routing_key = get_json_routing_key()\n\n                if routing_key:\n                    route = authenticate_route_token(routing_key)\n\n                    if route:\n                        request.current_auth_type = "pagerduty_routing_key"\n                        return func(*args, **kwargs)\n\n                    # Do not silently fall back to another credential when a\n                    # sender explicitly supplied a PagerDuty routing key.\n                    return jsonify({"error": "Route intake token is required"}), 401\n\n            raw_token = get_bearer_token()\n\n            api_token = authenticate_api_token(raw_token)\n\n            if api_token:\n                if token_has_scope(api_token, ["alerts:write"]):\n                    request.current_auth_type = "api_token"\n                    return func(*args, **kwargs)\n\n                return jsonify({"error": "Missing API token scope", "missing_scopes": ["alerts:write"]}), 403\n\n            route = authenticate_route_token(raw_token)\n\n            if route:\n                return func(*args, **kwargs)\n\n            return jsonify({"error": "Route intake token is required"}), 401\n\n        return wrapper\n\n    return decorator\n', 'default')], 'app/services/integrations/normalizers/webhook.py': [('5238b16e0034fecf692cbdd32da7314c840fdf85747e583301b0243c3cd29f51', '21fa01842e1fa62dbbad3a034b0c9ddfe13e6731fe695730a1ca1239626c02c4', 'import json\nfrom copy import deepcopy\n\nfrom app.services.integrations.normalizers.common import (\n    add_event_link_label,\n    first_event_link,\n    first_non_empty,\n    make_dedup_key,\n)\nfrom app.services.severity import normalize_severity\n\n\nPAGERDUTY_EVENT_ACTIONS = {"trigger", "acknowledge", "resolve"}\n\n\ndef is_pagerduty_events_v2(payload):\n    """Return True for a supported PagerDuty Events API v2 alert event."""\n\n    if not isinstance(payload, dict):\n        return False\n\n    action = str(payload.get("event_action") or "").strip().lower()\n    return action in PAGERDUTY_EVENT_ACTIONS\n\n\ndef _clean_string(value):\n    if value is None:\n        return None\n\n    value = str(value).strip()\n    return value or None\n\n\ndef _label_value(value):\n    """Convert custom detail values into matcher-friendly label values."""\n\n    if value is None:\n        return None\n\n    if isinstance(value, (str, int, float, bool)):\n        return value\n\n    try:\n        return json.dumps(value, ensure_ascii=False, sort_keys=True)\n    except (TypeError, ValueError):\n        return str(value)\n\n\ndef _sanitized_payload(payload):\n    """Copy the source payload without persisting the secret routing key."""\n\n    result = deepcopy(payload)\n\n    if result.get("routing_key"):\n        result["routing_key"] = "[REDACTED]"\n\n    return result\n\n\ndef _pagerduty_labels(payload, event_payload, action):\n    labels = dict(payload.get("labels") or {})\n    labels.setdefault("pagerduty_event_action", action)\n    labels.setdefault("pagerduty_format", "events_api_v2")\n\n    for key in ("source", "component", "group", "class"):\n        value = _clean_string(event_payload.get(key))\n\n        if value is not None:\n            labels.setdefault(key, value)\n\n    custom_details = event_payload.get("custom_details") or {}\n\n    if isinstance(custom_details, dict):\n        for key, value in custom_details.items():\n            key = _clean_string(key)\n\n            if not key or key in labels:\n                continue\n\n            value = _label_value(value)\n\n            if value is not None:\n                labels[key] = value\n\n    client = _clean_string(payload.get("client"))\n\n    if client:\n        labels.setdefault("pagerduty_client", client)\n\n    return labels\n\n\ndef _pagerduty_event_link(payload):\n    links = payload.get("links") or []\n    link_values = []\n\n    if isinstance(links, list):\n        for item in links:\n            if isinstance(item, dict):\n                link_values.append(item.get("href"))\n\n    return first_event_link(\n        *link_values,\n        payload.get("client_url"),\n    )\n\n\ndef _normalize_pagerduty_events_v2(payload):\n    action = str(payload.get("event_action") or "").strip().lower()\n    event_payload = payload.get("payload") or {}\n\n    if not isinstance(event_payload, dict):\n        event_payload = {}\n\n    labels = _pagerduty_labels(payload, event_payload, action)\n    event_link = _pagerduty_event_link(payload)\n    add_event_link_label(labels, event_link)\n\n    dedup_key = _clean_string(payload.get("dedup_key"))\n    summary = _clean_string(event_payload.get("summary"))\n    source = _clean_string(event_payload.get("source"))\n\n    title = summary or (\n        f"PagerDuty event {dedup_key}"\n        if dedup_key\n        else "PagerDuty webhook event"\n    )\n    title = title[:255]\n\n    if not dedup_key:\n        dedup_key = make_dedup_key(\n            "webhook",\n            source,\n            title,\n            {\n                "component": labels.get("component"),\n                "group": labels.get("group"),\n                "class": labels.get("class"),\n            },\n        )\n\n    custom_details = event_payload.get("custom_details") or {}\n\n    if not isinstance(custom_details, dict):\n        custom_details = {}\n\n    message = first_non_empty(\n        event_payload.get("message"),\n        event_payload.get("description"),\n        custom_details.get("message"),\n        custom_details.get("description"),\n        custom_details.get("details"),\n    ) or ""\n\n    raw_severity = event_payload.get("severity")\n    severity = normalize_severity(raw_severity) or "info"\n\n    team_slug = first_non_empty(\n        payload.get("team"),\n        labels.get("team"),\n        labels.get("oncall_team"),\n        custom_details.get("team"),\n        custom_details.get("oncall_team"),\n    )\n\n    status = {\n        "trigger": "firing",\n        "acknowledge": "acknowledged",\n        "resolve": "resolved",\n    }[action]\n\n    return [\n        {\n            "source": "webhook",\n            "team_slug": team_slug,\n            "external_id": dedup_key,\n            "dedup_key": dedup_key,\n            "title": title,\n            "message": str(message),\n            "severity": severity,\n            "labels": labels,\n            "payload": _sanitized_payload(payload),\n            "status": status,\n            "lifecycle_action": action,\n            "integration_format": "pagerduty_events_api_v2",\n        }\n    ]\n\n\ndef normalize_webhook(payload):\n    """Normalize a generic or PagerDuty Events API v2 webhook payload."""\n\n    if is_pagerduty_events_v2(payload):\n        return _normalize_pagerduty_events_v2(payload)\n\n    labels = dict(payload.get("labels") or {})\n\n    event_link = first_event_link(\n        payload.get("event_link"),\n        payload.get("event_url"),\n        payload.get("alert_url"),\n        payload.get("source_url"),\n        payload.get("dashboard_url"),\n        payload.get("runbook_url"),\n        labels.get("event_link"),\n        labels.get("event_url"),\n        labels.get("alert_url"),\n        labels.get("source_url"),\n        labels.get("dashboard_url"),\n        labels.get("runbook_url"),\n    )\n\n    add_event_link_label(labels, event_link)\n\n    title = payload.get("title") or "Webhook alert"\n\n    return [\n        {\n            "source": "webhook",\n            "team_slug": payload.get("team") or labels.get("team") or labels.get("oncall_team"),\n            "external_id": payload.get("external_id"),\n            "dedup_key": payload.get("fingerprint")\n                         or make_dedup_key("webhook", payload.get("external_id"), title, labels),\n            "title": title,\n            "message": payload.get("message") or "",\n            "severity": normalize_severity(payload.get("severity")) or "info",\n            "labels": labels,\n            "payload": _sanitized_payload(payload),\n            "status": payload.get("status") or "firing",\n        }\n    ]\n', 'default')], 'app/modules/db/alerts_repo.py': [('4ae8dccd93195e3780607b9f16905bd49dd27fdc1f4922886053464915598ed1', 'c3b46fa5a0c77bf179aada52777294c180a3a007544e1a85d4831d6a7df05764', 'from functools import reduce\nfrom operator import or_\nfrom math import ceil\nfrom datetime import datetime\nimport hashlib\n\nfrom peewee import Case, JOIN, fn\n\nfrom app.modules.db.models import (\n    Alert,\n    AlertEvent,\n    AlertGroup,\n    AlertGroupMerge,\n    AlertRoute,\n    AlertComment,\n    Rotation,\n    Service,\n    Team,\n    User,\n    IncidentResponder,\n    IncidentStakeholder,\n    AlertExplainStep,\n    AlertExplainTrace,\n)\nfrom app.modules.db.query_filters import (\n    apply_field_values_filter,\n    normalize_filter_values,\n)\n\n\nMAX_ALERTS_PAGE_SIZE = 100\n\nALERT_GROUP_STATUS_SORT = Case(\n    AlertGroup.status,\n    (\n        ("firing", 1),\n        ("acknowledged", 2),\n        ("silenced", 3),\n        ("resolved", 4),\n        ("merged", 5),\n    ),\n    9,\n)\n\nALERT_GROUP_SEVERITY_SORT = Case(\n    AlertGroup.severity,\n    (\n        ("critical", 4),\n        ("high", 3),\n        ("medium", 2),\n        ("low", 1),\n    ),\n    0,\n)\n\nALERT_GROUP_PRIORITY_SORT = fn.COALESCE(AlertGroup.priority_order, 3)\n\nALERT_GROUP_SORT_FIELDS = {\n    "id": AlertGroup.id,\n    "status": ALERT_GROUP_STATUS_SORT,\n    "title": AlertGroup.title,\n    "severity": ALERT_GROUP_SEVERITY_SORT,\n    "priority": ALERT_GROUP_PRIORITY_SORT,\n    "team": Team.slug,\n    "assignee": User.username,\n    "created": AlertGroup.first_seen_at,\n    "last_seen": AlertGroup.last_seen_at,\n    "activity": AlertGroup.last_seen_at,\n    "reminders": AlertGroup.reminder_count,\n}\n\n\ndef normalize_alert_group_sort(sort):\n    """Return a whitelisted alert group sort field."""\n\n    if sort in ALERT_GROUP_SORT_FIELDS:\n        return sort\n\n    return "activity"\n\n\ndef apply_alert_group_priority_filter(query, values):\n    """Apply incident priority filter by priority slug.\n\n    Old groups can have NULL priority_slug if they were created before\n    incident priorities were introduced. Treat NULL as the default P3\n    priority when filtering by p3.\n    """\n    priorities = []\n    seen = set()\n\n    for value in normalize_filter_values(values):\n        value = str(value).strip().lower()\n\n        if not value or value in seen:\n            continue\n\n        seen.add(value)\n        priorities.append(value)\n\n    if not priorities:\n        return query\n\n    conditions = [AlertGroup.priority_slug.in_(priorities)]\n\n    if "p3" in priorities:\n        conditions.append(AlertGroup.priority_slug.is_null(True))\n\n    return query.where(reduce(or_, conditions))\n\n\ndef build_alert_groups_query(\n    team_id=None,\n    team_ids=None,\n    status=None,\n    source=None,\n    severity=None,\n    priority=None,\n    service_id=None,\n    service_slug=None,\n    service_status=None,\n    service_criticality=None,\n    search=None,\n    assigned_to_user_id=None,\n    include_merged=False,\n):\n    """Build the base alert groups query with filters."""\n\n    query = (\n        AlertGroup\n        .select(AlertGroup, Team, AlertRoute, Rotation, User)\n        .join(Team, JOIN.LEFT_OUTER)\n        .switch(AlertGroup)\n        .join(AlertRoute, JOIN.LEFT_OUTER)\n        .switch(AlertGroup)\n        .join(Rotation, JOIN.LEFT_OUTER)\n        .switch(AlertGroup)\n        .join(User, JOIN.LEFT_OUTER, on=(AlertGroup.assignee == User.id))\n    )\n\n    if not include_merged:\n        query = query.where(AlertGroup.merged_into.is_null(True))\n        query = query.where(AlertGroup.status != "merged")\n\n    if team_id:\n        query = query.where(AlertGroup.team == team_id)\n    elif team_ids is not None:\n        if not team_ids:\n            return None\n\n        query = query.where(AlertGroup.team.in_(team_ids))\n\n    if assigned_to_user_id:\n        query = query.where(AlertGroup.assignee == assigned_to_user_id)\n\n    query = apply_field_values_filter(\n        query,\n        AlertGroup.status,\n        status,\n    )\n\n    query = apply_field_values_filter(\n        query,\n        AlertGroup.source,\n        source,\n    )\n\n    query = apply_field_values_filter(\n        query,\n        AlertGroup.severity,\n        severity,\n    )\n    query = apply_alert_group_priority_filter(\n        query,\n        priority,\n    )\n    query = apply_field_values_filter(\n        query,\n        AlertGroup.service,\n        service_id,\n        value_type=int,\n    )\n\n    if service_slug:\n        service_ids = (\n            Service\n            .select(Service.id)\n            .where(\n                (Service.slug == service_slug)\n                & (Service.deleted == False)\n            )\n        )\n        query = query.where(AlertGroup.service.in_(service_ids))\n\n    if service_status:\n        service_ids = (\n            Service\n            .select(Service.id)\n            .where(\n                (Service.status == service_status)\n                & (Service.deleted == False)\n            )\n        )\n        query = query.where(AlertGroup.service.in_(service_ids))\n\n    if service_criticality:\n        service_ids = (\n            Service\n            .select(Service.id)\n            .where(\n                (Service.criticality == service_criticality)\n                & (Service.deleted == False)\n            )\n        )\n        query = query.where(AlertGroup.service.in_(service_ids))\n\n    if search:\n        search = str(search).strip()\n\n        if search:\n            group_conditions = [\n                AlertGroup.title.contains(search),\n                AlertGroup.message.contains(search),\n                AlertGroup.source.contains(search),\n                AlertGroup.group_key.contains(search),\n                AlertGroup.severity.contains(search),\n                AlertGroup.status.contains(search),\n                AlertGroup.common_labels.contains(search),\n                AlertGroup.label_values.contains(search),\n                AlertGroup.payload_summary.contains(search),\n                Team.slug.contains(search),\n                Team.name.contains(search),\n                AlertRoute.name.contains(search),\n                Rotation.name.contains(search),\n                User.username.contains(search),\n                User.display_name.contains(search),\n            ]\n\n            child_conditions = [\n                Alert.title.contains(search),\n                Alert.message.contains(search),\n                Alert.source.contains(search),\n                Alert.external_id.contains(search),\n                Alert.dedup_key.contains(search),\n                Alert.group_key.contains(search),\n                Alert.severity.contains(search),\n                Alert.status.contains(search),\n                Alert.labels.contains(search),\n                Alert.payload.contains(search),\n            ]\n\n            if search.isdigit():\n                search_id = int(search)\n                group_conditions.append(AlertGroup.id == search_id)\n                child_conditions.append(Alert.id == search_id)\n\n            child_group_ids = (\n                Alert\n                .select(Alert.group)\n                .where(\n                    (Alert.group.is_null(False))\n                    & reduce(or_, child_conditions)\n                )\n            )\n\n            group_conditions.append(AlertGroup.id.in_(child_group_ids))\n\n            query = query.where(reduce(or_, group_conditions))\n\n    return query\n\n\ndef apply_alert_group_sort(query, sort, order):\n    """Apply whitelisted sorting to alert groups query."""\n\n    sort = normalize_alert_group_sort(sort)\n    order = normalize_alert_order(order)\n\n    expression = ALERT_GROUP_SORT_FIELDS[sort]\n    ordered_expression = expression.asc() if order == "asc" else expression.desc()\n\n    if sort == "id":\n        return query.order_by(ordered_expression)\n\n    return query.order_by(ordered_expression, AlertGroup.id.desc())\n\n\ndef summarize_alert_groups(query, total_items):\n    """Build summary counters for the current filtered group selection."""\n\n    summary = {\n        "firing": 0,\n        "acknowledged": 0,\n        "resolved": 0,\n        "silenced": 0,\n        "merged": 0,\n        "reminders": 0,\n        "alerts": 0,\n        "total": total_items,\n    }\n\n    if query is None:\n        return summary\n\n    summary_query = (\n        query\n        .select(\n            AlertGroup.status.alias("status"),\n            fn.COUNT(AlertGroup.id).alias("count"),\n            fn.COALESCE(fn.SUM(AlertGroup.reminder_count), 0).alias("reminders"),\n            fn.COALESCE(fn.SUM(AlertGroup.alert_count), 0).alias("alerts"),\n        )\n        .group_by(AlertGroup.status)\n    )\n\n    for row in summary_query.dicts():\n        status = row.get("status") or "unknown"\n        count = row.get("count") or 0\n\n        if status in summary:\n            summary[status] = count\n\n        summary["reminders"] += row.get("reminders") or 0\n        summary["alerts"] += row.get("alerts") or 0\n\n    return summary\n\n\ndef empty_paginated_alert_groups(page=1, page_size=25, sort="activity", order="desc"):\n    """Return an empty paginated alert group response."""\n\n    page = normalize_alert_page(page)\n    page_size = normalize_alert_page_size(page_size)\n\n    return {\n        "items": [],\n        "pagination": {\n            "page": page,\n            "page_size": page_size,\n            "total_items": 0,\n            "total_pages": 1,\n            "from": 0,\n            "to": 0,\n            "has_prev": False,\n            "has_next": False,\n        },\n        "summary": summarize_alert_groups(None, 0),\n        "sort": {\n            "field": normalize_alert_group_sort(sort),\n            "order": normalize_alert_order(order),\n        },\n    }\n\n\ndef paginate_alert_groups(\n    team_id=None,\n    team_ids=None,\n    status=None,\n    source=None,\n    severity=None,\n    priority=None,\n    service_id=None,\n    service_slug=None,\n    service_status=None,\n    service_criticality=None,\n    search=None,\n    assigned_to_user_id=None,\n    page=1,\n    page_size=25,\n    sort="activity",\n    order="desc",\n    include_merged=False,\n):\n    """Return alert groups with backend pagination, filtering and sorting."""\n\n    page = normalize_alert_page(page)\n    page_size = normalize_alert_page_size(page_size)\n    sort = normalize_alert_group_sort(sort)\n    order = normalize_alert_order(order)\n\n    query = build_alert_groups_query(\n        team_id=team_id,\n        team_ids=team_ids,\n        status=status,\n        source=source,\n        severity=severity,\n        priority=priority,\n        service_id=service_id,\n        service_slug=service_slug,\n        service_status=service_status,\n        service_criticality=service_criticality,\n        search=search,\n        assigned_to_user_id=assigned_to_user_id,\n        include_merged=include_merged,\n    )\n\n    if query is None:\n        return empty_paginated_alert_groups(\n            page=page,\n            page_size=page_size,\n            sort=sort,\n            order=order,\n        )\n\n    total_items = query.count()\n    total_pages = max(1, int(ceil(total_items / float(page_size))))\n\n    if page > total_pages:\n        page = total_pages\n\n    sorted_query = apply_alert_group_sort(query, sort, order)\n    items = list(sorted_query.paginate(page, page_size))\n\n    start_index = (page - 1) * page_size\n    end_index = start_index + len(items)\n\n    return {\n        "items": items,\n        "pagination": {\n            "page": page,\n            "page_size": page_size,\n            "total_items": total_items,\n            "total_pages": total_pages,\n            "from": start_index + 1 if total_items else 0,\n            "to": end_index if total_items else 0,\n            "has_prev": page > 1,\n            "has_next": page < total_pages,\n        },\n        "summary": summarize_alert_groups(query, total_items),\n        "sort": {\n            "field": sort,\n            "order": order,\n        },\n    }\n\n\ndef get_alert(alert_id: int) -> Alert | None:\n    return Alert.get_or_none(Alert.id == alert_id)\n\n\ndef get_alert_group(group_id):\n    """Return an alert group by id."""\n\n    return AlertGroup.get_by_id(group_id)\n\n\ndef hash_group_key(group_key):\n    """Return stable hash for potentially long group keys."""\n\n    value = str(group_key or "")\n    return hashlib.sha256(value.encode("utf-8")).hexdigest()\n\n\ndef find_open_alert_group(source, group_key, team_id=None, route_id=None, service_id=None):\n    """Return open alert group by routing/grouping key."""\n\n    group_key_hash = hash_group_key(group_key)\n\n    query = AlertGroup.select().where(\n        (AlertGroup.source == source)\n        & (AlertGroup.group_key_hash == group_key_hash)\n        & (AlertGroup.status.not_in(("resolved", "merged")))\n        & (AlertGroup.merged_into.is_null(True))\n    )\n\n    if team_id:\n        query = query.where(AlertGroup.team == team_id)\n\n    if route_id:\n        query = query.where(AlertGroup.route == route_id)\n\n    if service_id:\n        query = query.where(AlertGroup.service == service_id)\n\n    return query.order_by(AlertGroup.id.desc()).first()\n\n\ndef create_alert_group(**kwargs):\n    """Create alert group."""\n\n    group_key = kwargs.get("group_key") or ""\n    kwargs["group_key_hash"] = hash_group_key(group_key)\n    return AlertGroup.create(**kwargs)\n\n\ndef list_alerts_for_group(group_id):\n    """Return concrete alerts inside group."""\n\n    return list(\n        Alert.select()\n        .where(Alert.group == group_id)\n        .order_by(Alert.id.asc())\n    )\n\n\ndef _collect_group_labels(alerts):\n    """Build common labels and varying values for a group."""\n\n    if not alerts:\n        return {}, {}\n\n    all_keys = set()\n    for alert in alerts:\n        all_keys.update((alert.labels or {}).keys())\n\n    common = {}\n    values = {}\n\n    for key in sorted(all_keys):\n        seen = []\n        for alert in alerts:\n            value = (alert.labels or {}).get(key)\n            if value is not None and value not in seen:\n                seen.append(value)\n\n        if len(seen) == 1:\n            common[key] = seen[0]\n        elif seen:\n            values[key] = seen\n\n    return common, values\n\n\ndef recalculate_alert_group(group):\n    """Recalculate alert group counters, labels and effective status."""\n\n    now = datetime.utcnow()\n\n    alerts = list(\n        Alert\n        .select()\n        .where(Alert.group == group.id)\n        .order_by(Alert.last_seen_at.desc(), Alert.id.desc())\n    )\n\n    total = len(alerts)\n\n    firing_count = sum(1 for alert in alerts if alert.status == "firing")\n    acknowledged_count = sum(1 for alert in alerts if alert.status == "acknowledged")\n    resolved_count = sum(1 for alert in alerts if alert.status == "resolved")\n    silenced_count = sum(1 for alert in alerts if alert.status == "silenced")\n\n    previous_status = group.status\n\n    group.alert_count = total\n    group.firing_count = firing_count\n    group.acknowledged_count = acknowledged_count\n    group.resolved_count = resolved_count\n    group.silenced_count = silenced_count\n    group.silenced = total > 0 and silenced_count == total\n\n    if alerts:\n        newest_alert = alerts[0]\n\n        group.title = newest_alert.title\n        group.message = newest_alert.message\n        group.severity = newest_alert.severity\n        group.last_seen_at = newest_alert.last_seen_at or now\n\n        if not group.first_seen_at:\n            group.first_seen_at = min(\n                alert.first_seen_at\n                for alert in alerts\n                if alert.first_seen_at\n            )\n\n        common_labels, label_values = _collect_group_labels(alerts)\n\n        group.common_labels = common_labels\n        group.label_values = label_values\n        group.payload_summary = {\n            "latest_alert_id": newest_alert.id,\n            "latest_dedup_key": newest_alert.dedup_key,\n            "latest_external_id": newest_alert.external_id,\n            "latest_payload": newest_alert.payload or {},\n        }\n\n    if total == 0:\n        pass\n\n    elif resolved_count == total:\n        group.status = "resolved"\n\n        if not group.resolved_at:\n            group.resolved_at = now\n\n        alerts_without_resolved_at = [\n            alert\n            for alert in alerts\n            if not alert.resolved_at\n        ]\n\n        for alert in alerts_without_resolved_at:\n            alert.resolved_at = now\n            alert.updated_at = now\n            alert.save()\n\n    elif firing_count > 0:\n        if group.status == "acknowledged":\n            group.status = "acknowledged"\n        else:\n            group.status = "firing"\n            group.resolved_at = None\n            group.resolved_by = None\n\n    elif silenced_count > 0:\n        group.status = "silenced"\n        group.resolved_at = None\n        group.resolved_by = None\n\n    elif acknowledged_count > 0:\n        group.status = "acknowledged"\n        group.resolved_at = None\n        group.resolved_by = None\n\n    else:\n        group.status = previous_status\n\n    if previous_status != group.status:\n        group.previous_status = previous_status\n\n    group.updated_at = now\n    group.save()\n\n    return group\n\n\ndef acknowledge_alert_group(group_id, user_id=None):\n    """Acknowledge alert group."""\n\n    group = AlertGroup.get_by_id(group_id)\n    group.previous_status = group.status\n    group.status = "acknowledged"\n    group.acknowledged_by = user_id\n    group.acknowledged_at = datetime.utcnow()\n    group.save()\n    return group\n\n\ndef resolve_alert_group(group_id, user_id=None):\n    """Resolve alert group and all child alerts."""\n\n    now = datetime.utcnow()\n    group = AlertGroup.get_by_id(group_id)\n\n    (\n        Alert.update(status="resolved", resolved_at=now)\n        .where(\n            (Alert.group == group.id)\n            & (Alert.status != "resolved")\n        )\n        .execute()\n    )\n\n    group.previous_status = group.status\n    group.status = "resolved"\n    group.resolved_by = user_id\n    group.resolved_at = now\n    group.firing_count = 0\n    group.acknowledged_count = 0\n    group.resolved_count = Alert.select().where(Alert.group == group.id).count()\n    group.updated_at = now\n    group.save()\n\n    return group\n\n\ndef list_firing_alert_groups():\n    """Return groups for reminder/escalation processing."""\n\n    return list(\n        AlertGroup.select()\n        .where(\n            (AlertGroup.status == "firing")\n            & (AlertGroup.merged_into.is_null(True))\n        )\n    )\n\n\ndef record_group_notification_time(group, now):\n    group.last_notification_at = now\n    group.save()\n    return group\n\n\ndef increment_group_reminder(group, now):\n    group.reminder_count += 1\n    group.last_notification_at = now\n    group.save()\n    return group\n\n\ndef list_group_events(group_id):\n    """Return group-level and child alert events."""\n\n    alert_ids = Alert.select(Alert.id).where(Alert.group == group_id)\n\n    return list(\n        AlertEvent.select()\n        .where(\n            (AlertEvent.group == group_id)\n            | (AlertEvent.alert.in_(alert_ids))\n        )\n        .order_by(AlertEvent.id.asc())\n    )\n\n\ndef merge_alert_groups(target_group_id, source_group_ids, user_id=None, reason=None):\n    """Merge source groups into target group."""\n\n    target = AlertGroup.get_by_id(target_group_id)\n    now = datetime.utcnow()\n\n    for source_id in source_group_ids:\n        if int(source_id) == int(target_group_id):\n            continue\n\n        source = AlertGroup.get_by_id(source_id)\n\n        (\n            Alert.update(group=target.id, group_key=target.group_key)\n            .where(Alert.group == source.id)\n            .execute()\n        )\n\n        source.previous_status = source.status\n        source.status = "merged"\n        source.merged_into = target.id\n        source.merged_by = user_id\n        source.merged_at = now\n        source.merge_reason = reason\n        source.updated_at = now\n        source.save()\n\n        AlertGroupMerge.create(\n            source_group=source.id,\n            target_group=target.id,\n            merged_by=user_id,\n            reason=reason,\n        )\n\n        create_alert_event(\n            group_id=source.id,\n            event_type="merged",\n            message=f"Merged into group #{target.id}",\n            user_id=user_id,\n        )\n\n    recalculate_alert_group(target)\n\n    create_alert_event(\n        group_id=target.id,\n        event_type="merge_target_updated",\n        message="Alert groups merged into this group",\n        user_id=user_id,\n    )\n\n    return target\n\n\ndef normalize_alert_page(page):\n    """\n    Return a safe page number.\n    """\n    try:\n        page = int(page)\n    except (TypeError, ValueError):\n        return 1\n\n    return max(page, 1)\n\n\ndef normalize_alert_page_size(page_size):\n    """\n    Return a safe page size.\n\n    The upper limit protects the API from loading too many rows at once.\n    """\n    try:\n        page_size = int(page_size)\n    except (TypeError, ValueError):\n        return 25\n\n    page_size = max(page_size, 1)\n\n    return min(page_size, MAX_ALERTS_PAGE_SIZE)\n\n\ndef normalize_alert_order(order):\n    """\n    Return a safe sort order.\n    """\n    if order == "asc":\n        return "asc"\n\n    return "desc"\n\n\ndef find_existing_alert(\n    source,\n    dedup_key,\n    window_seconds=None,\n    route_id=None,\n):\n    """\n    Return an existing non-resolved alert by source and dedup key.\n\n    Resolved alerts are final occurrences. If the same fingerprint fires again\n    after resolve, a new alert must be created with a new id and first_seen_at.\n\n    route_id can scope lifecycle actions submitted with a route intake token.\n    This prevents a PagerDuty-compatible routing key from acknowledging or\n    resolving an alert owned by another webhook route that reused a dedup key.\n    """\n    query = Alert.select().where(\n        (Alert.source == source)\n        & (Alert.dedup_key == dedup_key)\n        & (Alert.status != "resolved")\n    )\n\n    if route_id is not None:\n        query = query.where(Alert.route == route_id)\n\n    return query.order_by(Alert.id.desc()).first()\n\n\ndef create_alert(**kwargs):\n    """\n    Create an alert.\n    """\n\n    return Alert.create(**kwargs)\n\n\ndef update_alert_from_payload(alert, alert_data, status, group_key):\n    """\n    Update an alert from normalized payload data.\n    """\n    now = datetime.utcnow()\n\n    alert.last_seen_at = now\n    previous_status = alert.status\n    alert.previous_status = previous_status\n    alert.last_seen_at = datetime.utcnow()\n    alert.payload = alert_data.get("payload")\n    alert.labels = alert_data.get("labels")\n    alert.message = alert_data.get("message")\n    alert.severity = alert_data.get("severity")\n    alert.group_key = group_key\n    if previous_status in ("acknowledged", "silenced") and status == "firing":\n        alert.status = previous_status\n    else:\n        alert.status = status\n    if status == "resolved" and not alert.resolved_at:\n        alert.resolved_at = now\n    alert.save()\n    return alert, previous_status\n\n\ndef create_alert_event(alert_id=None, event_type=None, message=None, user_id=None, group_id=None):\n    """Create alert/group event."""\n\n    return AlertEvent.create(\n        alert=alert_id,\n        group=group_id,\n        event_type=event_type,\n        message=message,\n        user=user_id,\n    )\n\n\ndef list_alert_events(alert_id):\n    """\n    Return alert events.\n    """\n\n    return list(\n        AlertEvent.select()\n        .where(AlertEvent.alert == alert_id)\n        .order_by(AlertEvent.id.asc())\n    )\n\n\ndef schedule_alert_group_notification(group, due_at, reason="notification"):\n    """Schedule a pending notification for an alert group."""\n\n    if (\n        group.notification_pending\n        and group.notification_due_at\n        and group.notification_due_at <= due_at\n    ):\n        return group\n\n    group.notification_pending = True\n    group.notification_due_at = due_at\n    group.notification_reason = reason\n    group.updated_at = datetime.utcnow()\n    group.save()\n\n    return group\n\n\ndef clear_alert_group_notification(group):\n    """Clear pending group notification."""\n\n    group.notification_pending = False\n    group.notification_due_at = None\n    group.notification_reason = None\n    group.updated_at = datetime.utcnow()\n    group.save()\n\n    return group\n\n\ndef mark_alert_group_notification_sent(group, now=None):\n    """Mark group notification as sent."""\n\n    now = now or datetime.utcnow()\n\n    group.notification_pending = False\n    group.notification_due_at = None\n    group.notification_reason = None\n    group.last_notification_at = now\n    group.updated_at = now\n    group.save()\n\n    return group\n\n\ndef list_due_alert_group_notifications(now=None, limit=100):\n    """Return groups with due pending notifications."""\n\n    now = now or datetime.utcnow()\n\n    return list(\n        AlertGroup\n        .select()\n        .where(\n            (AlertGroup.notification_pending == True)\n            & (AlertGroup.notification_due_at.is_null(False))\n            & (AlertGroup.notification_due_at <= now)\n            & (AlertGroup.merged_into.is_null(True))\n            & (AlertGroup.status != "merged")\n        )\n        .order_by(AlertGroup.notification_due_at.asc(), AlertGroup.id.asc())\n        .limit(limit)\n    )\n\n\ndef create_alert_comment(\n    *,\n    group_id: int | None = None,\n    alert_id: int | None = None,\n    user_id: int | None = None,\n    body: str,\n) -> AlertComment:\n    if not group_id and not alert_id:\n        raise ValueError("group_id or alert_id is required")\n\n    return AlertComment.create(\n        group=group_id,\n        alert=alert_id,\n        user=user_id,\n        body=body,\n        created_at=datetime.utcnow(),\n        updated_at=datetime.utcnow(),\n    )\n\n\ndef list_group_comments(\n    group_id: int,\n    *,\n    include_deleted: bool = False,\n) -> list[AlertComment]:\n    query = (\n        AlertComment\n        .select(AlertComment, User)\n        .join(User, JOIN.LEFT_OUTER)\n        .switch(AlertComment)\n        .where(AlertComment.group == group_id)\n        .order_by(AlertComment.created_at.asc(), AlertComment.id.asc())\n    )\n\n    if not include_deleted:\n        query = query.where(AlertComment.deleted == False)  # noqa: E712\n\n    return list(query)\n\n\ndef list_alert_comments(\n    alert_id: int,\n    *,\n    include_deleted: bool = False,\n) -> list[AlertComment]:\n    query = (\n        AlertComment\n        .select(AlertComment, User)\n        .join(User, JOIN.LEFT_OUTER)\n        .switch(AlertComment)\n        .where(AlertComment.alert == alert_id)\n        .order_by(AlertComment.created_at.asc(), AlertComment.id.asc())\n    )\n\n    if not include_deleted:\n        query = query.where(AlertComment.deleted == False)  # noqa: E712\n\n    return list(query)\n\n\ndef get_alert_comment(comment_id: int) -> AlertComment | None:\n    return AlertComment.get_or_none(AlertComment.id == comment_id)\n\n\ndef soft_delete_alert_comment(comment_id: int) -> bool:\n    updated = (\n        AlertComment\n        .update(\n            deleted=True,\n            deleted_at=datetime.utcnow(),\n            updated_at=datetime.utcnow(),\n        )\n        .where(\n            AlertComment.id == comment_id,\n            AlertComment.deleted == False,  # noqa: E712\n        )\n        .execute()\n    )\n\n    return bool(updated)\n\n\ndef update_alert_comment(\n    comment_id: int,\n    *,\n    body: str,\n) -> AlertComment | None:\n    comment = AlertComment.get_or_none(\n        AlertComment.id == comment_id,\n        AlertComment.deleted == False,  # noqa: E712\n    )\n\n    if not comment:\n        return None\n\n    comment.body = body\n    comment.updated_at = datetime.utcnow()\n    comment.save(only=[\n        AlertComment.body,\n        AlertComment.updated_at,\n    ])\n\n    return comment\n\n\ndef list_incident_stakeholders(group_id, *, include_inactive=False):\n    """Return incident stakeholders."""\n    query = (\n        IncidentStakeholder\n        .select()\n        .where(IncidentStakeholder.group == group_id)\n        .order_by(\n            IncidentStakeholder.created_at.asc(),\n            IncidentStakeholder.id.asc(),\n        )\n    )\n\n    if not include_inactive:\n        query = query.where(IncidentStakeholder.active == True)  # noqa: E712\n\n    return list(query)\n\n\ndef list_incident_responders(group_id):\n    """Return incident responder requests."""\n    return list(\n        IncidentResponder\n        .select()\n        .where(IncidentResponder.group == group_id)\n        .order_by(\n            IncidentResponder.created_at.asc(),\n            IncidentResponder.id.asc(),\n        )\n    )\n\n\ndef create_alert_explain_trace(\n    *,\n    trace_id,\n    mode="live",\n    source=None,\n    dedup_key=None,\n    input_summary=None,\n):\n    return AlertExplainTrace.create(\n        trace_id=trace_id,\n        mode=mode,\n        source=source,\n        dedup_key=dedup_key,\n        input_summary=input_summary or {},\n    )\n\n\ndef create_alert_explain_step(\n    *,\n    trace,\n    position,\n    stage,\n    code,\n    status,\n    title,\n    message=None,\n    data=None,\n):\n    return AlertExplainStep.create(\n        trace=trace,\n        position=position,\n        stage=stage,\n        code=code,\n        status=status,\n        title=title,\n        message=message,\n        data=data or {},\n    )\n\n\ndef attach_alert_explain_trace(trace, *, group=None, alert=None):\n    changed = False\n\n    if group and trace.group_id != group.id:\n        trace.group = group\n        changed = True\n\n    if alert and trace.alert_id != alert.id:\n        trace.alert = alert\n        changed = True\n\n    if changed:\n        trace.save(\n            only=[\n                AlertExplainTrace.group,\n                AlertExplainTrace.alert,\n            ]\n        )\n\n    return trace\n\n\ndef finish_alert_explain_trace(\n    trace,\n    *,\n    status="completed",\n    outcome=None,\n    reason=None,\n    result=None,\n    finished_at=None,\n):\n    from datetime import datetime\n\n    trace.status = status\n    trace.outcome = outcome\n    trace.reason = reason\n    trace.result = result or {}\n    trace.finished_at = finished_at or datetime.utcnow()\n\n    trace.save(\n        only=[\n            AlertExplainTrace.status,\n            AlertExplainTrace.outcome,\n            AlertExplainTrace.reason,\n            AlertExplainTrace.result,\n            AlertExplainTrace.finished_at,\n        ]\n    )\n\n    return trace\n\n\ndef get_alert_explain_trace(trace_id):\n    return AlertExplainTrace.get_or_none(\n        AlertExplainTrace.trace_id == trace_id\n    )\n\n\ndef list_alert_explain_traces_for_group(group_id):\n    return list(\n        AlertExplainTrace\n        .select()\n        .where(AlertExplainTrace.group == group_id)\n        .order_by(AlertExplainTrace.id.desc())\n    )\n\n\ndef list_alert_explain_steps(trace):\n    return list(\n        AlertExplainStep\n        .select()\n        .where(AlertExplainStep.trace == trace)\n        .order_by(AlertExplainStep.position.asc(), AlertExplainStep.id.asc())\n    )\n\n\ndef delete_alert_explain_traces_older_than(cutoff):\n    stale_trace_ids = (\n        AlertExplainTrace\n        .select(AlertExplainTrace.id)\n        .where(AlertExplainTrace.started_at < cutoff)\n    )\n\n    steps_deleted = (\n        AlertExplainStep\n        .delete()\n        .where(AlertExplainStep.trace.in_(stale_trace_ids))\n        .execute()\n    )\n\n    traces_deleted = (\n        AlertExplainTrace\n        .delete()\n        .where(AlertExplainTrace.id.in_(stale_trace_ids))\n        .execute()\n    )\n\n    return {\n        "traces_deleted": traces_deleted,\n        "steps_deleted": steps_deleted,\n        "cutoff": cutoff,\n    }\n', 'default')], 'app/views/integrations_view.py': [('380b2fa626965b81a325fd88b3a8c0a791626f27560c9b381ff10558a582032a', 'e9cd43080fe8cd1947dfb0e42ca8a54dfe6d4bd7430f2ce4effb612ae6d44ec4', 'import logging\nfrom datetime import datetime\n\nfrom flask import Blueprint, jsonify, request\nfrom peewee import DoesNotExist\n\nfrom app.api.schemas.integrations import (\n    AlertmanagerWebhookSchema,\n    AwsSnsEnvelopeSchema,\n    GenericWebhookSchema,\n    GrafanaWebhookSchema,\n    LibreNMSWebhookSchema,\n    RmonWebhookSchema,\n    SentryWebhookSchema,\n    ZabbixWebhookSchema,\n)\nfrom app.services.serializers.alerts import serialize_alert_processing_result\nfrom app.settings import Config\nfrom app.modules.db import channels_repo, users_repo, alerts_repo, routes_repo\nfrom app.services.alerts.actions import acknowledge_alert, resolve_alert\nfrom app.services.alerts.lifecycle import upsert_alert\nfrom app.services.integrations.auth import require_alert_token\nfrom app.services.integrations.normalizers.sentry import normalize_sentry\nfrom app.services.integrations.normalizers.webhook import (\n    is_pagerduty_events_v2,\n    normalize_webhook,\n)\nfrom app.services.integrations.normalizers.zabbix import normalize_zabbix\nfrom app.services.integrations.normalizers.alertmanager import normalize_alertmanager\nfrom app.services.integrations.normalizers.librenms import normalize_librenms\nfrom app.services.integrations.normalizers.grafana import normalize_grafana\nfrom app.services.integrations.normalizers.rmon import normalize_rmon\nfrom app.services.validation import make_error_response, validate_body\nfrom app.notifiers.voice.loader import create_voice_provider\nfrom app.modules.db.models import UserNotificationDelivery\nfrom app.services.integrations.sentry import validate_sentry_route_signature\nfrom app.services.integrations.aws_sns import (\n    AwsSnsError,\n    confirm_aws_sns_subscription,\n    validate_aws_sns_message,\n)\nfrom app.services.integrations.normalizers.aws_sns import (\n    normalize_aws_sns,\n)\nfrom app.notifiers.slack.actions import (\n    SlackActionError,\n    handle_slack_action,\n)\n\nintegrations_bp = Blueprint("integrations_api", __name__)\n\n\n@integrations_bp.route("/alertmanager", methods=["POST"])\n@require_alert_token()\ndef alertmanager_webhook():\n    """\n    Receive alerts from Prometheus Alertmanager.\n    """\n\n    payload, error = validate_body(AlertmanagerWebhookSchema)\n    if error:\n        return error\n    return process_incoming_alerts(normalize_alertmanager(payload.model_dump()))\n\n\n@integrations_bp.route("/grafana", methods=["POST"])\n@require_alert_token()\ndef grafana_webhook():\n    """Receive alerts from Grafana Alerting."""\n    payload, error = validate_body(GrafanaWebhookSchema)\n    if error:\n        return error\n\n    return process_incoming_alerts(\n        normalize_grafana(payload.model_dump())\n    )\n\n\n@integrations_bp.route("/rmon", methods=["POST"])\n@require_alert_token()\ndef rmon_webhook():\n    """Receive alerts from RMON."""\n\n    payload, error = validate_body(RmonWebhookSchema)\n\n    if error:\n        return error\n\n    return process_incoming_alerts(\n        normalize_rmon(payload.model_dump())\n    )\n\n\n@integrations_bp.route("/aws-sns/<int:route_id>", methods=["POST"])\ndef aws_sns_webhook(route_id):\n    """Receive signed Amazon SNS and CloudWatch notifications."""\n    try:\n        route = routes_repo.get_route(route_id)\n    except DoesNotExist:\n        return make_error_response(\n            error="not_found",\n            message="AWS SNS route was not found.",\n            status_code=404,\n        )\n\n    if route.source != "aws_sns":\n        return make_error_response(\n            error="route_source_mismatch",\n            message="Route source must be aws_sns.",\n            status_code=400,\n        )\n\n    if not route.enabled or route.deleted:\n        return make_error_response(\n            error="route_disabled",\n            message="AWS SNS route is disabled.",\n            status_code=403,\n        )\n\n    if not route.team or route.team.deleted or not route.team.active:\n        return make_error_response(\n            error="route_team_inactive",\n            message="AWS SNS route team is inactive.",\n            status_code=403,\n        )\n\n    if route.team.group and (route.team.group.deleted or not route.team.group.active):\n        return make_error_response(\n            error="route_group_inactive",\n            message="AWS SNS route group is inactive.",\n            status_code=403,\n        )\n\n    payload, error = validate_body(\n        AwsSnsEnvelopeSchema\n    )\n\n    if error:\n        return error\n\n    envelope = payload.model_dump(\n        by_alias=True,\n        exclude_none=True,\n    )\n\n    header_type = request.headers.get(\n        "x-amz-sns-message-type"\n    )\n    header_topic_arn = request.headers.get(\n        "x-amz-sns-topic-arn"\n    )\n\n    if header_type and header_type != envelope["Type"]:\n        return make_error_response(\n            error="aws_sns_header_mismatch",\n            message=(\n                "Amazon SNS message type header "\n                "does not match the payload."\n            ),\n            status_code=400,\n        )\n\n    if (\n            header_topic_arn\n            and header_topic_arn != envelope["TopicArn"]\n    ):\n        return make_error_response(\n            error="aws_sns_header_mismatch",\n            message=(\n                "Amazon SNS Topic ARN header "\n                "does not match the payload."\n            ),\n            status_code=400,\n        )\n\n    integration_config = (\n            route.integration_config or {}\n    )\n    aws_sns_config = dict(\n        integration_config.get("aws_sns") or {}\n    )\n    expected_topic_arn = str(\n        aws_sns_config.get("topic_arn") or ""\n    ).strip()\n\n    try:\n        validate_aws_sns_message(\n            envelope,\n            expected_topic_arn,\n        )\n\n        if envelope["Type"] == (\n                "SubscriptionConfirmation"\n        ):\n            confirm_aws_sns_subscription(\n                envelope["SubscribeURL"],\n                expected_topic_arn,\n            )\n\n            return jsonify({\n                "status": "confirmed",\n                "message_id": envelope["MessageId"],\n                "topic_arn": envelope["TopicArn"],\n            })\n\n        if envelope["Type"] == (\n                "UnsubscribeConfirmation"\n        ):\n            return jsonify({\n                "status": "unsubscribed",\n                "message_id": envelope["MessageId"],\n                "topic_arn": envelope["TopicArn"],\n            })\n    except AwsSnsError as exc:\n        return make_error_response(\n            error=exc.code,\n            message=exc.message,\n            status_code=exc.status_code,\n        )\n\n    request.current_intake_route = route\n    request.current_auth_type = "aws_sns_signature"\n\n    return process_incoming_alerts(\n        normalize_aws_sns(envelope)\n    )\n\n\n@integrations_bp.route("/zabbix", methods=["POST"])\n@require_alert_token()\ndef zabbix_webhook():\n    """\n    Receive alerts from Zabbix.\n    """\n\n    payload, error = validate_body(ZabbixWebhookSchema)\n    if error:\n        return error\n    return process_incoming_alerts(normalize_zabbix(payload.model_dump()))\n\n\ndef _pagerduty_events_success(dedup_key):\n    """Return the standard successful Events API v2 response shape."""\n\n    return jsonify({\n        "status": "success",\n        "message": "Event processed",\n        "dedup_key": dedup_key,\n    }), 202\n\n\ndef _process_pagerduty_events_v2(alert_data):\n    """Process one PagerDuty Events API v2-compatible webhook event."""\n\n    action = alert_data.get("lifecycle_action")\n    dedup_key = alert_data.get("dedup_key")\n    intake_route = getattr(request, "current_intake_route", None)\n\n    if not intake_route or intake_route.source != "webhook":\n        return make_error_response(\n            error="route_source_mismatch",\n            message="PagerDuty routing_key must belong to a webhook route.",\n            status_code=400,\n        )\n\n    if action == "trigger":\n        response, status_code = process_incoming_alerts([alert_data])\n\n        if status_code >= 400:\n            return response, status_code\n\n        return _pagerduty_events_success(dedup_key)\n\n    existing_alert = alerts_repo.find_existing_alert(\n        source="webhook",\n        dedup_key=dedup_key,\n        route_id=intake_route.id,\n    )\n\n    if existing_alert and existing_alert.group:\n        group_id = existing_alert.group.id\n\n        if action == "acknowledge":\n            acknowledge_alert(group_id)\n        elif action == "resolve":\n            resolve_alert(group_id)\n\n    logging.getLogger("oncall.alerts").info(\n        "PagerDuty Events API v2 lifecycle event processed",\n        extra={\n            "extra": {\n                "event_type": "pagerduty_events_api_v2",\n                "event_action": action,\n                "dedup_key": dedup_key,\n                "route_id": intake_route.id,\n                "matched_alert_id": existing_alert.id if existing_alert else None,\n                "matched_group_id": (\n                    existing_alert.group.id\n                    if existing_alert and existing_alert.group\n                    else None\n                ),\n            }\n        },\n    )\n\n    # PagerDuty accepts follow-up events asynchronously. Keep unknown or\n    # already-resolved dedup keys as successful no-ops for compatibility.\n    return _pagerduty_events_success(dedup_key)\n\n\n@integrations_bp.route("/webhook", methods=["POST"])\n@require_alert_token(allow_json_routing_key=True)\ndef generic_webhook():\n    """Receive generic or PagerDuty Events API v2-compatible alerts."""\n\n    payload, error = validate_body(GenericWebhookSchema)\n    if error:\n        return error\n\n    raw_payload = payload.model_dump()\n    normalized_alerts = normalize_webhook(raw_payload)\n\n    if is_pagerduty_events_v2(raw_payload):\n        return _process_pagerduty_events_v2(normalized_alerts[0])\n\n    return process_incoming_alerts(normalized_alerts)\n\n\n@integrations_bp.route("/sentry/<int:route_id>", methods=["POST"])\ndef sentry_webhook(route_id):\n    """Receive signed webhooks from Sentry Internal Integration."""\n    try:\n        route = routes_repo.get_route(route_id)\n    except DoesNotExist:\n        return jsonify({\n            "error": "route_not_found",\n            "message": "Sentry route was not found",\n        }), 404\n\n    if route.source != "sentry":\n        return jsonify({\n            "error": "route_source_mismatch",\n            "message": "Route source must be sentry",\n        }), 400\n\n    if not route.enabled or route.deleted:\n        return jsonify({\n            "error": "route_disabled",\n            "message": "Sentry route is disabled",\n        }), 403\n\n    if not route.team or route.team.deleted or not route.team.active:\n        return jsonify({\n            "error": "route_team_inactive",\n            "message": "Sentry route team is inactive",\n        }), 403\n\n    if route.team.group and (\n            route.team.group.deleted or not route.team.group.active\n    ):\n        return jsonify({\n            "error": "route_group_inactive",\n            "message": "Sentry route group is inactive",\n        }), 403\n\n    raw_body = request.get_data(cache=True)\n\n    signature_error, status_code = validate_sentry_route_signature(\n        route,\n        raw_body,\n        request.headers,\n    )\n\n    if signature_error:\n        return jsonify(signature_error), status_code\n\n    payload, error = validate_body(SentryWebhookSchema)\n    if error:\n        return error\n\n    request.current_intake_route = route\n    request.current_auth_type = "sentry_signature"\n\n    return process_incoming_alerts(\n        normalize_sentry(\n            payload.model_dump(),\n            headers=dict(request.headers),\n            route_config=route.integration_config or {},\n        )\n    )\n\n\n@integrations_bp.route("/librenms", methods=["POST"])\n@require_alert_token()\ndef librenms_webhook():\n    """Receive alerts from LibreNMS API transport."""\n    payload, error = validate_body(LibreNMSWebhookSchema)\n\n    if error:\n        return error\n\n    return process_incoming_alerts(\n        normalize_librenms(payload.model_dump())\n    )\n\n\ndef process_incoming_alerts(normalized_alerts):\n    """\n    Store normalized alerts and return created, updated, ignored or failed records.\n\n    Routing decisions are intentionally delegated to upsert_alert(), because\n    upsert_alert() creates an explain trace for both successful and stopped\n    processing paths.\n    """\n    items = []\n    intake_route = getattr(request, "current_intake_route", None)\n\n    for alert_data in normalized_alerts:\n        if intake_route:\n            # The route intake token is the routing boundary.\n            # Alerts submitted with this token are forced to this route,\n            # which already defines team, rotation and notification channels.\n            alert_data["forced_route_id"] = intake_route.id\n            alert_data["forced_team_id"] = intake_route.team.id\n            alert_data["team_slug"] = intake_route.team.slug\n\n        result = upsert_alert(alert_data)\n\n        item = serialize_alert_processing_result(\n            result,\n            status=alert_data.get("status") or "firing",\n            routing_error=alert_data.get("routing_error"),\n        )\n\n        items.append(item)\n\n    logging.getLogger("oncall.alerts").info(\n        "incoming alerts processed",\n        extra={\n            "extra": {\n                "event_type": "alert_intake",\n                "source": normalized_alerts[0].get("source") if normalized_alerts else None,\n                "alerts_count": len(normalized_alerts),\n                "route_id": intake_route.id if intake_route else None,\n                "team_id": intake_route.team.id if intake_route else None,\n                "results": items,\n            }\n        },\n    )\n\n    return jsonify(items), _incoming_alerts_status_code(items)\n\n\ndef _incoming_alerts_status_code(items):\n    if not items:\n        return 200\n\n    has_group = any(item.get("group_id") for item in items)\n\n    has_failure = any(\n        item.get("processing_status") == "failed"\n        or item.get("outcome") == "routing_failed"\n        for item in items\n    )\n\n    has_stopped = any(\n        item.get("processing_status") == "stopped"\n        for item in items\n    )\n\n    if has_failure and has_group:\n        return 207\n\n    if has_failure:\n        return 400\n\n    if has_stopped and not has_group:\n        return 202\n\n    return 200\n\n\n@integrations_bp.route("/mattermost/actions", methods=["POST"])\ndef mattermost_action():\n    """\n    Handle Mattermost interactive message button callbacks.\n    """\n\n    payload = request.json or {}\n    context = payload.get("context") or {}\n    alert_id = context.get("alert_id")\n    channel_id = context.get("channel_id")\n    action = context.get("action")\n    secret = context.get("secret")\n\n    if not alert_id or not channel_id or action not in {"acknowledge", "resolve"}:\n        return jsonify({"ephemeral_text": "Invalid action payload"}), 400\n\n    channel = channels_repo.get_channel(int(channel_id))\n    config = channel.config or {}\n    expected_secret = config.get("callback_secret") or Config.MATTERMOST_ACTION_SECRET\n\n    if secret != expected_secret:\n        return jsonify({"ephemeral_text": "Action rejected"}), 403\n\n    mattermost_user_id = payload.get("user_id")\n    user = users_repo.get_user_by_mattermost_id(mattermost_user_id)\n    user_id = user.id if user else None\n\n    if action == "acknowledge":\n        alert = acknowledge_alert(int(alert_id), user_id=user_id)\n        return jsonify({\n            "ephemeral_text": f"Alert #{alert.id} acknowledged",\n            "skip_slack_parsing": True,\n        })\n\n    alert = resolve_alert(int(alert_id), user_id=user_id)\n    return jsonify({\n        "ephemeral_text": f"Alert #{alert.id} resolved",\n        "skip_slack_parsing": True,\n    })\n\n\n@integrations_bp.route("/slack/actions", methods=["POST"])\ndef slack_action():\n    """Handle Slack Block Kit alert actions."""\n    raw_body = request.get_data(cache=True)\n\n    try:\n        result = handle_slack_action(\n            raw_body=raw_body,\n            timestamp=request.headers.get(\n                "X-Slack-Request-Timestamp"\n            ),\n            signature=request.headers.get(\n                "X-Slack-Signature"\n            ),\n        )\n    except SlackActionError as exc:\n        return jsonify(\n            {\n                "error": exc.error,\n                "message": exc.message,\n            }\n        ), exc.status_code\n\n    return jsonify(result)\n\n\n@integrations_bp.route("/voice/rule-callback/<int:delivery_id>/<secret>", methods=["POST"])\ndef voice_rule_callback(delivery_id, secret):\n    """Handle voice callbacks for user notification rules."""\n    expected_secret = Config.VOICE_CALLBACK_SECRET\n\n    if not expected_secret or secret != expected_secret:\n        return jsonify({"error": "voice callback rejected"}), 403\n\n    delivery = UserNotificationDelivery.get_or_none(\n        UserNotificationDelivery.id == delivery_id\n    )\n\n    if not delivery:\n        return jsonify({"error": "notification delivery was not found"}), 404\n\n    payload = request.get_json(silent=True)\n\n    if payload is None:\n        payload = request.form.to_dict(flat=True)\n\n    provider = create_voice_provider(\n        Config.VOICE_PROVIDER,\n        Config.VOICE_PROVIDER_CONFIG,\n    )\n\n    events = provider.parse_callback(\n        payload=payload or {},\n        headers=dict(request.headers),\n        raw_body=request.get_data(),\n        query_args=request.args.to_dict(flat=True),\n    )\n\n    if not events:\n        return jsonify({"status": "ignored", "events": 0})\n\n    processed = []\n\n    for event in events:\n        processed.append(\n            _process_voice_rule_callback_event(delivery, event)\n        )\n\n    return jsonify(\n        {\n            "status": "processed",\n            "events": processed,\n        }\n    )\n\n\ndef _process_voice_rule_callback_event(delivery, event):\n    delivery.provider_status = event.status\n    delivery.provider_payload = event.raw\n    delivery.updated_at = datetime.utcnow()\n    delivery.save()\n\n    alert = delivery.alert\n\n    action = None\n\n    if event.action in {"acknowledge", "resolve"}:\n        action = event.action\n    elif event.digit:\n        action = {\n            "1": "acknowledge",\n            "2": "resolve",\n        }.get(str(event.digit))\n\n    alerts_repo.create_alert_event(\n        alert.id,\n        f"voice_{event.event_type}",\n        _voice_rule_event_message(delivery, event, action),\n    )\n\n    if action == "acknowledge":\n        user_id = alert.assignee.id if alert.assignee else None\n        acknowledge_alert(alert.id, user_id=user_id)\n    elif action == "resolve":\n        user_id = alert.assignee.id if alert.assignee else None\n        resolve_alert(alert.id, user_id=user_id)\n\n    return {\n        "call_id": event.call_id,\n        "event_type": event.event_type,\n        "status": event.status,\n        "digit": event.digit,\n        "action": action,\n        "alert_id": alert.id,\n        "delivery_id": delivery.id,\n    }\n\n\ndef _voice_rule_event_message(delivery, event, action):\n    parts = [\n        f"Voice notification rule delivery #{delivery.id}",\n        f"event={event.event_type}",\n    ]\n\n    if event.status:\n        parts.append(f"status={event.status}")\n\n    if event.digit:\n        parts.append(f"digit={event.digit}")\n\n    if action:\n        parts.append(f"action={action}")\n\n    return ", ".join(parts)\n', 'default')], 'app/api/openapi/endpoints/integrations.py': [('d44953aca537b8ee7c4c5b347b8451478835d0785322d50bb65d9b17d8cb8add', 'a01259ab49ba6c37414c40e38607f6fc6a9cddfbd993b30f6fac427284c0e132', 'from app.api.openapi.common import response, path_param, json_body\n\n\ndef header_param(name, description, schema=None, required=False):\n    """Build a header parameter."""\n    return {\n        "name": name,\n        "in": "header",\n        "required": required,\n        "description": description,\n        "schema": schema or {"type": "string"},\n    }\n\n\nALERT_PROCESSING_RESULT_SCHEMA = {\n    "type": "object",\n    "description": "Result of processing one normalized incoming alert.",\n    "properties": {\n        "created": {\n            "type": "boolean",\n            "description": "True when a new alert group was created.",\n            "example": True,\n        },\n        "id": {\n            "type": "integer",\n            "nullable": True,\n            "description": "Alert group id. Kept for backward compatibility.",\n            "example": 123,\n        },\n        "group_id": {\n            "type": "integer",\n            "nullable": True,\n            "description": "Alert group id when an alert group exists.",\n            "example": 123,\n        },\n        "alert_id": {\n            "type": "integer",\n            "nullable": True,\n            "description": "Child alert id when an alert exists.",\n            "example": 456,\n        },\n        "status": {\n            "type": "string",\n            "nullable": True,\n            "description": "Resulting alert group or alert status.",\n            "example": "firing",\n        },\n        "outcome": {\n            "type": "string",\n            "description": "Alert processing outcome.",\n            "enum": [\n                "created",\n                "updated",\n                "resolved",\n                "routing_failed",\n                "suppressed",\n                "orphan_resolved_ignored",\n                "failed",\n            ],\n            "example": "created",\n        },\n        "processing_status": {\n            "type": "string",\n            "description": "High-level processing status.",\n            "enum": [\n                "completed",\n                "stopped",\n                "failed",\n            ],\n            "example": "completed",\n        },\n        "reason": {\n            "type": "string",\n            "nullable": True,\n            "description": "Reason for stopped or failed processing.",\n            "example": "Alert did not match any active route.",\n        },\n        "trace_id": {\n            "type": "string",\n            "nullable": True,\n            "description": (\n                "Explain trace id. Use GET /api/alerts/explain/{trace_id} "\n                "to inspect routing and processing steps."\n            ),\n            "example": "4fd2a8c9-8c2f-44e8-96fd-77b7f03e72f2",\n        },\n        "routing_error": {\n            "type": "string",\n            "nullable": True,\n            "description": "Routing error alias for routing_failed responses.",\n            "example": "Alert did not match any active route.",\n        },\n        "team_id": {\n            "type": "integer",\n            "nullable": True,\n            "description": "Matched team id.",\n        },\n        "team_slug": {\n            "type": "string",\n            "nullable": True,\n            "description": "Matched team slug.",\n        },\n        "route_id": {\n            "type": "integer",\n            "nullable": True,\n            "description": "Matched route id.",\n        },\n        "rotation_id": {\n            "type": "integer",\n            "nullable": True,\n            "description": "Resolved rotation id.",\n        },\n        "assignee": {\n            "type": "string",\n            "nullable": True,\n            "description": "Assigned username, when available.",\n        },\n    },\n    "additionalProperties": True,\n}\n\n\nALERT_PROCESSING_RESULT_LIST_SCHEMA = {\n    "type": "array",\n    "items": ALERT_PROCESSING_RESULT_SCHEMA,\n}\n\n\ndef incoming_alert_responses(success_description="Alerts accepted."):\n    """Build standard incoming alert processing responses."""\n    return {\n        "200": response(\n            success_description,\n            ALERT_PROCESSING_RESULT_LIST_SCHEMA,\n        ),\n        "202": response(\n            "Alerts processed, but no alert group was created.",\n            ALERT_PROCESSING_RESULT_LIST_SCHEMA,\n        ),\n        "207": response(\n            "Batch contains both accepted and failed alerts.",\n            ALERT_PROCESSING_RESULT_LIST_SCHEMA,\n        ),\n        "400": response(\n            "Invalid payload or all alerts failed routing.",\n        ),\n        "401": response("Route intake token or API token is required."),\n    }\n\n\nSENTRY_WEBHOOK_BODY_SCHEMA = {\n    "type": "object",\n    "description": (\n        "Sentry Internal Integration webhook payload. "\n        "The payload structure depends on Sentry-Hook-Resource and action. "\n        "IncidentRelay accepts event_alert, metric_alert and issue lifecycle events."\n    ),\n    "additionalProperties": True,\n    "properties": {\n        "action": {\n            "type": "string",\n            "description": "Sentry webhook action.",\n            "example": "triggered",\n        },\n        "actor": {\n            "type": "object",\n            "nullable": True,\n            "additionalProperties": True,\n            "description": "Sentry actor that caused the event, when present.",\n        },\n        "installation": {\n            "type": "object",\n            "nullable": True,\n            "additionalProperties": True,\n            "description": "Sentry integration installation object, when present.",\n        },\n        "data": {\n            "type": "object",\n            "additionalProperties": True,\n            "description": (\n                "Sentry event data. For issue alert rules this usually contains "\n                "event_alert, issue, event, project and organization. For metric "\n                "alerts it contains metric_alert. For issue lifecycle events it "\n                "contains issue."\n            ),\n            "properties": {\n                "event_alert": {\n                    "type": "object",\n                    "nullable": True,\n                    "additionalProperties": True,\n                    "description": "Sentry issue alert rule object.",\n                },\n                "metric_alert": {\n                    "type": "object",\n                    "nullable": True,\n                    "additionalProperties": True,\n                    "description": "Sentry metric alert object.",\n                },\n                "issue": {\n                    "type": "object",\n                    "nullable": True,\n                    "additionalProperties": True,\n                    "description": "Sentry issue/group object.",\n                },\n                "event": {\n                    "type": "object",\n                    "nullable": True,\n                    "additionalProperties": True,\n                    "description": "Sentry event object.",\n                },\n                "project": {\n                    "type": "object",\n                    "nullable": True,\n                    "additionalProperties": True,\n                    "description": "Sentry project object.",\n                },\n                "organization": {\n                    "type": "object",\n                    "nullable": True,\n                    "additionalProperties": True,\n                    "description": "Sentry organization object.",\n                },\n            },\n        },\n    },\n    "example": {\n        "action": "triggered",\n        "data": {\n            "event_alert": {"id": "event-rule-1", "name": "New issue alert"},\n            "issue": {\n                "id": "12345",\n                "shortId": "BACKEND-1",\n                "title": "ZeroDivisionError",\n                "level": "error",\n                "permalink": "https://sentry.example.com/issues/12345/",\n            },\n            "event": {\n                "event_id": "event-abc",\n                "environment": "production",\n                "message": "division by zero",\n            },\n            "project": {"id": 42, "slug": "backend-api", "name": "Backend API"},\n            "organization": {"slug": "acme", "name": "Acme"},\n        },\n    },\n}\n\n\nSENTRY_WEBHOOK_RESPONSE_SCHEMA = ALERT_PROCESSING_RESULT_LIST_SCHEMA\n\nSENTRY_WEBHOOK_PATH_ITEM = {\n    "post": {\n        "tags": ["integrations"],\n        "summary": "Receive Sentry webhook",\n        "description": (\n            "Receives signed webhooks from a Sentry Internal Integration. "\n            "The route id in the URL selects an IncidentRelay route with source=sentry. "\n            "The request is authenticated by Sentry-Hook-Signature using the "\n            "Sentry Client Secret stored in route integration_config. "\n            "This endpoint does not use IncidentRelay bearer intake tokens."\n        ),\n        "operationId": "receiveSentryWebhook",\n        "parameters": [\n            path_param("route_id", "Sentry IncidentRelay route id."),\n            header_param(\n                "Sentry-Hook-Signature",\n                "HMAC-SHA256 signature generated by Sentry using the Internal Integration Client Secret.",\n                {"type": "string", "minLength": 1},\n                required=True,\n            ),\n            header_param(\n                "Sentry-Hook-Resource",\n                "Sentry resource name. IncidentRelay uses this to normalize event_alert, metric_alert and issue events.",\n                {\n                    "type": "string",\n                    "enum": ["event_alert", "metric_alert", "issue"],\n                },\n                required=False,\n            ),\n        ],\n        "requestBody": json_body(\n            "Sentry Internal Integration webhook payload.",\n            SENTRY_WEBHOOK_BODY_SCHEMA,\n        ),\n        "responses": {\n            "200": response("Sentry webhook accepted.", SENTRY_WEBHOOK_RESPONSE_SCHEMA),\n            "400": response("Invalid payload or route source mismatch."),\n            "403": response("Missing or invalid Sentry-Hook-Signature, disabled route, or inactive team/group."),\n            "404": response("Sentry route was not found."),\n            "409": response("Sentry webhook secret is not configured for this route."),\n        },\n    }\n}\n\nVOICE_CALLBACK_BODY_SCHEMA = {\n    "type": "object",\n    "description": (\n        "Provider-specific voice callback payload. "\n        "IncidentRelay passes this payload to the selected voice provider module. "\n        "The provider normalizes it into status, DTMF or error events."\n    ),\n    "additionalProperties": True,\n    "properties": {\n        "call_id": {\n            "type": "string",\n            "description": "External provider call id.",\n            "example": "abc-123",\n        },\n        "event": {\n            "type": "string",\n            "description": "Provider event name.",\n            "example": "dtmf",\n        },\n        "event_type": {\n            "type": "string",\n            "description": "Normalized or provider-specific event type.",\n            "enum": ["status", "dtmf", "error"],\n            "example": "dtmf",\n        },\n        "status": {\n            "type": "string",\n            "description": "Provider call status.",\n            "example": "answered",\n        },\n        "digit": {\n            "type": "string",\n            "description": "DTMF digit pressed by the call recipient.",\n            "example": "1",\n        },\n        "action": {\n            "type": "string",\n            "description": (\n                "Optional normalized action. "\n                "If omitted, IncidentRelay maps digit through channel config dtmf_actions."\n            ),\n            "enum": ["acknowledge", "resolve"],\n            "example": "acknowledge",\n        },\n        "alert_id": {\n            "type": "integer",\n            "nullable": True,\n            "description": "Optional alert id returned by the provider.",\n            "example": 123,\n        },\n        "message": {\n            "type": "string",\n            "nullable": True,\n            "description": "Optional provider event message.",\n            "example": "User pressed 1",\n        },\n    },\n    "example": {\n        "call_id": "abc-123",\n        "event_type": "dtmf",\n        "status": "answered",\n        "digit": "1",\n    },\n}\n\n\nVOICE_CALLBACK_RESPONSE_SCHEMA = {\n    "type": "object",\n    "properties": {\n        "status": {\n            "type": "string",\n            "example": "processed",\n        },\n        "events": {\n            "type": "array",\n            "items": {\n                "type": "object",\n                "properties": {\n                    "call_id": {\n                        "type": "string",\n                        "example": "abc-123",\n                    },\n                    "event_type": {\n                        "type": "string",\n                        "example": "dtmf",\n                    },\n                    "status": {\n                        "type": "string",\n                        "nullable": True,\n                        "example": "answered",\n                    },\n                    "digit": {\n                        "type": "string",\n                        "nullable": True,\n                        "example": "1",\n                    },\n                    "action": {\n                        "type": "string",\n                        "nullable": True,\n                        "example": "acknowledge",\n                    },\n                    "alert_id": {\n                        "type": "integer",\n                        "nullable": True,\n                        "example": 123,\n                    },\n                },\n            },\n        },\n    },\n}\n\nSLACK_ACTION_FORM_SCHEMA = {\n    "type": "object",\n    "required": ["payload"],\n    "properties": {\n        "payload": {\n            "type": "string",\n            "description": (\n                "JSON-encoded Slack Block Kit interaction payload. "\n                "Slack sends this field using "\n                "application/x-www-form-urlencoded."\n            ),\n            "example": (\n                \'{"type":"block_actions",\'\n                \'"user":{"id":"U0123456789"},\'\n                \'"channel":{"id":"C0123456789"},\'\n                \'"actions":[{\'\n                \'"action_id":"incidentrelay_acknowledge",\'\n                \'"value":"{\\\\"action\\\\":\\\\"acknowledge\\\\",\'\n                \'\\\\"alert_id\\\\":123,\'\n                \'\\\\"channel_id\\\\":12}"\'\n                "}]}",\n            ),\n        },\n    },\n    "additionalProperties": True,\n}\n\n\nSLACK_ACTION_RESPONSE_SCHEMA = {\n    "type": "object",\n    "required": [\n        "ok",\n        "action",\n        "alert_id",\n        "user_id",\n    ],\n    "properties": {\n        "ok": {\n            "type": "boolean",\n            "description": "Whether the Slack action was processed.",\n            "example": True,\n        },\n        "action": {\n            "type": "string",\n            "enum": [\n                "acknowledge",\n                "resolve",\n            ],\n            "description": "Alert action performed by IncidentRelay.",\n            "example": "acknowledge",\n        },\n        "alert_id": {\n            "type": "integer",\n            "description": "IncidentRelay alert group ID.",\n            "example": 123,\n        },\n        "user_id": {\n            "type": "integer",\n            "nullable": True,\n            "description": (\n                "IncidentRelay user matched by Slack user ID. "\n                "Null when the Slack user is not linked."\n            ),\n            "example": 7,\n        },\n    },\n    "additionalProperties": False,\n}\n\n\nSLACK_ACTION_ERROR_SCHEMA = {\n    "type": "object",\n    "required": [\n        "error",\n        "message",\n    ],\n    "properties": {\n        "error": {\n            "type": "string",\n            "enum": [\n                "invalid_payload",\n                "invalid_action",\n                "invalid_signature",\n                "stale_request",\n                "action_rejected",\n                "alert_not_found",\n            ],\n            "description": "Machine-readable Slack action error.",\n            "example": "invalid_signature",\n        },\n        "message": {\n            "type": "string",\n            "description": "Human-readable error description.",\n            "example": "Slack request signature is invalid.",\n        },\n    },\n    "additionalProperties": False,\n}\n\nlibrenms_body = {\n    "type": "object",\n    "description": (\n        "LibreNMS API transport payload. Configure LibreNMS to send JSON "\n        "using alert template variables such as id, uid, state, severity, "\n        "title, msg, hostname and device_id."\n    ),\n    "additionalProperties": True,\n    "properties": {\n        "id": {\n            "type": "string",\n            "nullable": True,\n            "description": "LibreNMS alert id.",\n            "example": "12345",\n        },\n        "uid": {\n            "type": "string",\n            "nullable": True,\n            "description": "LibreNMS unique alert uid. Prefer this as stable external id.",\n            "example": "lnms-alert-12345",\n        },\n        "alert_id": {\n            "type": "string",\n            "nullable": True,\n            "description": "Alternative alert id field.",\n            "example": "12345",\n        },\n        "alert_uid": {\n            "type": "string",\n            "nullable": True,\n            "description": "Alternative alert uid field.",\n            "example": "lnms-alert-12345",\n        },\n        "external_id": {\n            "type": "string",\n            "nullable": True,\n            "description": (\n                "Stable LibreNMS alert identifier used by IncidentRelay for "\n                "deduplication. Must be the same for firing and recovery events."\n            ),\n            "example": "12345",\n        },\n        "state": {\n            "oneOf": [\n                {"type": "string"},\n                {"type": "integer"},\n            ],\n            "nullable": True,\n            "description": (\n                "LibreNMS alert state. 0/ok/clear/recovered/resolved are "\n                "normalized as resolved, other values are normalized as firing."\n            ),\n            "example": "1",\n        },\n        "status": {\n            "type": "string",\n            "nullable": True,\n            "description": "Alternative status/state field.",\n            "example": "firing",\n        },\n        "severity": {\n            "type": "string",\n            "nullable": True,\n            "description": "LibreNMS severity.",\n            "example": "critical",\n        },\n        "title": {\n            "type": "string",\n            "nullable": True,\n            "description": "Alert title.",\n            "example": "Device down",\n        },\n        "subject": {\n            "type": "string",\n            "nullable": True,\n            "description": "Alternative alert title.",\n            "example": "Device down",\n        },\n        "name": {\n            "type": "string",\n            "nullable": True,\n            "description": "LibreNMS alert rule name.",\n            "example": "Device down",\n        },\n        "rule": {\n            "type": "string",\n            "nullable": True,\n            "description": "Alternative alert rule name.",\n            "example": "Device down",\n        },\n        "message": {\n            "type": "string",\n            "nullable": True,\n            "description": "Alert message.",\n            "example": "Device router1 is unreachable",\n        },\n        "msg": {\n            "type": "string",\n            "nullable": True,\n            "description": "LibreNMS alert message variable.",\n            "example": "Device router1 is unreachable",\n        },\n        "description": {\n            "type": "string",\n            "nullable": True,\n            "description": "Alternative alert description.",\n            "example": "Device router1 is unreachable",\n        },\n        "alert_notes": {\n            "type": "string",\n            "nullable": True,\n            "description": "LibreNMS alert notes.",\n            "example": "Check uplink and power.",\n        },\n        "hostname": {\n            "type": "string",\n            "nullable": True,\n            "description": "Device hostname.",\n            "example": "router1",\n        },\n        "display": {\n            "type": "string",\n            "nullable": True,\n            "description": "Device display name.",\n            "example": "router1",\n        },\n        "sysName": {\n            "type": "string",\n            "nullable": True,\n            "description": "Device sysName.",\n            "example": "router1.example.com",\n        },\n        "device_id": {\n            "oneOf": [\n                {"type": "string"},\n                {"type": "integer"},\n            ],\n            "nullable": True,\n            "description": "LibreNMS device id.",\n            "example": "77",\n        },\n        "ip": {\n            "type": "string",\n            "nullable": True,\n            "description": "Device IP address.",\n            "example": "10.0.0.1",\n        },\n        "os": {\n            "type": "string",\n            "nullable": True,\n            "description": "Device OS.",\n            "example": "linux",\n        },\n        "type": {\n            "type": "string",\n            "nullable": True,\n            "description": "Device type.",\n            "example": "network",\n        },\n        "hardware": {\n            "type": "string",\n            "nullable": True,\n            "description": "Device hardware.",\n            "example": "Cisco IOS",\n        },\n        "version": {\n            "type": "string",\n            "nullable": True,\n            "description": "Device OS or hardware version.",\n            "example": "17.9",\n        },\n        "location": {\n            "type": "string",\n            "nullable": True,\n            "description": "Device location.",\n            "example": "DC1",\n        },\n        "timestamp": {\n            "type": "string",\n            "nullable": True,\n            "description": "LibreNMS alert timestamp.",\n            "example": "2026-06-17 10:00:00",\n        },\n        "elapsed": {\n            "type": "string",\n            "nullable": True,\n            "description": "Alert elapsed time.",\n            "example": "5m",\n        },\n        "proc": {\n            "type": "string",\n            "nullable": True,\n            "description": "LibreNMS procedure or source link when available.",\n        },\n        "team": {\n            "type": "string",\n            "nullable": True,\n            "description": "Optional IncidentRelay team slug override.",\n            "example": "sre",\n        },\n        "fingerprint": {\n            "type": "string",\n            "nullable": True,\n            "description": "Optional explicit IncidentRelay dedup key.",\n            "example": "librenms-router1-device-down",\n        },\n        "event_link": {\n            "type": "string",\n            "nullable": True,\n            "description": "External alert URL.",\n            "example": "https://librenms.example.com/alerts",\n        },\n        "event_url": {\n            "type": "string",\n            "nullable": True,\n            "description": "Alternative external alert URL.",\n        },\n        "alert_url": {\n            "type": "string",\n            "nullable": True,\n            "description": "Alternative external alert URL.",\n        },\n        "source_url": {\n            "type": "string",\n            "nullable": True,\n            "description": "Alternative external source URL.",\n        },\n        "device_url": {\n            "type": "string",\n            "nullable": True,\n            "description": "LibreNMS device URL.",\n        },\n        "librenms_url": {\n            "type": "string",\n            "nullable": True,\n            "description": (\n                "LibreNMS base URL. IncidentRelay can build a device link "\n                "from this plus hostname/device_id."\n            ),\n            "example": "https://librenms.example.com",\n        },\n        "labels": {\n            "type": "object",\n            "additionalProperties": True,\n            "description": "Additional labels copied to the normalized alert.",\n            "example": {\n                "environment": "prod",\n                "service": "network",\n            },\n        },\n        "faults": {\n            "description": "LibreNMS faults payload, if included by the transport.",\n            "nullable": True,\n        },\n        "contacts": {\n            "description": "LibreNMS contacts payload, if included by the transport.",\n            "nullable": True,\n        },\n        "applications": {\n            "description": "LibreNMS applications payload, if included by the transport.",\n            "nullable": True,\n        },\n    },\n    "example": {\n        "id": "12345",\n        "uid": "lnms-alert-12345",\n        "state": "1",\n        "severity": "critical",\n        "title": "Device down",\n        "message": "Device router1 is unreachable",\n        "hostname": "router1",\n        "device_id": "77",\n        "ip": "10.0.0.1",\n        "rule": "Device down",\n        "timestamp": "2026-06-17 10:00:00",\n        "team": "sre",\n        "librenms_url": "https://librenms.example.com",\n    },\n}\n\n\ndef tags():\n    """Return OpenAPI tags."""\n    return [\n        {\n            "name": "integrations",\n            "description": (\n                "Incoming alert endpoints for Alertmanager, Grafana, RMON, "\n                "Amazon SNS and CloudWatch, Zabbix, LibreNMS, generic webhooks "\n                "and Sentry. Alertmanager, Grafana, RMON, Zabbix, LibreNMS "\n                "and generic webhooks use route intake tokens. Sentry uses a "\n                "route-specific webhook signature. Amazon SNS requests are "\n                "verified using the SNS signature and the exact Topic ARN "\n                "configured for the route."\n            ),\n        },\n    ]\n\n\ndef paths():\n    """\n    Return OpenAPI paths for integration endpoints.\n    """\n\n    alertmanager_alert_schema = {\n        "type": "object",\n        "required": ["labels"],\n        "additionalProperties": True,\n        "properties": {\n            "status": {\n                "type": "string",\n                "enum": ["firing", "resolved"],\n                "default": "firing",\n                "description": "Alert status from Alertmanager.",\n            },\n            "labels": {\n                "type": "object",\n                "additionalProperties": {"type": "string"},\n                "description": "Alert labels.",\n                "example": {\n                    "alertname": "TargetMissingOrDown",\n                    "instance": "10.101.164.165:9290",\n                    "severity": "critical",\n                    "team": "infra",\n                },\n            },\n            "annotations": {\n                "type": "object",\n                "additionalProperties": {"type": "string"},\n                "description": "Alert annotations.",\n                "example": {\n                    "summary": "Target missing or down",\n                    "description": "Target 10.101.164.165:9290 is not available",\n                },\n            },\n            "startsAt": {\n                "type": "string",\n                "format": "date-time",\n                "nullable": True,\n                "description": "Alert start time.",\n            },\n            "endsAt": {\n                "type": "string",\n                "format": "date-time",\n                "nullable": True,\n                "description": "Alert end time.",\n            },\n            "generatorURL": {\n                "type": "string",\n                "nullable": True,\n                "description": "URL to the source that generated the alert.",\n            },\n            "fingerprint": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alertmanager alert fingerprint. Used for deduplication when available.",\n                "example": "target-missing-10.101.164.165",\n            },\n            "silenceURL": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alertmanager silence URL.",\n                "example": "https://alertmanager.example.com/#/silences/new",\n            },\n            "dashboardURL": {\n                "type": "string",\n                "nullable": True,\n                "description": "Dashboard URL related to this alert.",\n                "example": "https://grafana.example.com/d/node",\n            },\n            "panelURL": {\n                "type": "string",\n                "nullable": True,\n                "description": "Panel URL related to this alert.",\n                "example": "https://grafana.example.com/d/node?viewPanel=12",\n            },\n        },\n    }\n\n    alertmanager_body = {\n        "type": "object",\n        "required": ["alerts"],\n        "additionalProperties": True,\n        "properties": {\n            "receiver": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alertmanager receiver name.",\n                "example": "incidentrelay-infra",\n            },\n            "status": {\n                "type": "string",\n                "enum": ["firing", "resolved"],\n                "default": "firing",\n                "description": "Overall payload status.",\n            },\n            "alerts": {\n                "type": "array",\n                "description": "Alerts included in the Alertmanager notification.",\n                "items": alertmanager_alert_schema,\n            },\n            "groupLabels": {\n                "type": "object",\n                "additionalProperties": {"type": "string"},\n                "description": "Alertmanager groupLabels field.",\n                "example": {\n                    "alertname": "TargetMissingOrDown",\n                    "instance": "10.101.164.165:9290",\n                },\n            },\n            "commonLabels": {\n                "type": "object",\n                "additionalProperties": {"type": "string"},\n                "description": "Alertmanager commonLabels field.",\n                "example": {\n                    "alertname": "TargetMissingOrDown",\n                    "severity": "critical",\n                    "team": "infra",\n                },\n            },\n            "commonAnnotations": {\n                "type": "object",\n                "additionalProperties": {"type": "string"},\n                "description": "Alertmanager commonAnnotations field.",\n                "example": {\n                    "summary": "Target missing or down",\n                    "event_link": "https://grafana.example.com/d/node?viewPanel=12",\n                    "runbook_url": "https://wiki.example.com/runbooks/disk-full",\n                },\n            },\n            "externalURL": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alertmanager external URL.",\n                "example": "https://alertmanager.example.com",\n            },\n            "version": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alertmanager webhook payload version.",\n                "example": "4",\n            },\n            "groupKey": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alertmanager group key.",\n                "example": \'{}/{severity="critical"}:{alertname="TargetMissingOrDown"}\',\n            },\n            "truncatedAlerts": {\n                "type": "integer",\n                "nullable": True,\n                "description": "Number of truncated alerts.",\n                "example": 0,\n            },\n        },\n        "example": {\n            "receiver": "incidentrelay-infra",\n            "status": "firing",\n            "alerts": [\n                {\n                    "status": "firing",\n                    "labels": {\n                        "alertname": "TargetMissingOrDown",\n                        "instance": "10.101.164.165:9290",\n                        "severity": "critical",\n                        "team": "infra",\n                    },\n                    "annotations": {\n                        "summary": "Target missing or down",\n                        "description": "Target 10.101.164.165:9290 is not available",\n                    },\n                    "startsAt": "2026-04-28T15:35:00Z",\n                    "endsAt": "0001-01-01T00:00:00Z",\n                    "generatorURL": "https://prometheus.example.com/graph?g0.expr=up",\n                    "fingerprint": "target-missing-10.101.164.165",\n                }\n            ],\n            "groupLabels": {\n                "alertname": "TargetMissingOrDown",\n                "instance": "10.101.164.165:9290",\n            },\n            "commonLabels": {\n                "severity": "critical",\n                "team": "infra",\n            },\n            "commonAnnotations": {\n                "summary": "Target missing or down",\n            },\n            "externalURL": "https://alertmanager.example.com",\n            "version": "4",\n            "groupKey": \'{}/{severity="critical"}:{alertname="TargetMissingOrDown"}\',\n            "truncatedAlerts": 0,\n        },\n    }\n\n    grafana_alert_schema = {\n        "type": "object",\n        "additionalProperties": True,\n        "properties": {\n            "status": {\n                "type": "string",\n                "description": (\n                    "Grafana alert instance status. Resolved-like values are "\n                    "normalized to resolved; other values are treated as firing."\n                ),\n                "example": "firing",\n            },\n            "labels": {\n                "type": "object",\n                "additionalProperties": True,\n                "description": "Labels attached to this Grafana alert instance.",\n                "example": {\n                    "alertname": "DiskFull",\n                    "severity": "critical",\n                    "instance": "host1",\n                    "grafana_folder": "Infrastructure",\n                    "__alert_rule_uid__": "disk-full-rule",\n                },\n            },\n            "annotations": {\n                "type": "object",\n                "additionalProperties": True,\n                "description": (\n                    "Annotations attached to this Grafana alert instance."\n                ),\n                "example": {\n                    "summary": "Disk is full",\n                    "description": "/var is 95% full",\n                },\n            },\n            "startsAt": {\n                "type": "string",\n                "format": "date-time",\n                "nullable": True,\n                "description": "Time when the alert instance started firing.",\n            },\n            "endsAt": {\n                "type": "string",\n                "format": "date-time",\n                "nullable": True,\n                "description": "Time when the alert instance was resolved.",\n            },\n            "generatorURL": {\n                "type": "string",\n                "nullable": True,\n                "description": "URL of the Grafana alert rule.",\n            },\n            "fingerprint": {\n                "type": "string",\n                "nullable": True,\n                "description": (\n                    "Stable Grafana alert instance fingerprint used for "\n                    "deduplication. When omitted, IncidentRelay generates a "\n                    "stable deduplication key."\n                ),\n                "example": "grafana-disk-full-host1",\n            },\n            "silenceURL": {\n                "type": "string",\n                "nullable": True,\n                "description": "Grafana URL for creating a matching silence.",\n            },\n            "dashboardURL": {\n                "type": "string",\n                "nullable": True,\n                "description": "Related Grafana dashboard URL.",\n                "example": (\n                    "https://grafana.example.com/d/system-overview"\n                ),\n            },\n            "panelURL": {\n                "type": "string",\n                "nullable": True,\n                "description": "Related Grafana panel URL.",\n                "example": (\n                    "https://grafana.example.com/d/system-overview"\n                    "?viewPanel=12"\n                ),\n            },\n            "values": {\n                "type": "object",\n                "additionalProperties": True,\n                "description": "Evaluated Grafana expression values.",\n                "example": {\n                    "A": 95,\n                },\n            },\n            "valueString": {\n                "type": "string",\n                "nullable": True,\n                "description": (\n                    "Human-readable Grafana expression values."\n                ),\n                "example": "[ var=\'A\' value=95 ]",\n            },\n        },\n    }\n\n    grafana_body = {\n        "type": "object",\n        "required": ["alerts"],\n        "additionalProperties": True,\n        "properties": {\n            "receiver": {\n                "type": "string",\n                "nullable": True,\n                "description": "Grafana contact point receiver name.",\n                "example": "incidentrelay",\n            },\n            "status": {\n                "type": "string",\n                "nullable": True,\n                "description": (\n                    "Overall Grafana notification status. The status of each "\n                    "individual alert instance takes precedence."\n                ),\n                "example": "firing",\n            },\n            "orgId": {\n                "oneOf": [\n                    {"type": "integer"},\n                    {"type": "string"},\n                ],\n                "nullable": True,\n                "description": "Grafana organization id.",\n                "example": 1,\n            },\n            "alerts": {\n                "type": "array",\n                "minItems": 1,\n                "description": (\n                    "Grafana alert instances included in the notification."\n                ),\n                "items": grafana_alert_schema,\n            },\n            "groupLabels": {\n                "type": "object",\n                "additionalProperties": True,\n                "description": "Labels used by Grafana to group the notification.",\n                "example": {\n                    "alertname": "DiskFull",\n                },\n            },\n            "commonLabels": {\n                "type": "object",\n                "additionalProperties": True,\n                "description": "Labels common to all alert instances.",\n                "example": {\n                    "team": "sre",\n                    "environment": "production",\n                },\n            },\n            "commonAnnotations": {\n                "type": "object",\n                "additionalProperties": True,\n                "description": "Annotations common to all alert instances.",\n                "example": {\n                    "runbook_url": (\n                        "https://example.com/runbooks/disk"\n                    ),\n                },\n            },\n            "externalURL": {\n                "type": "string",\n                "nullable": True,\n                "description": "Base URL of the Grafana instance.",\n                "example": "https://grafana.example.com/",\n            },\n            "version": {\n                "type": "string",\n                "nullable": True,\n                "description": "Grafana webhook payload version.",\n            },\n            "groupKey": {\n                "type": "string",\n                "nullable": True,\n                "description": "Grafana notification group key.",\n                "example": \'{}:{alertname="DiskFull"}\',\n            },\n            "truncatedAlerts": {\n                "type": "integer",\n                "minimum": 0,\n                "nullable": True,\n                "description": (\n                    "Number of alert instances omitted from the payload."\n                ),\n                "example": 0,\n            },\n            "title": {\n                "type": "string",\n                "nullable": True,\n                "description": "Rendered Grafana notification title.",\n                "example": "[FIRING:1] DiskFull",\n            },\n            "state": {\n                "type": "string",\n                "nullable": True,\n                "description": "Rendered Grafana notification state.",\n                "example": "alerting",\n            },\n            "message": {\n                "type": "string",\n                "nullable": True,\n                "description": "Rendered Grafana notification message.",\n                "example": "Grafana notification",\n            },\n        },\n        "example": {\n            "receiver": "incidentrelay",\n            "status": "firing",\n            "orgId": 1,\n            "groupKey": \'{}:{alertname="DiskFull"}\',\n            "groupLabels": {\n                "alertname": "DiskFull",\n            },\n            "commonLabels": {\n                "team": "sre",\n                "environment": "production",\n            },\n            "commonAnnotations": {\n                "runbook_url": (\n                    "https://example.com/runbooks/disk"\n                ),\n            },\n            "externalURL": "https://grafana.example.com/",\n            "title": "[FIRING:1] DiskFull",\n            "state": "alerting",\n            "message": "Grafana notification",\n            "alerts": [\n                {\n                    "status": "firing",\n                    "labels": {\n                        "alertname": "DiskFull",\n                        "severity": "critical",\n                        "instance": "host1",\n                        "grafana_folder": "Infrastructure",\n                        "__alert_rule_uid__": "disk-full-rule",\n                    },\n                    "annotations": {\n                        "summary": "Disk is full",\n                        "description": "/var is 95% full",\n                    },\n                    "startsAt": "2026-06-21T10:00:00Z",\n                    "endsAt": "0001-01-01T00:00:00Z",\n                    "generatorURL": (\n                        "https://grafana.example.com/alerting/"\n                        "grafana/disk-full-rule/view"\n                    ),\n                    "fingerprint": "grafana-disk-full-host1",\n                    "silenceURL": (\n                        "https://grafana.example.com/alerting/silence/new"\n                    ),\n                    "dashboardURL": (\n                        "https://grafana.example.com/d/system-overview"\n                    ),\n                    "panelURL": (\n                        "https://grafana.example.com/d/system-overview"\n                        "?viewPanel=12"\n                    ),\n                    "values": {\n                        "A": 95,\n                    },\n                    "valueString": "[ var=\'A\' value=95 ]",\n                },\n            ],\n        },\n    }\n\n    rmon_body = {\n        "type": "object",\n        "required": ["title"],\n        "additionalProperties": True,\n        "properties": {\n            "title": {\n                "type": "string",\n                "minLength": 1,\n                "maxLength": 255,\n                "description": "RMON alert title.",\n                "example": "[rmon-production] critical: HTTP check failed",\n            },\n            "message": {\n                "type": "string",\n                "nullable": True,\n                "description": "Detailed alert message.",\n                "example": "critical: HTTP check failed",\n            },\n            "severity": {\n                "type": "string",\n                "nullable": True,\n                "description": "RMON alert severity.",\n                "example": "critical",\n            },\n            "status": {\n                "type": "string",\n                "nullable": True,\n                "description": (\n                    "RMON alert status. Resolved-like values such as resolved, "\n                    "recovered, ok and up are normalized to resolved. Other "\n                    "values are treated as firing."\n                ),\n                "example": "firing",\n            },\n            "fingerprint": {\n                "type": "string",\n                "nullable": True,\n                "description": (\n                    "Stable deduplication key. It must remain unchanged "\n                    "between firing and resolved notifications."\n                ),\n                "example": "42 7",\n            },\n            "external_id": {\n                "oneOf": [\n                    {"type": "string"},\n                    {"type": "integer"},\n                ],\n                "nullable": True,\n                "description": "External RMON check or event identifier.",\n                "example": 42,\n            },\n            "team": {\n                "type": "string",\n                "nullable": True,\n                "description": "Optional IncidentRelay team slug fallback.",\n                "example": "sre",\n            },\n            "labels": {\n                "type": "object",\n                "additionalProperties": True,\n                "description": (\n                    "Additional labels used for routing and grouping."\n                ),\n                "example": {\n                    "team": "sre",\n                    "environment": "production",\n                },\n            },\n            "runbook": {\n                "type": "string",\n                "nullable": True,\n                "description": (\n                    "Runbook text or URL configured for the RMON check."\n                ),\n            },\n            "runbook_url": {\n                "type": "string",\n                "nullable": True,\n                "description": "Runbook URL associated with the check.",\n                "example": (\n                    "https://example.com/runbooks/public-api"\n                ),\n            },\n            "event_link": {\n                "type": "string",\n                "nullable": True,\n                "description": "Direct URL to the source event or check.",\n            },\n            "rmon_name": {\n                "type": "string",\n                "nullable": True,\n                "description": "Name of the originating RMON instance.",\n                "example": "rmon-production",\n            },\n            "check_id": {\n                "oneOf": [\n                    {"type": "string"},\n                    {"type": "integer"},\n                ],\n                "nullable": True,\n                "description": "RMON check identifier.",\n                "example": 42,\n            },\n            "multi_check_id": {\n                "oneOf": [\n                    {"type": "string"},\n                    {"type": "integer"},\n                ],\n                "nullable": True,\n                "description": "RMON multi-check identifier.",\n                "example": 42,\n            },\n            "state_id": {\n                "oneOf": [\n                    {"type": "string"},\n                    {"type": "integer"},\n                ],\n                "nullable": True,\n                "description": "RMON check-state identifier.",\n                "example": 7,\n            },\n            "check_name": {\n                "type": "string",\n                "nullable": True,\n                "description": "Human-readable RMON check name.",\n                "example": "Public API",\n            },\n            "check_type": {\n                "type": "string",\n                "nullable": True,\n                "description": "RMON check type.",\n                "example": "http",\n            },\n            "target": {\n                "type": "string",\n                "nullable": True,\n                "description": "Host, URL or other checked target.",\n                "example": "https://api.example.com/health",\n            },\n            "agent": {\n                "type": "string",\n                "nullable": True,\n                "description": "RMON agent that executed the check.",\n                "example": "eu-west-agent",\n            },\n            "region": {\n                "type": "string",\n                "nullable": True,\n                "description": "Region associated with the check.",\n                "example": "eu-west",\n            },\n            "country": {\n                "type": "string",\n                "nullable": True,\n                "description": "Country associated with the check.",\n                "example": "DE",\n            },\n            "description": {\n                "type": "string",\n                "nullable": True,\n                "description": "Description configured for the RMON check.",\n            },\n        },\n        "example": {\n            "title": "[rmon-production] critical: HTTP check failed",\n            "message": "critical: HTTP check failed",\n            "severity": "critical",\n            "status": "firing",\n            "fingerprint": "42 7",\n            "external_id": 42,\n            "rmon_name": "rmon-production",\n            "multi_check_id": 42,\n            "state_id": 7,\n            "check_name": "Public API",\n            "check_type": "http",\n            "target": "https://api.example.com/health",\n            "agent": "eu-west-agent",\n            "region": "eu-west",\n            "country": "DE",\n            "runbook_url": (\n                "https://example.com/runbooks/public-api"\n            ),\n            "labels": {\n                "team": "sre",\n                "environment": "production",\n            },\n        },\n    }\n\n    aws_sns_message_attribute = {\n        "type": "object",\n        "additionalProperties": True,\n        "properties": {\n            "Type": {\n                "type": "string",\n                "example": "String",\n            },\n            "Value": {\n                "type": "string",\n                "example": "production",\n            },\n            "StringValue": {\n                "type": "string",\n                "nullable": True,\n            },\n        },\n    }\n\n    aws_sns_body = {\n        "type": "object",\n        "required": [\n            "Type",\n            "MessageId",\n            "TopicArn",\n            "Message",\n            "Timestamp",\n            "SignatureVersion",\n            "Signature",\n            "SigningCertURL",\n        ],\n        "additionalProperties": True,\n        "properties": {\n            "Type": {\n                "type": "string",\n                "enum": [\n                    "Notification",\n                    "SubscriptionConfirmation",\n                    "UnsubscribeConfirmation",\n                ],\n                "description": "Amazon SNS envelope type.",\n                "example": "Notification",\n            },\n            "MessageId": {\n                "type": "string",\n                "minLength": 1,\n                "description": "Unique Amazon SNS message identifier.",\n                "example": "sns-message-1",\n            },\n            "TopicArn": {\n                "type": "string",\n                "minLength": 1,\n                "description": (\n                    "Amazon SNS Topic ARN. It must exactly match the Topic ARN "\n                    "configured for the IncidentRelay route."\n                ),\n                "example": (\n                    "arn:aws:sns:eu-west-1:"\n                    "123456789012:incidentrelay-alerts"\n                ),\n            },\n            "Subject": {\n                "type": "string",\n                "nullable": True,\n                "description": "Optional Amazon SNS message subject.",\n                "example": "ALARM: HighCPU",\n            },\n            "Message": {\n                "type": "string",\n                "description": (\n                    "SNS message body. CloudWatch alarm notifications contain "\n                    "a JSON-encoded CloudWatch alarm object."\n                ),\n            },\n            "Timestamp": {\n                "type": "string",\n                "format": "date-time",\n                "description": "Time when Amazon SNS created the message.",\n                "example": "2026-06-21T10:00:01.000Z",\n            },\n            "SignatureVersion": {\n                "type": "string",\n                "enum": ["1", "2"],\n                "description": (\n                    "Amazon SNS signature version. Version 1 uses SHA-1 and "\n                    "version 2 uses SHA-256."\n                ),\n                "example": "2",\n            },\n            "Signature": {\n                "type": "string",\n                "minLength": 1,\n                "description": "Base64-encoded Amazon SNS RSA signature.",\n            },\n            "SigningCertURL": {\n                "type": "string",\n                "format": "uri",\n                "description": (\n                    "HTTPS URL of the Amazon SNS signing certificate. "\n                    "IncidentRelay only accepts approved Amazon SNS hosts "\n                    "and certificate paths."\n                ),\n                "example": (\n                    "https://sns.eu-west-1.amazonaws.com/"\n                    "SimpleNotificationService-example.pem"\n                ),\n            },\n            "Token": {\n                "type": "string",\n                "nullable": True,\n                "description": (\n                    "Subscription confirmation token. Required for "\n                    "confirmation messages."\n                ),\n            },\n            "SubscribeURL": {\n                "type": "string",\n                "format": "uri",\n                "nullable": True,\n                "description": (\n                    "Signed Amazon SNS subscription confirmation URL."\n                ),\n            },\n            "UnsubscribeURL": {\n                "type": "string",\n                "format": "uri",\n                "nullable": True,\n            },\n            "MessageAttributes": {\n                "type": "object",\n                "description": (\n                    "Optional SNS message attributes. String attributes are "\n                    "converted into IncidentRelay labels."\n                ),\n                "additionalProperties": aws_sns_message_attribute,\n                "example": {\n                    "team": {\n                        "Type": "String",\n                        "Value": "sre",\n                    },\n                    "environment": {\n                        "Type": "String",\n                        "Value": "production",\n                    },\n                },\n            },\n        },\n        "example": {\n            "Type": "Notification",\n            "MessageId": "sns-message-1",\n            "TopicArn": (\n                "arn:aws:sns:eu-west-1:"\n                "123456789012:incidentrelay-alerts"\n            ),\n            "Subject": "ALARM: HighCPU",\n            "Message": (\n                \'{"AlarmName":"HighCPU",\'\n                \'"AlarmDescription":"CPU usage is too high",\'\n                \'"AWSAccountId":"123456789012",\'\n                \'"NewStateValue":"ALARM",\'\n                \'"NewStateReason":"Threshold crossed",\'\n                \'"Region":"EU (Ireland)",\'\n                \'"AlarmArn":"arn:aws:cloudwatch:eu-west-1:\'\n                \'123456789012:alarm:HighCPU",\'\n                \'"OldStateValue":"OK"}\'\n            ),\n            "Timestamp": "2026-06-21T10:00:01.000Z",\n            "SignatureVersion": "2",\n            "Signature": "base64-signature",\n            "SigningCertURL": (\n                "https://sns.eu-west-1.amazonaws.com/"\n                "SimpleNotificationService-example.pem"\n            ),\n            "MessageAttributes": {\n                "team": {\n                    "Type": "String",\n                    "Value": "sre",\n                },\n                "environment": {\n                    "Type": "String",\n                    "Value": "production",\n                },\n            },\n        },\n    }\n\n    aws_sns_confirmation_response = {\n        "type": "object",\n        "required": [\n            "status",\n            "message_id",\n            "topic_arn",\n        ],\n        "properties": {\n            "status": {\n                "type": "string",\n                "enum": [\n                    "confirmed",\n                    "unsubscribed",\n                ],\n                "example": "confirmed",\n            },\n            "message_id": {\n                "type": "string",\n                "example": "sns-confirmation-1",\n            },\n            "topic_arn": {\n                "type": "string",\n                "example": (\n                    "arn:aws:sns:eu-west-1:"\n                    "123456789012:incidentrelay-alerts"\n                ),\n            },\n        },\n        "additionalProperties": False,\n    }\n\n    aws_sns_responses = incoming_alert_responses(\n        "Amazon SNS notification accepted."\n    )\n\n    aws_sns_ingest_response = aws_sns_responses[\n        "200"\n    ]["content"]["application/json"]["schema"]\n\n    aws_sns_responses["200"] = {\n        "description": (\n            "Notification processed or subscription state confirmed."\n        ),\n        "content": {\n            "application/json": {\n                "schema": {\n                    "oneOf": [\n                        aws_sns_ingest_response,\n                        aws_sns_confirmation_response,\n                    ],\n                },\n            },\n        },\n    }\n\n    # AWS SNS authenticates requests using the SNS signature and configured\n    # Topic ARN, not a route intake bearer token.\n    aws_sns_responses.pop("401", None)\n\n    aws_sns_responses["403"] = {\n        "description": (\n            "Signature validation failed, Topic ARN did not match, "\n            "or the route is not available."\n        ),\n    }\n\n    aws_sns_responses["404"] = {\n        "description": "AWS SNS route was not found.",\n    }\n\n    aws_sns_responses["502"] = {\n        "description": (\n            "The signing certificate could not be loaded or subscription "\n            "confirmation failed."\n        ),\n    }\n\n    zabbix_body = {\n        "type": "object",\n        "additionalProperties": True,\n        "properties": {\n            "event_id": {\n                "type": "string",\n                "description": "Zabbix event id. Use the same event_id for firing and resolved events.",\n                "example": "100500",\n            },\n            "trigger_id": {\n                "type": "string",\n                "nullable": True,\n                "description": "Zabbix trigger id.",\n                "example": "200600",\n            },\n            "title": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alert title.",\n                "example": "Disk space is low",\n            },\n            "host": {\n                "type": "string",\n                "nullable": True,\n                "description": "Zabbix host name.",\n                "example": "host1",\n            },\n            "trigger": {\n                "type": "string",\n                "nullable": True,\n                "description": "Zabbix trigger name.",\n                "example": "DiskSpaceLow",\n            },\n            "message": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alert message.",\n                "example": "/var is 95% full",\n            },\n            "severity": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alert severity.",\n                "example": "high",\n            },\n            "status": {\n                "type": "string",\n                "enum": ["firing", "resolved"],\n                "default": "firing",\n                "description": "Alert status.",\n            },\n            "team": {\n                "type": "string",\n                "nullable": True,\n                "description": "Optional team slug fallback.",\n                "example": "infra",\n            },\n            "labels": {\n                "type": "object",\n                "additionalProperties": {"type": "string"},\n                "description": "Additional labels.",\n                "example": {\n                    "team": "infra",\n                    "host": "host1",\n                    "trigger": "DiskSpaceLow",\n                },\n            },\n            "fingerprint": {\n                "type": "string",\n                "nullable": True,\n                "description": "Optional stable deduplication key.",\n                "example": "zabbix-100500",\n            },\n            "event_name": {\n                "type": "string",\n                "nullable": True,\n                "description": "Zabbix event name, usually {EVENT.NAME}.",\n                "example": "Disk space is low on host1",\n            },\n            "problem_name": {\n                "type": "string",\n                "nullable": True,\n                "description": "Problem name fallback.",\n                "example": "Disk space is low on host1",\n            },\n            "trigger_name": {\n                "type": "string",\n                "nullable": True,\n                "description": "Zabbix trigger name.",\n                "example": "Free disk space is less than 10%",\n            },\n            "event_tag": {\n                "nullable": True,\n                "description": "Zabbix event tags, for example {EVENT.TAGS} or {EVENT.TAGSJSON}.",\n                "example": "team: infra, service: filesystem",\n            },\n            "tags": {\n                "nullable": True,\n                "description": "Zabbix tags. Can be object, array, JSON string or EVENT.TAGSJSON.",\n                "example": [\n                    {"tag": "team", "value": "infra"},\n                    {"tag": "service", "value": "filesystem"}\n                ],\n            },\n            "event_link": {\n                "type": "string",\n                "nullable": True,\n                "description": "Direct URL to the Zabbix event/problem.",\n                "example": "https://zabbix.example.com/tr_events.php?triggerid=98765&eventid=123456",\n            },\n            "event_url": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alias for event_link.",\n            },\n            "problem_url": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alias for event_link.",\n            },\n            "trigger_url": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alias for event_link.",\n            },\n            "zabbix_url": {\n                "type": "string",\n                "nullable": True,\n                "description": "Base Zabbix frontend URL. IncidentRelay can build event_link from it when event_id is present.",\n                "example": "https://zabbix.example.com",\n            },\n        },\n        "example": {\n            "status": "firing",\n            "event_id": "123456",\n            "trigger_id": "98765",\n            "event_name": "High CPU load on host1",\n            "host": "host1",\n            "event_severity": "High",\n            "event_status": "PROBLEM",\n            "opdata": "CPU load is above 90%",\n            "event_tag": "team: infra, service: cpu",\n            "event_link": "https://zabbix.example.com/tr_events.php?triggerid=98765&eventid=123456",\n            "team": "infra",\n            "labels": {\n                "host": "host1",\n                "service": "cpu",\n                "environment": "prod"\n            }\n        }\n    }\n\n    generic_webhook_body = {\n        "type": "object",\n        "required": ["title"],\n        "additionalProperties": True,\n        "properties": {\n            "title": {\n                "type": "string",\n                "description": "Alert title.",\n                "example": "Disk is full",\n            },\n            "message": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alert message.",\n                "example": "/var is 95% full",\n            },\n            "severity": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alert severity.",\n                "example": "critical",\n            },\n            "status": {\n                "type": "string",\n                "enum": ["firing", "resolved"],\n                "default": "firing",\n                "description": "Alert status.",\n            },\n            "team": {\n                "type": "string",\n                "nullable": True,\n                "description": "Optional team slug fallback.",\n                "example": "infra",\n            },\n            "labels": {\n                "type": "object",\n                "additionalProperties": True,\n                "description": "Alert labels.",\n                "example": {\n                    "team": "infra",\n                    "instance": "host1",\n                    "alertname": "DiskFull",\n                },\n            },\n            "fingerprint": {\n                "type": "string",\n                "nullable": True,\n                "description": "Stable deduplication key. Use the same value for firing and resolved.",\n                "example": "disk-full-host1-var",\n            },\n            "external_id": {\n                "type": "string",\n                "nullable": True,\n                "description": "External system event id.",\n                "example": "event-123",\n            },\n            "event_link": {\n                "type": "string",\n                "nullable": True,\n                "description": "Direct URL to the source event.",\n                "example": "https://monitoring.example.com/events/deploy-incident-42",\n            },\n            "event_url": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alias for event_link.",\n            },\n            "alert_url": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alias for event_link.",\n            },\n            "source_url": {\n                "type": "string",\n                "nullable": True,\n                "description": "Alias for event_link.",\n            },\n            "dashboard_url": {\n                "type": "string",\n                "nullable": True,\n                "description": "Dashboard URL related to the alert.",\n            },\n            "runbook_url": {\n                "type": "string",\n                "nullable": True,\n                "description": "Runbook URL related to the alert.",\n            },\n        },\n        "example": {\n            "title": "Disk is full",\n            "message": "/var is 95% full",\n            "severity": "critical",\n            "status": "firing",\n            "fingerprint": "disk-full-host1-var",\n            "labels": {\n                "team": "infra",\n                "instance": "host1",\n                "alertname": "DiskFull",\n            },\n            "event_link": "https://monitoring.example.com/events/deploy-incident-42",\n        },\n    }\n\n    pagerduty_events_v2_body = {\n        "type": "object",\n        "required": ["routing_key", "event_action"],\n        "additionalProperties": True,\n        "properties": {\n            "routing_key": {\n                "type": "string",\n                "description": "IncidentRelay webhook route intake token.",\n            },\n            "event_action": {\n                "type": "string",\n                "enum": ["trigger", "acknowledge", "resolve"],\n            },\n            "dedup_key": {\n                "type": "string",\n                "nullable": True,\n                "description": "Stable incident key. Required for acknowledge and resolve.",\n                "example": "database-prod-01",\n            },\n            "payload": {\n                "type": "object",\n                "nullable": True,\n                "description": "PagerDuty Common Event Format payload. Required for trigger.",\n                "properties": {\n                    "summary": {"type": "string"},\n                    "source": {"type": "string"},\n                    "severity": {\n                        "type": "string",\n                        "enum": ["critical", "error", "warning", "info"],\n                    },\n                    "timestamp": {"type": "string", "nullable": True},\n                    "component": {"type": "string", "nullable": True},\n                    "group": {"type": "string", "nullable": True},\n                    "class": {"type": "string", "nullable": True},\n                    "custom_details": {\n                        "type": "object",\n                        "additionalProperties": True,\n                    },\n                },\n            },\n            "links": {\n                "type": "array",\n                "items": {\n                    "type": "object",\n                    "properties": {\n                        "href": {"type": "string"},\n                        "text": {"type": "string", "nullable": True},\n                    },\n                },\n            },\n            "images": {\n                "type": "array",\n                "items": {"type": "object", "additionalProperties": True},\n            },\n            "client": {"type": "string", "nullable": True},\n            "client_url": {"type": "string", "nullable": True},\n        },\n        "example": {\n            "routing_key": "WEBHOOK_ROUTE_INTAKE_TOKEN",\n            "event_action": "trigger",\n            "dedup_key": "database-prod-01",\n            "payload": {\n                "summary": "Production database is unavailable",\n                "source": "db-prod-01",\n                "severity": "critical",\n                "component": "postgresql",\n                "group": "production",\n                "class": "database",\n                "custom_details": {\n                    "team": "sre",\n                    "environment": "prod",\n                },\n            },\n            "links": [\n                {\n                    "href": "https://monitoring.example.com/incidents/db-prod-01",\n                    "text": "Open monitoring",\n                }\n            ],\n        },\n    }\n\n    webhook_body = {\n        "oneOf": [\n            generic_webhook_body,\n            pagerduty_events_v2_body,\n        ],\n        "description": (\n            "IncidentRelay generic webhook payload or a PagerDuty Events API "\n            "v2-compatible alert event."\n        ),\n    }\n\n\n    return {\n        "/api/integrations/alertmanager": {\n            "post": {\n                "tags": ["integrations"],\n                "summary": "Receive Alertmanager alerts",\n                "description": (\n                    "Accepts the standard Prometheus Alertmanager webhook payload. "\n                    "The route intake token must belong to a route with source=alertmanager. "\n                    "Top-level Alertmanager fields such as receiver, groupLabels, commonLabels, "\n                    "commonAnnotations, externalURL, version, groupKey and truncatedAlerts are accepted."\n                ),\n                "operationId": "receiveAlertmanagerAlerts",\n                "security": [{"bearerAuth": []}],\n                "requestBody": json_body("Standard Alertmanager webhook payload.", alertmanager_body),\n                "responses": incoming_alert_responses("Alertmanager alerts accepted."),\n            }\n        },\n        "/api/integrations/grafana": {\n            "post": {\n                "tags": ["integrations"],\n                "summary": "Receive Grafana Alerting alerts",\n                "description": (\n                    "Receives a Grafana Alerting webhook contact point payload. "\n                    "The route intake token must belong to an active route with "\n                    "source=grafana. Each object in alerts is normalized and "\n                    "processed independently. The individual alert status takes "\n                    "precedence over the top-level notification status."\n                ),\n                "operationId": "receiveGrafanaAlerts",\n                "security": [{"bearerAuth": []}],\n                "requestBody": json_body(\n                    "Grafana Alerting webhook payload.",\n                    grafana_body,\n                ),\n                "responses": incoming_alert_responses(\n                    "Grafana alerts accepted."\n                ),\n            },\n        },\n        "/api/integrations/rmon": {\n            "post": {\n                "tags": ["integrations"],\n                "summary": "Receive RMON alerts",\n                "description": (\n                    "Receives an alert from RMON. The route intake token "\n                    "must belong to an active route with source=rmon. "\n                    "The payload is normalized and processed through the "\n                    "standard IncidentRelay routing, grouping, suppression, "\n                    "notification and escalation pipeline."\n                ),\n                "operationId": "receiveRmonAlert",\n                "security": [{"bearerAuth": []}],\n                "requestBody": json_body(\n                    "RMON alert payload.",\n                    rmon_body,\n                ),\n                "responses": incoming_alert_responses(\n                    "RMON alert accepted."\n                ),\n            },\n        },\n        "/api/integrations/aws-sns/{route_id}": {\n            "post": {\n                "tags": ["integrations"],\n                "summary": "Receive Amazon SNS and CloudWatch notifications",\n                "description": (\n                    "Receives signed Amazon SNS messages for one IncidentRelay "\n                    "route. The route must use source=aws_sns and have an exact "\n                    "SNS Topic ARN configured. IncidentRelay verifies the SNS "\n                    "signature, signing certificate URL and Topic ARN before "\n                    "processing the message. CloudWatch ALARM and "\n                    "INSUFFICIENT_DATA states are normalized to firing, while "\n                    "OK is normalized to resolved. Subscription confirmation "\n                    "messages are verified and confirmed automatically."\n                ),\n                "operationId": "receiveAwsSnsNotification",\n                "security": [],\n                "parameters": [\n                    {\n                        "name": "route_id",\n                        "in": "path",\n                        "required": True,\n                        "description": (\n                            "IncidentRelay route configured for this SNS topic."\n                        ),\n                        "schema": {\n                            "type": "integer",\n                            "minimum": 1,\n                        },\n                    },\n                ],\n                "requestBody": json_body(\n                    "Signed Amazon SNS message envelope.",\n                    aws_sns_body,\n                ),\n                "responses": aws_sns_responses,\n            },\n        },\n        "/api/integrations/zabbix": {\n            "post": {\n                "tags": ["integrations"],\n                "summary": "Receive Zabbix alerts",\n                "description": (\n                    "Receives a Zabbix webhook payload. The route intake token must belong "\n                    "to a route with source=zabbix. Use the same event_id for firing and resolved events."\n                ),\n                "operationId": "receiveZabbixAlerts",\n                "security": [{"bearerAuth": []}],\n                "requestBody": json_body("Zabbix webhook payload.", zabbix_body),\n                "responses": incoming_alert_responses("Zabbix alert accepted."),\n            }\n        },\n        "/api/integrations/sentry/{route_id}": {\n            "post": {\n                "tags": ["integrations"],\n                "summary": "Receive Sentry webhook",\n                "description": (\n                    "Receives signed webhooks from a Sentry Internal Integration. "\n                    "The route id in the URL selects an IncidentRelay route with source=sentry. "\n                    "The request is authenticated by Sentry-Hook-Signature using the "\n                    "Sentry Client Secret stored in route integration_config. "\n                    "This endpoint does not use IncidentRelay bearer intake tokens."\n                ),\n                "operationId": "receiveSentryWebhook",\n                "parameters": [\n                    path_param("route_id", "Sentry IncidentRelay route id."),\n                    {\n                        "name": "Sentry-Hook-Signature",\n                        "in": "header",\n                        "required": True,\n                        "description": (\n                            "HMAC-SHA256 signature generated by Sentry using the "\n                            "Internal Integration Client Secret."\n                        ),\n                        "schema": {\n                            "type": "string",\n                            "minLength": 1,\n                        },\n                    },\n                    {\n                        "name": "Sentry-Hook-Resource",\n                        "in": "header",\n                        "required": False,\n                        "description": (\n                            "Sentry resource name. IncidentRelay uses this to normalize "\n                            "event_alert, metric_alert and issue events."\n                        ),\n                        "schema": {\n                            "type": "string",\n                            "enum": [\n                                "event_alert",\n                                "metric_alert",\n                                "issue",\n                            ],\n                        },\n                    },\n                ],\n                "requestBody": json_body(\n                    "Sentry Internal Integration webhook payload.",\n                    SENTRY_WEBHOOK_BODY_SCHEMA,\n                ),\n                "responses": {\n                    "200": response(\n                        "Sentry webhook accepted.",\n                        ALERT_PROCESSING_RESULT_LIST_SCHEMA,\n                    ),\n                    "202": response(\n                        "Sentry webhook processed, but no alert group was created.",\n                        ALERT_PROCESSING_RESULT_LIST_SCHEMA,\n                    ),\n                    "207": response(\n                        "Batch contains both accepted and failed alerts.",\n                        ALERT_PROCESSING_RESULT_LIST_SCHEMA,\n                    ),\n                    "400": response("Invalid payload or route source mismatch."),\n                    "403": response(\n                        "Missing or invalid Sentry-Hook-Signature, disabled route, or inactive team/group."\n                    ),\n                    "404": response("Sentry route was not found."),\n                    "409": response("Sentry webhook secret is not configured for this route."),\n                },\n            }\n        },\n        "/api/integrations/librenms": {\n            "post": {\n                "tags": ["integrations"],\n                "summary": "Receive LibreNMS alerts",\n                "description": (\n                    "Receives a LibreNMS API transport payload. The route intake "\n                    "token must belong to a route with source=librenms. Use the "\n                    "same uid/id for firing and recovery events."\n                ),\n                "operationId": "receiveLibreNMSAlerts",\n                "security": [{"bearerAuth": []}],\n                "requestBody": json_body("LibreNMS API transport payload.", librenms_body),\n                "responses": incoming_alert_responses("LibreNMS alert accepted."),\n            }\n        },\n        "/api/integrations/webhook": {\n            "post": {\n                "tags": ["integrations"],\n                "summary": "Receive generic or PagerDuty-compatible webhook alerts",\n                "description": (\n                    "Receives either the IncidentRelay generic payload or a PagerDuty "\n                    "Events API v2-compatible trigger, acknowledge or resolve event. "\n                    "Generic payloads normally use Authorization: Bearer. PagerDuty-compatible "\n                    "payloads use the webhook route intake token as routing_key. Provide a "\n                    "stable fingerprint or dedup_key to avoid duplicate alerts."\n                ),\n                "operationId": "receiveGenericWebhookAlerts",\n                "security": [{"bearerAuth": []}],\n                "requestBody": json_body("Generic or PagerDuty Events API v2-compatible payload.", webhook_body),\n                "responses": incoming_alert_responses("Generic webhook alert accepted."),\n            }\n        },\n        "/api/integrations/mattermost/actions": {\n            "post": {\n                "tags": ["integrations"],\n                "summary": "Handle Mattermost interactive buttons",\n                "description": (\n                    "Receives Mattermost interactive message button callbacks. The endpoint validates "\n                    "the callback secret from the action context, acknowledges or resolves the alert, "\n                    "and updates the original Mattermost post when the channel is configured in Bot API mode."\n                ),\n                "operationId": "handleMattermostAction",\n                "requestBody": json_body("Mattermost interactive action payload.", {\n                    "type": "object",\n                    "properties": {\n                        "context": {\n                            "type": "object",\n                            "properties": {\n                                "alert_id": {"type": "integer"},\n                                "channel_id": {"type": "integer"},\n                                "action": {"type": "string", "enum": ["acknowledge", "resolve"]},\n                                "secret": {"type": "string"},\n                            },\n                        }\n                    },\n                }),\n                "responses": {\n                    "200": response("Action processed."),\n                    "400": response("Invalid action payload."),\n                    "403": response("Invalid callback secret."),\n                },\n            }\n        },\n        "/api/integrations/slack/actions": {\n            "post": {\n                "tags": ["integrations"],\n                "summary": "Handle Slack alert action",\n                "description": (\n                    "Receives Slack Block Kit button interactions for alert "\n                    "acknowledgement and resolution. "\n                    "The request body is application/x-www-form-urlencoded "\n                    "and contains a JSON-encoded payload field. "\n                    "IncidentRelay verifies the raw request body using the "\n                    "Slack signing secret configured on the referenced channel. "\n                    "Requests older than five minutes are rejected. "\n                    "This endpoint does not use IncidentRelay bearer tokens."\n                ),\n                "operationId": "handleSlackAlertAction",\n                "security": [],\n                "parameters": [\n                    header_param(\n                        "X-Slack-Request-Timestamp",\n                        (\n                            "Unix timestamp included in the Slack request. "\n                            "Requests older than five minutes are rejected."\n                        ),\n                        {\n                            "type": "string",\n                            "pattern": "^[0-9]+$",\n                            "example": "1782123456",\n                        },\n                        required=True,\n                    ),\n                    header_param(\n                        "X-Slack-Signature",\n                        (\n                            "Slack v0 HMAC-SHA256 signature calculated from "\n                            "the raw request body, timestamp and signing secret."\n                        ),\n                        {\n                            "type": "string",\n                            "pattern": "^v0=[a-f0-9]{64}$",\n                            "example": (\n                                "v0="\n                                "0123456789abcdef"\n                                "0123456789abcdef"\n                                "0123456789abcdef"\n                                "0123456789abcdef"\n                            ),\n                        },\n                        required=True,\n                    ),\n                ],\n                "requestBody": {\n                    "required": True,\n                    "description": (\n                        "Slack Block Kit interaction encoded as a form field."\n                    ),\n                    "content": {\n                        "application/x-www-form-urlencoded": {\n                            "schema": SLACK_ACTION_FORM_SCHEMA,\n                        },\n                    },\n                },\n                "responses": {\n                    "200": response(\n                        "Slack alert action processed.",\n                        SLACK_ACTION_RESPONSE_SCHEMA,\n                    ),\n                    "400": response(\n                        (\n                            "Malformed form body, unsupported interaction type, "\n                            "invalid action or invalid alert context."\n                        ),\n                        SLACK_ACTION_ERROR_SCHEMA,\n                    ),\n                    "403": response(\n                        (\n                            "Missing or invalid Slack signature, expired request, "\n                            "disabled or mismatched Slack channel, or alert and "\n                            "channel belong to different teams."\n                        ),\n                        SLACK_ACTION_ERROR_SCHEMA,\n                    ),\n                    "404": response(\n                        "Referenced IncidentRelay alert was not found.",\n                        SLACK_ACTION_ERROR_SCHEMA,\n                    ),\n                },\n            }\n        },\n        "/api/integrations/voice/callback/{channel_id}/{secret}": {\n            "post": {\n                "tags": ["integrations"],\n                "summary": "Handle voice provider callback",\n                "description": (\n                    "Receives callbacks from voice call providers. "\n                    "The endpoint validates the channel callback secret, loads the provider "\n                    "configured for the voice_call channel, and passes the raw payload to "\n                    "provider.parse_callback(). "\n                    "Providers may normalize call status changes, DTMF button presses and errors. "\n                    "DTMF digits can be mapped to IncidentRelay actions through channel config, "\n                    "for example 1=acknowledge and 2=resolve."\n                ),\n                "operationId": "handleVoiceProviderCallback",\n                "parameters": [\n                    path_param("channel_id", "Voice call notification channel id."),\n                    {\n                        "name": "secret",\n                        "in": "path",\n                        "required": True,\n                        "description": (\n                            "Voice callback secret. "\n                            "Uses channel config callback_secret or global voice.callback_secret."\n                        ),\n                        "schema": {\n                            "type": "string",\n                            "minLength": 1,\n                        },\n                    },\n                ],\n                "requestBody": json_body(\n                    "Provider-specific callback payload.",\n                    VOICE_CALLBACK_BODY_SCHEMA,\n                ),\n                "responses": {\n                    "200": response(\n                        "Callback processed or ignored.",\n                        VOICE_CALLBACK_RESPONSE_SCHEMA,\n                    ),\n                    "400": response(\n                        "Invalid callback payload, provider parse error or channel is not voice_call."\n                    ),\n                    "403": response("Invalid voice callback secret."),\n                    "404": response("Channel or notification not found."),\n                },\n            }\n        },\n    }\n', 'default')], 'docs/integrations/generic-webhook.md': [('0e01d8f4070543a222bc2279d6d3127f1ca1566c0d95993a801769047f544f37', '197d7cc06e662fd83371053a5efcbff3fb73b3f91c624ba8673a54b1127e731e', '---\ntitle: Generic Webhook and PagerDuty Events API v2\ndescription: Generic incoming webhook route setup, payload format and PagerDuty Events API v2 compatibility.\n---\n\n# Generic webhook integration\n\nThe Webhook route accepts two request formats at the same endpoint:\n\n1. IncidentRelay generic webhook payload.\n2. PagerDuty Events API v2-compatible alert events.\n\nEndpoint:\n\n```text\nPOST /api/integrations/webhook\n```\n\n## Route setup\n\nCreate a route with:\n\n```text\nSource: Webhook / PagerDuty Events API v2\n```\n\nAttach the required notification channels and copy the route intake token.\n\nThe same token is used differently by each format:\n\n| Format | Authentication |\n|---|---|\n| IncidentRelay generic webhook | `Authorization: Bearer ROUTE_TOKEN` |\n| PagerDuty Events API v2-compatible | JSON field `routing_key: ROUTE_TOKEN` |\n\n`routing_key` is treated as a secret. IncidentRelay uses it to select the route and stores `[REDACTED]` instead of the token in the alert payload.\n\n## IncidentRelay generic format\n\n```bash\ncurl -X POST \'https://incidentrelay.example.com/api/integrations/webhook\' \\\n  -H \'Content-Type: application/json\' \\\n  -H \'Authorization: Bearer ROUTE_TOKEN\' \\\n  -d \'{\n    "status": "firing",\n    "external_id": "deploy-incident-42",\n    "fingerprint": "deploy-api-prod-42",\n    "title": "API deploy failed",\n    "message": "Deployment failed on api-prod-1",\n    "severity": "critical",\n    "team": "infra",\n    "event_link": "https://monitoring.example.com/events/deploy-incident-42",\n    "labels": {\n      "service": "api",\n      "environment": "prod",\n      "host": "api-prod-1"\n    },\n    "details": {\n      "deploy_id": "42",\n      "region": "eu-1"\n    }\n  }\'\n```\n\n### Generic normalized fields\n\n| IncidentRelay field | Source |\n|---|---|\n| `source` | `webhook` |\n| `team_slug` | `team`, `labels.team`, or `labels.oncall_team` |\n| `external_id` | `external_id` |\n| `dedup_key` | `fingerprint`, or generated from source, external ID, title and labels |\n| `title` | `title` |\n| `message` | `message` |\n| `severity` | `severity` |\n| `labels` | `labels` plus helper labels such as `event_link` |\n| `payload` | Original payload, with secret `routing_key` redacted when present |\n| `status` | `status`, default `firing` |\n\n## PagerDuty Events API v2-compatible format\n\nSystems that already have a PagerDuty Events API v2 exporter can point it at the IncidentRelay webhook endpoint.\n\nUse the IncidentRelay route intake token as `routing_key`.\n\n### Trigger\n\n```bash\ncurl -X POST \'https://incidentrelay.example.com/api/integrations/webhook\' \\\n  -H \'Content-Type: application/json\' \\\n  -d \'{\n    "routing_key": "ROUTE_TOKEN",\n    "event_action": "trigger",\n    "dedup_key": "database-prod-01",\n    "payload": {\n      "summary": "Production database is unavailable",\n      "source": "db-prod-01",\n      "severity": "critical",\n      "component": "postgresql",\n      "group": "production",\n      "class": "database",\n      "custom_details": {\n        "service": "database",\n        "environment": "prod",\n        "host": "db-prod-01"\n      }\n    },\n    "links": [\n      {\n        "href": "https://monitoring.example.com/incidents/db-prod-01",\n        "text": "Open monitoring"\n      }\n    ]\n  }\'\n```\n\nSuccessful PagerDuty-compatible requests return HTTP `202`:\n\n```json\n{\n  "status": "success",\n  "message": "Event processed",\n  "dedup_key": "database-prod-01"\n}\n```\n\n### Acknowledge\n\n```bash\ncurl -X POST \'https://incidentrelay.example.com/api/integrations/webhook\' \\\n  -H \'Content-Type: application/json\' \\\n  -d \'{\n    "routing_key": "ROUTE_TOKEN",\n    "event_action": "acknowledge",\n    "dedup_key": "database-prod-01"\n  }\'\n```\n\nThe matching IncidentRelay alert group is acknowledged. The lookup is scoped to the webhook route identified by `routing_key`.\n\n### Resolve\n\n```bash\ncurl -X POST \'https://incidentrelay.example.com/api/integrations/webhook\' \\\n  -H \'Content-Type: application/json\' \\\n  -d \'{\n    "routing_key": "ROUTE_TOKEN",\n    "event_action": "resolve",\n    "dedup_key": "database-prod-01"\n  }\'\n```\n\nThe matching IncidentRelay alert group and its child alerts are resolved.\n\nUnknown or already-resolved `dedup_key` values are accepted as successful no-ops. This preserves the asynchronous follow-up-event behavior expected by PagerDuty-compatible senders.\n\n### PagerDuty field mapping\n\n| PagerDuty field | IncidentRelay field |\n|---|---|\n| `routing_key` | Webhook route intake token; not stored in clear text |\n| `event_action=trigger` | `status=firing` |\n| `event_action=acknowledge` | Acknowledge matching alert group |\n| `event_action=resolve` | Resolve matching alert group |\n| `dedup_key` | `dedup_key` and `external_id` |\n| `payload.summary` | `title` |\n| `payload.severity` | Normalized severity |\n| `payload.source` | Label `source` |\n| `payload.component` | Label `component` |\n| `payload.group` | Label `group` |\n| `payload.class` | Label `class` |\n| `payload.custom_details` | Scalar values copied to labels; full object retained in payload |\n| First `links[].href` | `labels.event_link` |\n| `client` | Label `pagerduty_client` |\n\nPagerDuty severity is normalized using IncidentRelay aliases. For example, `error` becomes `critical`.\n\n`payload.summary`, `payload.source`, and `payload.severity` are required for `trigger`. `dedup_key` is required for `acknowledge` and `resolve`.\n\n## Service assignment\n\nAfter the route accepts an event, IncidentRelay can attach it to a service in two ways:\n\n1. Select a default service on the route.\n2. Configure service match rules.\n\nPagerDuty standard fields and scalar `custom_details` become labels, so they can be used by service match rules.\n\nExample:\n\n```json\n{\n  "labels": {\n    "component": "postgresql",\n    "environment": {\n      "op": "regex",\n      "value": "^(prod|production)$"\n    }\n  }\n}\n```\n\n## Event links\n\nThe generic format supports these aliases:\n\n```text\nevent_link\nevent_url\nalert_url\nsource_url\ndashboard_url\nrunbook_url\n```\n\nFor PagerDuty-compatible requests, the first non-empty `links[].href` is stored as `labels.event_link`. `client_url` is used as a fallback.\n', 'default')], 'docs/integrations/index.md': [('f456da32c24a487ffb322a6e68de6e1b23026b2b1d3f38596cf1e50578a7c2af', 'c6993076e292b3aa70f0385336429c27e7c18c31fed8bc5db1415ca66e7e7b05', "---\ntitle: Integrations\ndescription: Incoming alert sources and outgoing notification channels\n---\n\n# Integrations\n\nIncidentRelay has two different integration layers. Keep them separate when configuring or troubleshooting the system.\n\n```text\nMonitoring system -> Incoming integration -> Route -> Notification channels -> User action\n```\n\nProfile-level browser push is separate from notification channels. Users enable browser/PWA push in Profile, and alerts are delivered to active browser push devices of the assigned user. Read more: [Browser Push](../usage/browser-push.md).\n\n## Incoming alert integrations\n\nIncoming integrations create or update alerts in IncidentRelay. They are selected by the route `source` field and require a route intake token.\n\n| Source              | Endpoint                                   | Documentation                                             |\n|---------------------|--------------------------------------------|-----------------------------------------------------------|\n| Alertmanager        | `POST /api/integrations/alertmanager`      | [Alertmanager](alertmanager.md)                           |\n| AWS SNS/Cloud watch | `POST /api/integrations/aws-sns`           | [AWS SNS/Cloud watch](integrations/aws-sns-cloudwatch.md) |\n| Grafana             | `POST /api/integrations/grafana`           | [Grafana](grafana.md)                                     |\n| RMON                | `POST /api/integrations/rmon`              | [Grafana](rmon.md)                                        |\n| Zabbix              | `POST /api/integrations/zabbix`            | [Zabbix](zabbix.md)                                       |\n| Sentry              | `POST /api/integrations/sentry/<route_id>` | [Sentry](sentry.md)                                       |\n| LibreNMS            | `POST /api/integrations/librenms`          | [LibreNMS](librenms.md)                                   |\n| Generic webhook / PagerDuty Events API v2 | `POST /api/integrations/webhook` | [Generic webhook](generic-webhook.md) |\n\nRoute intake tokens belong to routes, not to channels. Create a route first, copy its intake token, and use that token in the monitoring system.\n\n## Notification channels\n\nNotification channels deliver alerts after a route has matched an incoming alert.\n\n| Channel type    | Purpose                                                                        | Documentation                                 |\n|-----------------|--------------------------------------------------------------------------------|-----------------------------------------------|\n| Mattermost      | Chat notifications, optional ACK/Resolve buttons, message updates              | [Mattermost channel](mattermost.md)           |\n| Telegram        | Telegram Bot API notifications, optional inline actions                        | [Telegram channel](telegram.md)               |\n| Email           | Sends email to the assigned user's profile email                               | [Email channel](email.md)                     |\n| Slack           | Sends notifications to a Slack incoming webhook                                | [Webhook-based channels](webhook-channels.md) |\n| Discord         | Sends notifications to a Discord webhook                                       | [Webhook-based channels](webhook-channels.md) |\n| Microsoft Teams | Sends notifications to a Teams webhook                                         | [Webhook-based channels](webhook-channels.md) |\n| Webhook         | Sends notification payloads to a custom HTTP endpoint                          | [Webhook-based channels](webhook-channels.md) |\n| Voice call      | Calls the assigned user's phone through the globally configured voice provider | [Voice call channel](voice-call.md)           |\n\nRead the common channel behavior first: [Notification channels](channels.md).\n\n## Common setup order\n\n```text\n1. Create a group\n2. Create users and fill contact fields, such as email, phone, Telegram ID\n3. Optional: configure browser push and ask users to enable it in Profile\n4. Create a team\n5. Create a rotation and assign on-call users\n6. Create notification channels\n7. Create a route and attach channels\n8. Copy the route intake token\n9. Configure Alertmanager, Zabbix, or webhook sender\n10. Send a test alert\n11. Verify notification delivery and ACK/Resolve flow\n```\n\n## Troubleshooting direction\n\nWhen an alert does not notify a user through a channel, check the chain in order:\n\n```text\nIncoming payload -> route match -> route channels -> channel severity filter -> notifier -> external provider\n```\n\nFor browser push, check the profile-level chain instead:\n\n```text\nalert assignee -> assignee has active browser push subscription -> service worker/browser notification\n```\n\nIf a test channel notification works but real alerts do not, the issue is usually route matching, route-channel binding, severity filtering, missing assignee contact data, or a silence rule.\n\nIf the browser push test works but the real alert push does not, check that the alert is assigned to the same user who enabled push in Profile.\n", 'default')], 'app/templates/pages/routes.html': [('0af3f0af92a405ff931a283a6146944f900125a2e2da6bc6bd3112d331a17c12', '221197b5d5250b18c54055e5a609b740b5cfcaf5b57f8a14bbf01b5c9e9544e3', '<div class="first-div-page-layout page-stack">\n  <div class="summary-grid">\n    <div class="summary-card summary-primary">\n      <div class="summary-icon">↱</div>\n      <div>\n        <div class="summary-title">Routes</div>\n        <div id="routes-summary-total" class="summary-value">0</div>\n        <div class="summary-hint">Alert intake rules</div>\n      </div>\n    </div>\n\n    <div class="summary-card summary-success">\n      <div class="summary-icon">✓</div>\n      <div>\n        <div class="summary-title">Enabled</div>\n        <div id="routes-summary-enabled" class="summary-value">0</div>\n        <div class="summary-hint">Accepting alerts</div>\n      </div>\n    </div>\n\n    <div class="summary-card summary-muted">\n      <div class="summary-icon">×</div>\n      <div>\n        <div class="summary-title">Disabled</div>\n        <div id="routes-summary-disabled" class="summary-value">0</div>\n        <div class="summary-hint">Not accepting alerts</div>\n      </div>\n    </div>\n\n    <div class="summary-card summary-info">\n      <div class="summary-icon">↻</div>\n      <div>\n        <div class="summary-title">With escalation</div>\n        <div id="routes-summary-rotation" class="summary-value">0</div>\n        <div class="summary-hint">Rotation or policy</div>\n      </div>\n    </div>\n  </div>\n\n  <div class="page-layout-grid">\n    <section class="card page-layout-main">\n      <div class="card-header">\n        <div>\n          <h2>Alert routes</h2>\n          <div class="card-subtitle">\n            Showing <span id="routes-filtered-count">0</span>\n            of <span id="routes-total-count">0</span> routes\n          </div>\n        </div>\n\n        <div class="card-header-actions">\n          <button id="open-route-create-modal" type="button" class="btn btn-primary">\n            New route\n          </button>\n\n          <button id="reload-routes" type="button" class="btn">\n            Reload\n          </button>\n        </div>\n      </div>\n\n      <div class="page-toolbar is-flush-top">\n        <div class="page-toolbar-search">\n          <span class="page-toolbar-search-icon">⌕</span>\n          <input\n            id="routes-search"\n            class="input"\n            type="search"\n            placeholder="Search routes..."\n            autocomplete="off"\n          >\n        </div>\n\n        <select id="routes-source-filter" class="input page-toolbar-filter">\n          <option value="">All sources</option>\n        </select>\n\n        <select id="routes-status-filter" class="input page-toolbar-filter">\n          <option value="">All statuses</option>\n          <option value="enabled">Enabled</option>\n          <option value="disabled">Disabled</option>\n        </select>\n      </div>\n\n      <div class="table-wrapper">\n        <table id="routes-table-view" class="data-table sortable-table">\n          <thead>\n            <tr>\n              <th data-sort="name">Route</th>\n              <th data-sort="team">Team</th>\n              <th data-sort="source">Source</th>\n              <th data-sort="rotation">Escalation</th>\n              <th data-sort="channels">Channels</th>\n              <th>Token</th>\n              <th data-sort="enabled">Status</th>\n              <th class="actions-th">Actions</th>\n            </tr>\n          </thead>\n\n          <tbody id="routes-table">\n            <tr>\n              <td colspan="8" class="empty-cell">No routes loaded</td>\n            </tr>\n          </tbody>\n        </table>\n      </div>\n    </section>\n\n    <aside class="card page-details-card">\n      <div class="card-header">\n        <div>\n          <h2>Route details</h2>\n          <div id="route-details-subtitle" class="card-subtitle">\n            Select a route\n          </div>\n        </div>\n      </div>\n\n      <div id="route-details-body" class="details-body">\n        <div class="details-empty">\n          Click a route name to inspect matchers, group by, channels and intake token prefix.\n        </div>\n      </div>\n    </aside>\n  </div>\n\n  <div id="route-form-modal" class="app-modal" style="display: none;">\n    <div class="app-modal-dialog app-modal-dialog-wide">\n      <div class="app-modal-header">\n        <div>\n          <h2 id="route-form-title">Create route</h2>\n          <div class="card-subtitle">\n            Match incoming alerts and deliver them to rotations and channels.\n          </div>\n        </div>\n\n        <button\n          id="close-route-form-modal"\n          type="button"\n          class="app-modal-close"\n          aria-label="Close"\n        >\n          ×\n        </button>\n      </div>\n\n      <div class="app-modal-body">\n        <div class="grid-two">\n          <section class="card">\n            <div class="card-header">\n              <div>\n                <h2>Route</h2>\n                <div class="card-subtitle">\n                  Team, source, rotation and notification channels.\n                </div>\n              </div>\n            </div>\n\n            <div class="form-body">\n              <input id="route-id" type="hidden">\n\n              <label for="route-team">Team</label>\n              <select id="route-team" class="input"></select>\n\n              <label for="route-name">Name</label>\n              <input\n                id="route-name"\n                class="input"\n                type="text"\n                placeholder="Critical infrastructure alerts"\n                autocomplete="off"\n              >\n\n              <label for="route-source">Source</label>\n              <select id="route-source" class="input">\n                  <option value="alertmanager">Alertmanager</option>\n                  <option value="aws_sns">AWS SNS / CloudWatch</option>\n                  <option value="grafana">Grafana</option>\n                  <option value="rmon">RMON</option>\n                  <option value="heartbeat">Heartbeat</option>\n                  <option value="zabbix">Zabbix</option>\n                  <option value="sentry">Sentry</option>\n                  <option value="librenms">LibreNMS</option>\n                  <option value="webhook">Webhook / PagerDuty Events API v2</option>\n              </select>\n              <div id="route-webhook-compatibility-help" class="help-text is-hidden">\n                Webhook routes accept the IncidentRelay generic payload and PagerDuty Events API v2-compatible trigger, acknowledge and resolve events. Use the route intake token as routing_key.\n              </div>\n                <div id="route-sentry-settings" class="form-group is-hidden">\n                    <label for="route-sentry-webhook-secret">Sentry webhook secret</label>\n                    <input\n                        id="route-sentry-webhook-secret"\n                        class="input"\n                        type="password"\n                        placeholder="Paste Sentry Client Secret"\n                        autocomplete="new-password"\n                    >\n                    <div id="route-sentry-secret-help" class="help-text">\n                        Create the route first to get the Sentry webhook URL. Then create a Sentry Internal Integration and paste its Client Secret here.\n                    </div>\n                    <div class="form-group">\n                        <label for="route-sentry-base-url">Sentry base URL</label>\n                        <input\n                            id="route-sentry-base-url"\n                            class="input"\n                            type="url"\n                            placeholder="https://sentry.io"\n                            autocomplete="off"\n                            spellcheck="false"\n                        >\n                        <div class="help-text">\n                            Used as fallback when webhook payload does not include an event URL.\n                            For self-hosted Sentry, use your Sentry root URL.\n                        </div>\n                    </div>\n\n                    <div class="form-group">\n                        <label for="route-sentry-organization-slug">Sentry organization slug</label>\n                        <input\n                            id="route-sentry-organization-slug"\n                            class="input"\n                            type="text"\n                            placeholder="my-org"\n                            autocomplete="off"\n                            spellcheck="false"\n                        >\n                        <div class="help-text">\n                            Used together with issue_id to build a fallback Sentry issue link.\n                        </div>\n                    </div>\n                </div>\n                <div id="route-aws-sns-settings" class="is-hidden">\n                    <div class="form-group">\n                        <label for="route-aws-sns-topic-arn">\n                            AWS SNS Topic ARN\n                        </label>\n\n                        <input\n                            id="route-aws-sns-topic-arn"\n                            type="text"\n                            autocomplete="off"\n                            spellcheck="false"\n                            placeholder="arn:aws:sns:eu-west-1:123456789012:incidentrelay-alerts"\n                        >\n\n                        <div class="form-help">\n                            Only signed SNS messages from this exact topic will be accepted.\n                        </div>\n                    </div>\n\n                    <div\n                        id="route-aws-sns-webhook-group"\n                        class="form-group is-hidden"\n                    >\n                        <label for="route-aws-sns-webhook-url">\n                            AWS SNS webhook URL\n                        </label>\n\n                        <input\n                            id="route-aws-sns-webhook-url"\n                            type="text"\n                            readonly\n                        >\n\n                        <div class="form-help">\n                            Add this HTTPS endpoint as an HTTP/S subscription of the SNS topic.\n                            No route intake token is required because SNS requests are verified\n                            by signature and Topic ARN.\n                        </div>\n                    </div>\n                </div>\n                <label for="route-service">Default service</label>\n                <select id="route-service" class="input">\n                  <option value="">No service</option>\n                </select>\n                <div class="form-group">\n                    <label for="route-escalation-mode">Escalation mode</label>\n                    <select id="route-escalation-mode">\n                        <option value="rotation">Simple rotation</option>\n                        <option value="policy">Escalation policy</option>\n                    </select>\n                </div>\n\n                <div class="form-group" id="route-policy-group">\n                    <label for="route-escalation-policy">Escalation policy</label>\n                    <select id="route-escalation-policy">\n                        <option value="">No policy</option>\n                    </select>\n                </div>\n              <label for="route-rotation">Rotation</label>\n              <select id="route-rotation" class="input">\n                <option value="">No rotation</option>\n              </select>\n\n              <label for="route-channels">Channels</label>\n              <select id="route-channels" class="input" multiple></select>\n\n                <label for="route-notification-channel-mode">\n                  Notification channel source\n                </label>\n\n                <select id="route-notification-channel-mode" class="input">\n                  <option value="route_only">Route channels only</option>\n                  <option value="service_policy">Service notification policy</option>\n                  <option value="service_policy_plus_route">\n                    Service policy + route channels\n                  </option>\n                </select>\n\n                <div id="route-notification-channel-mode-help" class="help-text"></div>\n\n              <label class="md-checkbox">\n                <input id="route-enabled" type="checkbox" checked>\n                <span>Enabled</span>\n              </label>\n\n              <div class="help-text">\n                Disabled routes stay visible, but stop accepting incoming alerts.\n              </div>\n            </div>\n          </section>\n\n          <section class="card">\n            <div class="card-header">\n              <div>\n                <h2>Matching</h2>\n                <div class="card-subtitle">\n                  Alert filters and group-by configuration.\n                </div>\n              </div>\n            </div>\n\n            <div class="form-body">\n                <label for="route-matcher-preset">Matcher preset</label>\n                <select id="route-matcher-preset" class="input">\n                  <option value="">No preset</option>\n                </select>\n\n                <div id="route-matcher-preset-hint" class="help-text">\n                  No preset selected. Only the additional matchers below will be evaluated.\n                </div>\n\n                <label for="route-matchers">Additional matchers JSON</label>\n\n              <div class="json-editor">\n                <div class="json-editor-header">\n                  <span>Matchers</span>\n\n                </div>\n\n                <textarea\n                  id="route-matchers"\n                  class="input code-textarea json-editor-textarea"\n                  spellcheck="false"\n                  autocomplete="off"\n                >{}</textarea>\n              </div>\n\n              <label for="route-group-by">Group by JSON</label>\n\n              <div class="json-editor">\n                <div class="json-editor-header">\n                  <span>Group by</span>\n                </div>\n\n                <textarea\n                  id="route-group-by"\n                  class="input code-textarea json-editor-textarea"\n                  spellcheck="false"\n                  autocomplete="off"\n                >[]</textarea>\n              </div>\n\n              <div class="help-text">\n                Matchers are JSON object filters. Group by is a JSON array of label names used for alert aggregation. Leave it empty to keep each dedup key in its own group.\n              </div>\n            </div>\n          </section>\n        </div>\n      </div>\n\n      <div class="app-modal-footer">\n        <button id="reset-route-form" type="button" class="btn">\n          Reset\n        </button>\n\n        <button id="save-route" type="button" class="btn btn-primary">\n          Save route\n        </button>\n      </div>\n    </div>\n  </div>\n    <div id="route-token-box" class="app-modal is-hidden" style="display: none;" aria-hidden="true">\n        <div class="app-modal-dialog">\n            <div class="app-modal-header">\n                <div>\n                    <h2 id="route-intake-title">Route intake details</h2>\n                    <p id="route-intake-subtitle">\n                        Copy these values now. The token may not be shown again.\n                    </p>\n                </div>\n                <button id="close-route-token-modal" class="modal-close" type="button">\n                    ×\n                </button>\n            </div>\n\n            <div class="app-modal-body">\n                <div class="copy-fields">\n                    <div class="copy-field">\n                        <label for="route-intake-url">Webhook URL</label>\n                        <input\n                            id="route-intake-url"\n                            class="input copy-field-control"\n                            type="text"\n                            readonly\n                        >\n                        <div id="route-intake-url-help" class="help-text">\n                            Send incoming alerts to this URL.\n                        </div>\n                    </div>\n\n                    <div id="route-intake-token-group" class="copy-field">\n                        <label for="route-intake-token">Token</label>\n                        <input\n                            id="route-intake-token"\n                            class="input copy-field-control"\n                            type="text"\n                            readonly\n                        >\n                        <div class="help-text">\n                            Use this token as an Authorization Bearer token.\n                        </div>\n                    </div>\n\n                    <div id="route-intake-command-group" class="copy-field">\n                        <label for="route-intake-curl">Example curl</label>\n                        <textarea\n                            id="route-intake-curl"\n                            class="input copy-field-control copy-field-code"\n                            rows="8"\n                            readonly\n                        ></textarea>\n                    </div>\n                </div>\n            </div>\n\n            <div class="app-modal-footer">\n                <button id="copy-route-intake-url" class="btn btn-secondary" type="button">\n                    Copy URL\n                </button>\n                <button id="copy-route-intake-token" class="btn btn-secondary" type="button">\n                    Copy token\n                </button>\n                <button id="copy-route-intake-curl" class="btn btn-secondary" type="button">\n                    Copy curl\n                </button>\n                <button id="close-route-token-modal-footer" class="btn btn-primary" type="button">\n                    Close\n                </button>\n            </div>\n        </div>\n    </div>\n    <div id="route-service-rules-modal" class="app-modal" style="display: none;">\n  <div class="app-modal-dialog app-modal-dialog-wide">\n    <div class="app-modal-header">\n      <div>\n        <h2 id="route-service-rules-title">Service rules</h2>\n        <div class="card-subtitle">\n          Match alerts inside this route to affected services.\n        </div>\n      </div>\n\n      <button\n        id="close-route-service-rules-modal"\n        type="button"\n        class="app-modal-close"\n        aria-label="Close"\n      >\n        ×\n      </button>\n    </div>\n\n    <div class="app-modal-body">\n      <div class="grid-two">\n        <section class="card">\n          <div class="card-header">\n            <div>\n              <h2>Rules</h2>\n              <div class="card-subtitle">\n                Route-specific service classification rules.\n              </div>\n            </div>\n          </div>\n\n          <div class="table-wrapper">\n            <table class="data-table">\n              <thead>\n                <tr>\n                  <th>Position</th>\n                  <th>Name</th>\n                  <th>Service</th>\n                  <th>Matchers</th>\n                  <th>Status</th>\n                  <th class="actions-th">Actions</th>\n                </tr>\n              </thead>\n              <tbody id="route-service-rules-table">\n                <tr>\n                  <td colspan="6" class="empty-cell">No service rules</td>\n                </tr>\n              </tbody>\n            </table>\n          </div>\n        </section>\n\n        <section class="card">\n          <div class="card-header">\n            <div>\n              <h2 id="service-rule-form-title">Create rule</h2>\n              <div class="card-subtitle">\n                Define matchers and target service.\n              </div>\n            </div>\n          </div>\n\n          <div class="form-body">\n            <input id="service-rule-id" type="hidden">\n            <input id="service-rule-team" type="hidden">\n            <input id="service-rule-route" type="hidden">\n\n            <label for="service-rule-name">Name</label>\n            <input\n              id="service-rule-name"\n              class="input"\n              type="text"\n              placeholder="PostgreSQL alerts"\n              autocomplete="off"\n            >\n\n            <label for="service-rule-service">Service</label>\n            <select id="service-rule-service" class="input">\n              <option value="">Select service</option>\n            </select>\n\n            <label for="service-rule-position">Position</label>\n            <input id="service-rule-position" class="input" type="number" min="0" value="0">\n              <label for="service-rule-matcher-preset">Matcher preset</label>\n              <select id="service-rule-matcher-preset" class="input">\n                  <option value="">No preset</option>\n              </select>\n\n                <div id="service-rule-matcher-preset-hint" class="help-text">\n                  No preset selected. Only the additional matchers below will be evaluated.\n                </div>\n\n            <div class="json-editor">\n              <div class="json-editor-header">\n                  <span>Additional matchers</span>\n              </div>\n\n              <textarea\n                id="service-rule-matchers"\n                class="input code-textarea json-editor-textarea"\n                rows="8"\n                spellcheck="false"\n              >{\n              "labels": {\n                "cluster": "cloud-postgresql"\n              }\n            }</textarea>\n            </div>\n\n            <label class="md-checkbox">\n              <input id="service-rule-enabled" type="checkbox" checked>\n              <span>Enabled</span>\n            </label>\n\n            <div class="form-actions">\n              <button id="reset-service-rule-form" type="button" class="btn">\n                Reset\n              </button>\n              <button id="save-service-rule" type="button" class="btn btn-primary">\n                Save rule\n              </button>\n            </div>\n          </div>\n        </section>\n      </div>\n    </div>\n  </div>\n</div>\n</div>\n', 'plain'), ('0ef58ceff9dfcb31647fde1be70c1b909d08ccd2ce298fe695774f4ae839e105', 'b5a3db62f081d4ccaed176ede497bb36eddf62b66eefd56072b706e8990abdae', '<div class="first-div-page-layout page-stack">\n  <div class="summary-grid">\n    <div class="summary-card summary-primary">\n      <div class="summary-icon">↱</div>\n      <div>\n        <div class="summary-title">{{ _("routes.summary.routes") }}</div>\n        <div id="routes-summary-total" class="summary-value">0</div>\n        <div class="summary-hint">{{ _("routes.summary.routes_hint") }}</div>\n      </div>\n    </div>\n\n    <div class="summary-card summary-success">\n      <div class="summary-icon">✓</div>\n      <div>\n        <div class="summary-title">{{ _("routes.summary.enabled") }}</div>\n        <div id="routes-summary-enabled" class="summary-value">0</div>\n        <div class="summary-hint">{{ _("routes.summary.enabled_hint") }}</div>\n      </div>\n    </div>\n\n    <div class="summary-card summary-muted">\n      <div class="summary-icon">×</div>\n      <div>\n        <div class="summary-title">{{ _("routes.summary.disabled") }}</div>\n        <div id="routes-summary-disabled" class="summary-value">0</div>\n        <div class="summary-hint">{{ _("routes.summary.disabled_hint") }}</div>\n      </div>\n    </div>\n\n    <div class="summary-card summary-info">\n      <div class="summary-icon">↻</div>\n      <div>\n        <div class="summary-title">{{ _("routes.summary.escalation") }}</div>\n        <div id="routes-summary-rotation" class="summary-value">0</div>\n        <div class="summary-hint">{{ _("routes.summary.escalation_hint") }}</div>\n      </div>\n    </div>\n  </div>\n\n  <div class="page-layout-grid">\n    <section class="card page-layout-main">\n      <div class="card-header">\n        <div>\n          <h2>{{ _("routes.list.title") }}</h2>\n          <div class="card-subtitle">\n            {{ _("routes.list.showing") }} <span id="routes-filtered-count">0</span>\n            {{ _("routes.list.of") }} <span id="routes-total-count">0</span> {{ _("routes.list.routes") }}\n          </div>\n        </div>\n\n        <div class="card-header-actions">\n          <button id="open-route-create-modal" type="button" class="btn btn-primary">\n            {{ _("routes.actions.new") }}\n          </button>\n\n          <button id="reload-routes" type="button" class="btn">\n            {{ _("routes.actions.reload") }}\n          </button>\n        </div>\n      </div>\n\n      <div class="page-toolbar is-flush-top">\n        <div class="page-toolbar-search">\n          <span class="page-toolbar-search-icon">⌕</span>\n          <input\n            id="routes-search"\n            class="input"\n            type="search"\n            placeholder="{{ _(\'routes.search.placeholder\') }}"\n            autocomplete="off"\n          >\n        </div>\n\n        <select id="routes-source-filter" class="input page-toolbar-filter">\n          <option value="">{{ _("routes.filters.all_sources") }}</option>\n        </select>\n\n        <select id="routes-status-filter" class="input page-toolbar-filter">\n          <option value="">{{ _("routes.filters.all_statuses") }}</option>\n          <option value="enabled">{{ _("routes.status.enabled") }}</option>\n          <option value="disabled">{{ _("routes.status.disabled") }}</option>\n        </select>\n      </div>\n\n      <div class="table-wrapper">\n        <table id="routes-table-view" class="data-table sortable-table">\n          <thead>\n            <tr>\n              <th data-sort="name">{{ _("routes.table.route") }}</th>\n              <th data-sort="team">{{ _("routes.table.team") }}</th>\n              <th data-sort="source">{{ _("routes.table.source") }}</th>\n              <th data-sort="rotation">{{ _("routes.table.escalation") }}</th>\n              <th data-sort="channels">{{ _("routes.table.channels") }}</th>\n              <th>{{ _("routes.table.token") }}</th>\n              <th data-sort="enabled">{{ _("routes.table.status") }}</th>\n              <th class="actions-th">{{ _("routes.table.actions") }}</th>\n            </tr>\n          </thead>\n\n          <tbody id="routes-table">\n            <tr>\n              <td colspan="8" class="empty-cell">{{ _("routes.table.empty_loaded") }}</td>\n            </tr>\n          </tbody>\n        </table>\n      </div>\n    </section>\n\n    <aside class="card page-details-card">\n      <div class="card-header">\n        <div>\n          <h2>{{ _("routes.details.title") }}</h2>\n          <div id="route-details-subtitle" class="card-subtitle">\n            {{ _("routes.details.select") }}\n          </div>\n        </div>\n      </div>\n\n      <div id="route-details-body" class="details-body">\n        <div class="details-empty">\n          {{ _("routes.details.empty") }}\n        </div>\n      </div>\n    </aside>\n  </div>\n\n  <div id="route-form-modal" class="app-modal" style="display: none;">\n    <div class="app-modal-dialog app-modal-dialog-wide">\n      <div class="app-modal-header">\n        <div>\n          <h2 id="route-form-title">{{ _("routes.form.create") }}</h2>\n          <div class="card-subtitle">\n            {{ _("routes.form.subtitle") }}\n          </div>\n        </div>\n\n        <button\n          id="close-route-form-modal"\n          type="button"\n          class="app-modal-close"\n          aria-label="{{ _(\'routes.actions.close\') }}"\n        >\n          ×\n        </button>\n      </div>\n\n      <div class="app-modal-body">\n        <div class="grid-two">\n          <section class="card">\n            <div class="card-header">\n              <div>\n                <h2>{{ _("routes.form.route_section") }}</h2>\n                <div class="card-subtitle">\n                  {{ _("routes.form.route_section_hint") }}\n                </div>\n              </div>\n            </div>\n\n            <div class="form-body">\n              <input id="route-id" type="hidden">\n\n              <label for="route-team">{{ _("routes.form.team") }}</label>\n              <select id="route-team" class="input"></select>\n\n              <label for="route-name">{{ _("routes.form.name") }}</label>\n              <input\n                id="route-name"\n                class="input"\n                type="text"\n                placeholder="{{ _(\'routes.form.name_placeholder\') }}"\n                autocomplete="off"\n              >\n\n              <label for="route-source">{{ _("routes.form.source") }}</label>\n              <select id="route-source" class="input">\n                  <option value="alertmanager">Alertmanager</option>\n                  <option value="aws_sns">AWS SNS / CloudWatch</option>\n                  <option value="grafana">Grafana</option>\n                  <option value="rmon">RMON</option>\n                  <option value="heartbeat">Heartbeat</option>\n                  <option value="zabbix">Zabbix</option>\n                  <option value="sentry">Sentry</option>\n                  <option value="librenms">LibreNMS</option>\n                  <option value="webhook">Webhook / PagerDuty Events API v2</option>\n              </select>\n              <div id="route-webhook-compatibility-help" class="help-text is-hidden">\n                {{ _("routes.form.webhook_pd_help") }}\n              </div>\n                <div id="route-sentry-settings" class="form-group is-hidden">\n                    <label for="route-sentry-webhook-secret">{{ _("routes.form.sentry_secret") }}</label>\n                    <input\n                        id="route-sentry-webhook-secret"\n                        class="input"\n                        type="password"\n                        placeholder="{{ _(\'routes.form.sentry_secret_placeholder\') }}"\n                        autocomplete="new-password"\n                    >\n                    <div id="route-sentry-secret-help" class="help-text">\n                        {{ _("routes.form.sentry_secret_new_help") }}\n                    </div>\n                    <div class="form-group">\n                        <label for="route-sentry-base-url">{{ _("routes.form.sentry_base_url") }}</label>\n                        <input\n                            id="route-sentry-base-url"\n                            class="input"\n                            type="url"\n                            placeholder="https://sentry.io"\n                            autocomplete="off"\n                            spellcheck="false"\n                        >\n                        <div class="help-text">\n                            {{ _("routes.form.sentry_base_url_help") }}\n                        </div>\n                    </div>\n\n                    <div class="form-group">\n                        <label for="route-sentry-organization-slug">{{ _("routes.form.sentry_org") }}</label>\n                        <input\n                            id="route-sentry-organization-slug"\n                            class="input"\n                            type="text"\n                            placeholder="my-org"\n                            autocomplete="off"\n                            spellcheck="false"\n                        >\n                        <div class="help-text">\n                            {{ _("routes.form.sentry_org_help") }}\n                        </div>\n                    </div>\n                </div>\n                <div id="route-aws-sns-settings" class="is-hidden">\n                    <div class="form-group">\n                        <label for="route-aws-sns-topic-arn">\n                            {{ _("routes.form.sns_topic") }}\n                        </label>\n\n                        <input\n                            id="route-aws-sns-topic-arn"\n                            type="text"\n                            autocomplete="off"\n                            spellcheck="false"\n                            placeholder="arn:aws:sns:eu-west-1:123456789012:incidentrelay-alerts"\n                        >\n\n                        <div class="form-help">\n                            {{ _("routes.form.sns_topic_help") }}\n                        </div>\n                    </div>\n\n                    <div\n                        id="route-aws-sns-webhook-group"\n                        class="form-group is-hidden"\n                    >\n                        <label for="route-aws-sns-webhook-url">\n                            {{ _("routes.form.sns_webhook") }}\n                        </label>\n\n                        <input\n                            id="route-aws-sns-webhook-url"\n                            type="text"\n                            readonly\n                        >\n\n                        <div class="form-help">\n                            {{ _("routes.form.sns_webhook_help") }}\n                        </div>\n                    </div>\n                </div>\n                <label for="route-service">{{ _("routes.form.default_service") }}</label>\n                <select id="route-service" class="input">\n                  <option value="">{{ _("routes.form.no_service") }}</option>\n                </select>\n                <div class="form-group">\n                    <label for="route-escalation-mode">{{ _("routes.form.escalation_mode") }}</label>\n                    <select id="route-escalation-mode">\n                        <option value="rotation">{{ _("routes.form.simple_rotation") }}</option>\n                        <option value="policy">{{ _("routes.form.escalation_policy") }}</option>\n                    </select>\n                </div>\n\n                <div class="form-group" id="route-policy-group">\n                    <label for="route-escalation-policy">{{ _("routes.form.escalation_policy") }}</label>\n                    <select id="route-escalation-policy">\n                        <option value="">{{ _("routes.form.no_policy") }}</option>\n                    </select>\n                </div>\n              <label for="route-rotation">{{ _("routes.form.rotation") }}</label>\n              <select id="route-rotation" class="input">\n                <option value="">{{ _("routes.form.no_rotation") }}</option>\n              </select>\n\n              <label for="route-channels">{{ _("routes.form.channels") }}</label>\n              <select id="route-channels" class="input" multiple></select>\n\n                <label for="route-notification-channel-mode">\n                  {{ _("routes.form.notification_source") }}\n                </label>\n\n                <select id="route-notification-channel-mode" class="input">\n                  <option value="route_only">{{ _("routes.notification.route_only") }}</option>\n                  <option value="service_policy">{{ _("routes.notification.service_policy") }}</option>\n                  <option value="service_policy_plus_route">\n                    {{ _("routes.notification.combined") }}\n                  </option>\n                </select>\n\n                <div id="route-notification-channel-mode-help" class="help-text"></div>\n\n              <label class="md-checkbox">\n                <input id="route-enabled" type="checkbox" checked>\n                <span>{{ _("routes.form.enabled") }}</span>\n              </label>\n\n              <div class="help-text">\n                {{ _("routes.form.disabled_help") }}\n              </div>\n            </div>\n          </section>\n\n          <section class="card">\n            <div class="card-header">\n              <div>\n                <h2>{{ _("routes.form.matching") }}</h2>\n                <div class="card-subtitle">\n                  {{ _("routes.form.matching_hint") }}\n                </div>\n              </div>\n            </div>\n\n            <div class="form-body">\n                <label for="route-matcher-preset">{{ _("routes.form.matcher_preset") }}</label>\n                <select id="route-matcher-preset" class="input">\n                  <option value="">{{ _("routes.form.no_preset") }}</option>\n                </select>\n\n                <div id="route-matcher-preset-hint" class="help-text">\n                  {{ _("routes.form.no_preset_hint") }}\n                </div>\n\n                <label for="route-matchers">{{ _("routes.form.additional_matchers_json") }}</label>\n\n              <div class="json-editor">\n                <div class="json-editor-header">\n                  <span>{{ _("routes.form.matchers") }}</span>\n\n                </div>\n\n                <textarea\n                  id="route-matchers"\n                  class="input code-textarea json-editor-textarea"\n                  spellcheck="false"\n                  autocomplete="off"\n                >{}</textarea>\n              </div>\n\n              <label for="route-group-by">{{ _("routes.form.group_by_json") }}</label>\n\n              <div class="json-editor">\n                <div class="json-editor-header">\n                  <span>{{ _("routes.form.group_by") }}</span>\n                </div>\n\n                <textarea\n                  id="route-group-by"\n                  class="input code-textarea json-editor-textarea"\n                  spellcheck="false"\n                  autocomplete="off"\n                >[]</textarea>\n              </div>\n\n              <div class="help-text">\n                {{ _("routes.form.matchers_help") }}\n              </div>\n            </div>\n          </section>\n        </div>\n      </div>\n\n      <div class="app-modal-footer">\n        <button id="reset-route-form" type="button" class="btn">\n          {{ _("routes.actions.reset") }}\n        </button>\n\n        <button id="save-route" type="button" class="btn btn-primary">\n          {{ _("routes.actions.save_route") }}\n        </button>\n      </div>\n    </div>\n  </div>\n    <div id="route-token-box" class="app-modal is-hidden" style="display: none;" aria-hidden="true">\n        <div class="app-modal-dialog">\n            <div class="app-modal-header">\n                <div>\n                    <h2 id="route-intake-title">{{ _("routes.intake.title") }}</h2>\n                    <p id="route-intake-subtitle">\n                        {{ _("routes.intake.copy_now") }}\n                    </p>\n                </div>\n                <button id="close-route-token-modal" class="modal-close" type="button">\n                    ×\n                </button>\n            </div>\n\n            <div class="app-modal-body">\n                <div class="copy-fields">\n                    <div class="copy-field">\n                        <label for="route-intake-url">{{ _("routes.intake.webhook_url") }}</label>\n                        <input\n                            id="route-intake-url"\n                            class="input copy-field-control"\n                            type="text"\n                            readonly\n                        >\n                        <div id="route-intake-url-help" class="help-text">\n                            {{ _("routes.intake.send_url") }}\n                        </div>\n                    </div>\n\n                    <div id="route-intake-token-group" class="copy-field">\n                        <label for="route-intake-token">{{ _("routes.intake.token") }}</label>\n                        <input\n                            id="route-intake-token"\n                            class="input copy-field-control"\n                            type="text"\n                            readonly\n                        >\n                        <div class="help-text">\n                            {{ _("routes.intake.token_help") }}\n                        </div>\n                    </div>\n\n                    <div id="route-intake-command-group" class="copy-field">\n                        <label for="route-intake-curl">{{ _("routes.intake.example_curl") }}</label>\n                        <textarea\n                            id="route-intake-curl"\n                            class="input copy-field-control copy-field-code"\n                            rows="8"\n                            readonly\n                        ></textarea>\n                    </div>\n                </div>\n            </div>\n\n            <div class="app-modal-footer">\n                <button id="copy-route-intake-url" class="btn btn-secondary" type="button">\n                    {{ _("routes.intake.copy_url") }}\n                </button>\n                <button id="copy-route-intake-token" class="btn btn-secondary" type="button">\n                    {{ _("routes.intake.copy_token") }}\n                </button>\n                <button id="copy-route-intake-curl" class="btn btn-secondary" type="button">\n                    {{ _("routes.intake.copy_curl") }}\n                </button>\n                <button id="close-route-token-modal-footer" class="btn btn-primary" type="button">\n                    {{ _("routes.actions.close") }}\n                </button>\n            </div>\n        </div>\n    </div>\n    <div id="route-service-rules-modal" class="app-modal" style="display: none;">\n  <div class="app-modal-dialog app-modal-dialog-wide">\n    <div class="app-modal-header">\n      <div>\n        <h2 id="route-service-rules-title">{{ _("routes.rules.title") }}</h2>\n        <div class="card-subtitle">\n          {{ _("routes.rules.subtitle") }}\n        </div>\n      </div>\n\n      <button\n        id="close-route-service-rules-modal"\n        type="button"\n        class="app-modal-close"\n        aria-label="{{ _(\'routes.actions.close\') }}"\n      >\n        ×\n      </button>\n    </div>\n\n    <div class="app-modal-body">\n      <div class="grid-two">\n        <section class="card">\n          <div class="card-header">\n            <div>\n              <h2>{{ _("routes.rules.rules") }}</h2>\n              <div class="card-subtitle">\n                {{ _("routes.rules.rules_hint") }}\n              </div>\n            </div>\n          </div>\n\n          <div class="table-wrapper">\n            <table class="data-table">\n              <thead>\n                <tr>\n                  <th>{{ _("routes.rules.position") }}</th>\n                  <th>{{ _("routes.rules.name") }}</th>\n                  <th>{{ _("routes.rules.service") }}</th>\n                  <th>{{ _("routes.rules.matchers") }}</th>\n                  <th>{{ _("routes.rules.status") }}</th>\n                  <th class="actions-th">{{ _("routes.table.actions") }}</th>\n                </tr>\n              </thead>\n              <tbody id="route-service-rules-table">\n                <tr>\n                  <td colspan="6" class="empty-cell">{{ _("routes.rules.empty") }}</td>\n                </tr>\n              </tbody>\n            </table>\n          </div>\n        </section>\n\n        <section class="card">\n          <div class="card-header">\n            <div>\n              <h2 id="service-rule-form-title">{{ _("routes.rules.create") }}</h2>\n              <div class="card-subtitle">\n                {{ _("routes.rules.form_hint") }}\n              </div>\n            </div>\n          </div>\n\n          <div class="form-body">\n            <input id="service-rule-id" type="hidden">\n            <input id="service-rule-team" type="hidden">\n            <input id="service-rule-route" type="hidden">\n\n            <label for="service-rule-name">{{ _("routes.rules.name") }}</label>\n            <input\n              id="service-rule-name"\n              class="input"\n              type="text"\n              placeholder="{{ _(\'routes.rules.name_placeholder\') }}"\n              autocomplete="off"\n            >\n\n            <label for="service-rule-service">{{ _("routes.rules.service") }}</label>\n            <select id="service-rule-service" class="input">\n              <option value="">{{ _("routes.rules.select_service") }}</option>\n            </select>\n\n            <label for="service-rule-position">{{ _("routes.rules.position") }}</label>\n            <input id="service-rule-position" class="input" type="number" min="0" value="0">\n              <label for="service-rule-matcher-preset">{{ _("routes.rules.matcher_preset") }}</label>\n              <select id="service-rule-matcher-preset" class="input">\n                  <option value="">{{ _("routes.form.no_preset") }}</option>\n              </select>\n\n                <div id="service-rule-matcher-preset-hint" class="help-text">\n                  {{ _("routes.form.no_preset_hint") }}\n                </div>\n\n            <div class="json-editor">\n              <div class="json-editor-header">\n                  <span>{{ _("routes.rules.additional_matchers") }}</span>\n              </div>\n\n              <textarea\n                id="service-rule-matchers"\n                class="input code-textarea json-editor-textarea"\n                rows="8"\n                spellcheck="false"\n              >{\n              "labels": {\n                "cluster": "cloud-postgresql"\n              }\n            }</textarea>\n            </div>\n\n            <label class="md-checkbox">\n              <input id="service-rule-enabled" type="checkbox" checked>\n              <span>{{ _("routes.form.enabled") }}</span>\n            </label>\n\n            <div class="form-actions">\n              <button id="reset-service-rule-form" type="button" class="btn">\n                {{ _("routes.actions.reset") }}\n              </button>\n              <button id="save-service-rule" type="button" class="btn btn-primary">\n                {{ _("routes.actions.save_rule") }}\n              </button>\n            </div>\n          </div>\n        </section>\n      </div>\n    </div>\n  </div>\n</div>\n</div>\n', 'localized')], 'app/static/js/pages/routes.js': [('2b708898706eb59951b47ccb29808cda51b1bc8800969ca657ced36dd8cb5464', '8209e6efdd40368c438e74b98970aa62303aa6065d8b43c026e46234cddeb1ff', 'let routesCache = [];\nlet selectedRouteDetailsId = null;\nlet routesSortState = createTableSortState("id", "desc");\nconst routesSortColumns = {\n    name: {path: "name", type: "text", defaultDirection: "asc"},\n    team: {\n        value: function (route) {\n            return getRouteTeamLabel(route);\n        },\n        type: "text",\n        defaultDirection: "asc"\n    },\n    source: {path: "source", type: "text", defaultDirection: "asc"},\n    rotation: {\n        value: function (route) {\n            return route.escalation_policy_name || route.rotation_name || "";\n        },\n        type: "text",\n        defaultDirection: "asc"\n    },\n    channels: {\n        value: function (route) {\n            return asArray(route.channels).map(function (channel) {\n                return channel.name || "";\n            }).join(", ");\n        },\n        type: "text",\n        defaultDirection: "asc",\n    },\n    enabled: {path: "enabled", type: "boolean", defaultDirection: "desc"},\n};\nlet selectedRouteForServiceRules = null;\nlet routeServiceRulesCache = [];\nfunction getRouteTeamLabel(route) {\n    return route.team_name || route.team_slug || "-";\n}\nfunction getRouteEscalationLabel(route) {\n    if (route.escalation_policy_name) {\n        return "Policy: " + route.escalation_policy_name;\n    }\n\n    if (route.rotation_name) {\n        return "Rotation: " + route.rotation_name;\n    }\n\n    return "-";\n}\nlet routeMatcherPresetsCache = [];\nlet routeServiceRuleMatcherPresetsCache = [];\n\nfunction updateRouteMatcherPresetHint() {\n    const preset = findMatcherPresetById(routeMatcherPresetsCache, $("#route-matcher-preset").val());\n    $("#route-matcher-preset-hint").text(matcherPresetAndLocalHint(preset));\n}\n\n\nfunction updateServiceRuleMatcherPresetHint() {\n    const preset = findMatcherPresetById(routeServiceRuleMatcherPresetsCache, $("#service-rule-matcher-preset").val());\n    $("#service-rule-matcher-preset-hint").text(matcherPresetAndLocalHint(preset));\n}\n\nfunction getRouteNotificationModeLabel(route) {\n    const mode = route.notification_channel_mode || "route_only";\n\n    if (mode === "service_policy") {\n        return "Service notification policy";\n    }\n\n    if (mode === "service_policy_plus_route") {\n        return "Service policy + route channels";\n    }\n\n    return "Route channels only";\n}\n\nfunction updateRouteNotificationChannelModeUi() {\n    const mode = (\n        $("#route-notification-channel-mode").val()\n        || "route_only"\n    );\n    const routeChannelsDisabled = mode === "service_policy";\n\n    $("#route-channels")\n        .prop("disabled", routeChannelsDisabled)\n        .closest(".form-group")\n        .toggleClass("is-muted", routeChannelsDisabled);\n\n    let helpText;\n\n    if (mode === "service_policy") {\n        helpText = (\n            "Only channels selected by the matched service notification "\n            + "policy will receive shared notifications."\n        );\n    } else if (mode === "service_policy_plus_route") {\n        helpText = (\n            "Channels selected by the service policy are combined with "\n            + "the channels configured on this route."\n        );\n    } else {\n        helpText = (\n            "Only channels configured directly on this route are used."\n        );\n    }\n\n    $("#route-notification-channel-mode-help").text(helpText);\n}\n\nfunction getRouteTeamEscalationLabel(route) {\n    if (route.escalation_policy_name) {\n        return "Ignored for policy mode";\n    }\n\n    if (route.team_escalation_enabled) {\n        return "After " + (route.team_escalation_after_reminders || 0) + " reminders";\n    }\n\n    return "Disabled";\n}\nfunction initializeRouteMatcherEditors() {\n    enhanceMatcherEditor("#route-matchers", {\n        label: "Additional matchers JSON",\n        header: "Additional matchers",\n        context: function () {\n            return {\n                scope: "route",\n                teamId: Number($("#route-team").val()) || null,\n                routeId: Number($("#route-id").val()) || null,\n                matcherPresetId: Number($("#route-matcher-preset").val()) || null,\n            };\n        },\n    });\n\n    enhanceMatcherEditor("#service-rule-matchers", {\n        label: "Additional matchers",\n        context: function () {\n            const route = getSelectedRouteForServiceRules();\n\n            return {\n                scope: "service_rule",\n                teamId: route ? route.team_id : null,\n                routeId: route ? route.id : null,\n                serviceId: Number($("#service-rule-service").val()) || null,\n                matcherPresetId: Number($("#service-rule-matcher-preset").val()) || null,\n            };\n        },\n    });\n}\n\nfunction loadRoutes() {\n    initializeRouteMatcherEditors();\n    fillTeamSelect("#route-team", false, loadRouteDependencies);\n    initRoutesTableSorting();\n    refreshRoutes();\n}\n\nfunction loadRouteDependencies(callback, selectedMatcherPresetId) {\n    const teamId = $("#route-team").val();\n\n    const rotationSelect = $("#route-rotation");\n    const channelSelect = $("#route-channels");\n    const policySelect = $("#route-escalation-policy");\n    const serviceSelect = $("#route-service");\n    const matcherPresetSelect = $("#route-matcher-preset");\n\n    rotationSelect.empty().append($("<option>").val("").text("No rotation"));\n    channelSelect.empty();\n\n    if (policySelect.length) {\n        policySelect.empty().append($("<option>").val("").text("No policy"));\n    }\n\n    if (serviceSelect.length) {\n        serviceSelect.empty().append($("<option>").val("").text("No default service"));\n    }\n\n    if (!teamId) {\n        if (typeof callback === "function") {\n            callback();\n        }\n        return;\n    }\n\n    routeMatcherPresetsCache = [];\n    fillMatcherPresetSelect(matcherPresetSelect, [], selectedMatcherPresetId);\n    updateRouteMatcherPresetHint();\n\n    let rotationsLoaded = false;\n    let channelsLoaded = false;\n    let policiesLoaded = !policySelect.length;\n    let servicesLoaded = !serviceSelect.length;\n    let matcherPresetsLoaded = !matcherPresetSelect.length;\n\n    function finishWhenReady() {\n        if (!rotationsLoaded || !channelsLoaded || !policiesLoaded || !servicesLoaded || !matcherPresetsLoaded) {\n            return;\n        }\n\n        if (typeof callback === "function") {\n            callback();\n        }\n    }\n\n    apiGet("/api/rotations?team_id=" + encodeURIComponent(teamId), function (rotations) {\n        rotationSelect.empty().append($("<option>").val("").text("No rotation"));\n\n        asArray(rotations).forEach(function (rotation) {\n            if (!rotation.enabled) {\n                return;\n            }\n\n            rotationSelect.append(\n                $("<option>")\n                    .val(String(rotation.id))\n                    .text(rotation.name)\n            );\n        });\n\n        rotationsLoaded = true;\n        finishWhenReady();\n    });\n\n    apiGet("/api/channels?team_id=" + encodeURIComponent(teamId), function (channels) {\n        channelSelect.empty();\n\n        asArray(channels).forEach(function (channel) {\n            if (!channel.enabled) {\n                return;\n            }\n\n            channelSelect.append(\n                $("<option>")\n                    .val(String(channel.id))\n                    .text(channel.name + " (" + channel.channel_type + ")")\n            );\n        });\n\n        channelsLoaded = true;\n        finishWhenReady();\n    });\n\n    if (policySelect.length) {\n        apiGet("/api/escalation-policies?team_id=" + encodeURIComponent(teamId), function (policies) {\n            policySelect.empty().append($("<option>").val("").text("No policy"));\n\n            asArray(policies).forEach(function (policy) {\n                if (!policy.enabled) {\n                    return;\n                }\n\n                policySelect.append(\n                    $("<option>")\n                        .val(String(policy.id))\n                        .text(policy.name)\n                );\n            });\n\n            policiesLoaded = true;\n            finishWhenReady();\n        });\n    }\n\n    if (serviceSelect.length) {\n        apiGet("/api/services?team_id=" + encodeURIComponent(teamId), function (services) {\n            serviceSelect.empty().append($("<option>").val("").text("No default service"));\n\n            asArray(services).forEach(function (service) {\n                if (!service.enabled) {\n                    return;\n                }\n\n                serviceSelect.append(\n                    $("<option>")\n                        .val(String(service.id))\n                        .text(service.name + " (" + service.slug + ")")\n                );\n            });\n\n            servicesLoaded = true;\n            finishWhenReady();\n        });\n    }\n    if (matcherPresetSelect.length) {\n        loadMatcherPresetsForTeam(teamId, function (presets) {\n            routeMatcherPresetsCache = presets;\n            fillMatcherPresetSelect(matcherPresetSelect, presets, selectedMatcherPresetId);\n            updateRouteMatcherPresetHint();\n            matcherPresetsLoaded = true;\n            finishWhenReady();\n        });\n    }\n}\n\nfunction refreshRoutes() {\n    apiGet("/api/routes" + selectedTeamQuery(), function (routes) {\n        routesCache = asArray(routes);\n        renderRoutesSummary(routesCache);\n        fillRouteSourceFilter(routesCache);\n        renderRoutesTable();\n        restoreRouteDetails();\n    });\n}\n\nfunction renderRoutesSummary(routes) {\n    routes = asArray(routes);\n\n    const enabled = routes.filter(function (route) {\n        return !!route.enabled;\n    }).length;\n\n    const withEscalation = routes.filter(function (route) {\n        return !!route.rotation_id || !!route.rotation_name ||\n            !!route.escalation_policy_id || !!route.escalation_policy_name;\n    }).length;\n\n    $("#routes-summary-total").text(routes.length);\n    $("#routes-summary-enabled").text(enabled);\n    $("#routes-summary-disabled").text(routes.length - enabled);\n    $("#routes-summary-rotation").text(withEscalation);\n}\n\nfunction fillRouteSourceFilter(routes) {\n    const filter = $("#routes-source-filter");\n    const selected = filter.val();\n    const sources = {\n        alertmanager: true,\n        aws_sns: true,\n        grafana: true,\n        rmon: true,\n        heartbeat: true,\n        zabbix: true,\n        webhook: true,\n        sentry: true,\n        librenms: true\n    };\n\n    asArray(routes).forEach(function (route) {\n        if (route.source) {\n            sources[route.source] = true;\n        }\n    });\n\n    filter.empty();\n    filter.append($("<option>").val("").text("All sources"));\n    Object.keys(sources).sort().forEach(function (source) {\n        filter.append($("<option>").val(source).text(source));\n    });\n    if (selected && sources[selected]) {\n        filter.val(selected);\n    }\n}\n\nfunction getRouteSearchText(route) {\n    const channels = asArray(route.channels).map(function (channel) {\n        return channel.name + " " + channel.channel_type;\n    }).join(" ");\n\n    return [\n        route.id,\n        route.team_name,\n        route.team_slug,\n        route.name,\n        route.source,\n        route.rotation_name,\n        route.escalation_policy_name,\n        route.intake_token_prefix,\n        route.enabled ? "enabled" : "disabled",\n        channels,\n        route.service_name,\n        route.service_slug,\n        route.notification_channel_mode,\n        route.matcher_preset ? route.matcher_preset.name : "",\n        getRouteNotificationModeLabel(route),\n    ].join(" ").toLowerCase();\n}\n\nfunction renderRoutesCounter(filteredRoutes, allRoutes) {\n    filteredRoutes = asArray(filteredRoutes);\n    allRoutes = asArray(allRoutes);\n    $("#routes-filtered-count").text(filteredRoutes.length);\n    $("#routes-total-count").text(allRoutes.length);\n}\n\nfunction renderRoutesTable() {\n    const tbody = $("#routes-table");\n    const routes = getFilteredRoutes();\n    tbody.empty();\n    renderRoutesCounter(routes, routesCache);\n\n    if (!routes.length) {\n        tbody.append(\n            $("<tr>").append(\n                $("<td>").attr("colspan", "8").addClass("empty-cell").text("No routes")\n            )\n        );\n        return;\n    }\n\n    routes.forEach(function (route) {\n        tbody.append(renderRouteRow(route));\n    });\n}\n\nfunction renderRouteRow(route) {\n    const row = $("<tr>");\n\n    row.append(\n        $("<td>")\n            .append(\n                $("<button>")\n                    .attr("type", "button")\n                    .addClass("name-button")\n                    .text(route.name || "-")\n                    .on("click", function () {\n                        renderRouteDetails(route);\n                    })\n            )\n            .append($("<div>").addClass("row-subtitle").text("Route #" + route.id))\n    );\n    row.append($("<td>").append($("<span>").addClass("route-pill").text(getRouteTeamLabel(route))));\n    row.append($("<td>").text(route.source || "-"));\n    row.append($("<td>").text(getRouteEscalationLabel(route)));\n    row.append($("<td>").append(renderRouteChannels(route)));\n    row.append($("<td>").append($("<span>").addClass("token-pill").text(route.intake_token_prefix || "-")));\n    row.append(\n        window.AppMaintenanceBadges.statusCell(\n            renderStatusBadge(route.enabled, "Enabled", "Disabled"),\n            route\n        )\n    );\n    row.append($("<td>").addClass("actions-cell").append(renderRouteActions(route)));\n    return row;\n}\n\nfunction renderRouteChannels(route) {\n    const wrapper = $("<div>").addClass("route-channels-list");\n    const mode = route.notification_channel_mode || "route_only";\n    const channels = asArray(route.channels);\n\n    if (mode === "service_policy") {\n        return wrapper.append(\n            $("<span>")\n                .addClass("route-channel-chip")\n                .text("Service policy")\n        );\n    }\n\n    if (mode === "service_policy_plus_route") {\n        wrapper.append(\n            $("<span>")\n                .addClass("route-channel-chip")\n                .text("Service policy")\n        );\n    }\n\n    channels.forEach(function (channel) {\n        wrapper.append(\n            $("<span>")\n                .addClass("route-channel-chip")\n                .text(channel.name || channel.id)\n        );\n    });\n\n    if (!wrapper.children().length) {\n        wrapper.append($("<span>").text("-"));\n    }\n\n    return wrapper;\n}\n\nfunction renderRouteActions(route) {\n    const items = [\n        {\n            label: "Edit",\n            icon: "fas fa-edit",\n            required: "write",\n            denyMessage: "Team manager or group editor/admin role is required to edit this route.",\n            onClick: function () {\n                editRoute(route.id);\n            }\n        }\n    ];\n\n    if (!["sentry", "aws_sns"].includes(route.source)) {\n        items.push({\n            label: "Regenerate token",\n            icon: "fas fa-sync-alt",\n            required: "write",\n            denyMessage: "Team manager or group editor/admin role is required to regenerate route tokens.",\n            onClick: function () {\n                regenerateRouteToken(route.id);\n            }\n        });\n    }\n\n    items.push(\n        {\n            label: "Service rules",\n            icon: "fas fa-project-diagram",\n            required: "write",\n            denyMessage: "Team manager or group editor/admin role is required to manage service rules.",\n            onClick: function () {\n                openRouteServiceRules(route.id);\n            }\n        },\n        {\n            label: route.enabled ? "Disable" : "Enable",\n            icon: route.enabled ? "fas fa-pause" : "fas fa-play",\n            required: "write",\n            danger: route.enabled,\n            denyMessage: "Team manager or group editor/admin role is required to enable or disable this route.",\n            onClick: function () {\n                if (route.enabled) {\n                    disableRoute(route);\n                } else {\n                    enableRoute(route);\n                }\n            }\n        },\n        {\n            label: "Delete",\n            icon: "fas fa-trash",\n            required: "delete",\n            danger: true,\n            denyMessage: "Delete permission is required to delete this route.",\n            onClick: function () {\n                deleteRoute(route);\n            }\n        }\n    );\n\n    return makeActionMenu({\n        object: route,\n        items: items\n    });\n}\nfunction getSelectedRouteForServiceRules() {\n    if (!selectedRouteForServiceRules) {\n        return null;\n    }\n\n    return routesCache.find(function (route) {\n        return Number(route.id) === Number(selectedRouteForServiceRules);\n    }) || null;\n}\n\nfunction openRouteServiceRules(routeId) {\n    const route = routesCache.find(function (item) {\n        return Number(item.id) === Number(routeId);\n    });\n\n    if (!route) {\n        return;\n    }\n\n    if (!canWriteObject(route)) {\n        showAppError("You do not have permission to manage service rules for this route.");\n        return;\n    }\n\n    selectedRouteForServiceRules = route.id;\n\n    $("#route-service-rules-title").text("Service rules / " + (route.name || ("Route #" + route.id)));\n    $("#service-rule-team").val(route.team_id);\n    $("#service-rule-route").val(route.id);\n\n    resetServiceRuleForm();\n\n    loadServiceRuleServices(route.team_id, function () {\n        loadServiceRuleMatcherPresets(route.team_id, null, function () {\n            refreshRouteServiceRules();\n            openAppModal("#route-service-rules-modal");\n        });\n    });\n}\n\nfunction loadServiceRuleServices(teamId, callback) {\n    const select = $("#service-rule-service");\n\n    select.empty();\n    select.append($("<option>").val("").text("Select service"));\n\n    if (!teamId) {\n        if (typeof callback === "function") {\n            callback();\n        }\n        return;\n    }\n\n    apiGet("/api/services?team_id=" + encodeURIComponent(teamId), function (services) {\n        select.empty();\n        select.append($("<option>").val("").text("Select service"));\n\n        asArray(services).forEach(function (service) {\n            if (!service.enabled) {\n                return;\n            }\n\n            select.append(\n                $("<option>")\n                    .val(String(service.id))\n                    .text(service.name + " (" + service.slug + ")")\n            );\n        });\n\n        if (typeof callback === "function") {\n            callback();\n        }\n    });\n}\n\nfunction loadServiceRuleMatcherPresets(teamId, selectedPresetId, callback) {\n    routeServiceRuleMatcherPresetsCache = [];\n    fillMatcherPresetSelect("#service-rule-matcher-preset", [], null);\n    updateServiceRuleMatcherPresetHint();\n\n    loadMatcherPresetsForTeam(teamId, function (presets) {\n        routeServiceRuleMatcherPresetsCache = presets;\n        fillMatcherPresetSelect("#service-rule-matcher-preset", presets, selectedPresetId);\n        updateServiceRuleMatcherPresetHint();\n\n        if (typeof callback === "function") {\n            callback();\n        }\n    });\n}\n\nfunction refreshRouteServiceRules() {\n    const route = getSelectedRouteForServiceRules();\n\n    if (!route) {\n        routeServiceRulesCache = [];\n        renderRouteServiceRulesTable();\n        return;\n    }\n\n    apiGet(\n        "/api/services/match-rules?route_id=" + encodeURIComponent(route.id),\n        function (rules) {\n            routeServiceRulesCache = asArray(rules);\n            renderRouteServiceRulesTable();\n        }\n    );\n}\n\nfunction renderRouteServiceRulesTable() {\n    const tbody = $("#route-service-rules-table");\n\n    tbody.empty();\n\n    if (!routeServiceRulesCache.length) {\n        tbody.append(\n            $("<tr>").append(\n                $("<td>")\n                    .attr("colspan", "6")\n                    .addClass("empty-cell")\n                    .text("No service rules")\n            )\n        );\n        return;\n    }\n\n    routeServiceRulesCache.forEach(function (rule) {\n        tbody.append(renderRouteServiceRuleRow(rule));\n    });\n}\n\nfunction renderRouteServiceRuleRow(rule) {\n    const row = $("<tr>");\n\n    row.append($("<td>").text(rule.position || 0));\n\n    row.append(\n        $("<td>")\n            .addClass("table-cell-truncate")\n            .attr("title", rule.name || "-")\n            .text(rule.name || "-")\n    );\n\n    row.append(\n        $("<td>")\n            .addClass("table-cell-truncate")\n            .attr("title", rule.service_name || rule.service_slug || "-")\n            .text(rule.service_name || rule.service_slug || "-")\n    );\n\n    const matchersCell = $("<td>");\n    const matcherPreset = rule.matcher_preset;\n\n    if (matcherPreset) {\n        matchersCell.append(\n            $("<div>")\n                .addClass("row-subtitle")\n                .text("Preset: " + formatMatcherPresetOption(matcherPreset))\n        );\n    }\n\n    matchersCell.append(\n        $("<code>")\n            .addClass("inline-code")\n            .text(JSON.stringify(rule.matchers || {}))\n    );\n\n    row.append(matchersCell);\n\n    row.append(\n        $("<td>").append(\n            renderStatusBadge(rule.enabled, "Enabled", "Disabled")\n        )\n    );\n\n    row.append(\n        $("<td>")\n            .addClass("actions-cell")\n            .append(\n                makeActionMenu({\n                    object: getSelectedRouteForServiceRules(),\n                    items: [\n                        {\n                            label: "Edit",\n                            icon: "fas fa-edit",\n                            required: "write",\n                            onClick: function () {\n                                editRouteServiceRule(rule);\n                            }\n                        },\n                        {\n                            label: "Delete",\n                            icon: "fas fa-trash",\n                            required: "write",\n                            danger: true,\n                            onClick: function () {\n                                deleteRouteServiceRule(rule);\n                            }\n                        }\n                    ]\n                })\n            )\n    );\n\n    return row;\n}\n\nfunction resetServiceRuleForm() {\n    $("#service-rule-id").val("");\n    $("#service-rule-name").val("");\n    $("#service-rule-service").val("");\n    $("#service-rule-position").val("0");\n    $("#service-rule-enabled").prop("checked", true);\n\n    fillMatcherPresetSelect("#service-rule-matcher-preset", routeServiceRuleMatcherPresetsCache, null);\n    updateServiceRuleMatcherPresetHint();\n\n    setMatcherEditorValue("#service-rule-matchers", {});\n}\n\nfunction editRouteServiceRule(rule) {\n    $("#service-rule-id").val(rule.id);\n    $("#service-rule-name").val(rule.name || "");\n    $("#service-rule-service").val(rule.service_id || "");\n    $("#service-rule-position").val(rule.position || 0);\n    $("#service-rule-enabled").prop("checked", rule.enabled !== false);\n\n    const matcherPresetId = rule.matcher_preset_id || (rule.matcher_preset ? rule.matcher_preset.id : null);\n\n    fillMatcherPresetSelect("#service-rule-matcher-preset", routeServiceRuleMatcherPresetsCache, matcherPresetId);\n    updateServiceRuleMatcherPresetHint();\n\n    setMatcherEditorValue("#service-rule-matchers", rule.matchers || {});\n}\n\nfunction collectRouteServiceRulePayload() {\n    return {\n        team_id: Number($("#service-rule-team").val()),\n        route_id: $("#service-rule-route").val() ? Number($("#service-rule-route").val()) : null,\n        service_id: Number($("#service-rule-service").val()),\n        position: Number($("#service-rule-position").val() || 0),\n        name: $("#service-rule-name").val(),\n        description: null,\n        matcher_preset_id: $("#service-rule-matcher-preset").val() ? Number($("#service-rule-matcher-preset").val()) : null,\n        matchers: getMatcherEditorValue("#service-rule-matchers", {}),\n        enabled: $("#service-rule-enabled").is(":checked"),\n    };\n}\n\nfunction saveRouteServiceRule() {\n    const id = $("#service-rule-id").val();\n    const payload = collectRouteServiceRulePayload();\n\n    if (!payload.service_id) {\n        showAppError("Service is required.");\n        return;\n    }\n\n    if (!payload.matcher_preset_id && !Object.keys(payload.matchers || {}).length) {\n        showAppError("Select a matcher preset or define additional matchers.");\n        return;\n    }\n\n    if (id) {\n        apiPut("/api/services/match-rules/" + id, payload, function () {\n            resetServiceRuleForm();\n            refreshRouteServiceRules();\n        });\n        return;\n    }\n\n    apiPost(\n        "/api/services/" + payload.service_id + "/match-rules",\n        payload,\n        function () {\n            resetServiceRuleForm();\n            refreshRouteServiceRules();\n        }\n    );\n}\n\nfunction deleteRouteServiceRule(rule) {\n    showAppConfirm({\n        title: "Delete this service rule?",\n        message: "Delete service rule \\"" + (rule.name || ("#" + rule.id)) + "\\"?",\n        confirmText: "Delete",\n        confirmClass: "btn-danger",\n    }).done(function () {\n        apiDelete("/api/services/match-rules/" + rule.id, function () {\n            refreshRouteServiceRules();\n        });\n    });\n}\nfunction routeDetailsItem(label, value) {\n    return $("<div>")\n        .addClass("details-item")\n        .append($("<div>").addClass("details-label").text(label))\n        .append($("<div>").addClass("details-value").text(value || "-"));\n}\n\nfunction routeDetailsCode(label, value) {\n    return $("<div>")\n        .addClass("details-item")\n        .append($("<div>").addClass("details-label").text(label))\n        .append($("<pre>").addClass("details-code").text(JSON.stringify(value || {}, null, 2)));\n}\n\nfunction renderRouteDetails(route) {\n    selectedRouteDetailsId = route.id;\n\n    $("#route-details-subtitle").text(getRouteTeamLabel(route) + " / " + (route.enabled ? "Enabled" : "Disabled"));\n\n    const body = $("#route-details-body");\n    body.empty();\n    body.append(\n        $("<div>")\n            .addClass("details-list")\n            .append(routeDetailsItem("Name", route.name))\n            .append(routeDetailsItem("Team", getRouteTeamLabel(route)))\n            .append(routeDetailsItem("Source", route.source))\n            .append(\n                route.source === "sentry"\n                    ? routeDetailsItem("Sentry webhook URL", getSentryWebhookUrl(route))\n                    : $()\n            )\n            .append(\n                route.source === "sentry"\n                    ? routeDetailsItem(\n                        "Sentry secret",\n                        getRouteSentryConfig(route).has_webhook_secret\n                            ? "Configured"\n                            : "Not configured"\n                    )\n                    : $()\n            )\n            .append(\n                route.source === "sentry"\n                    ? routeDetailsItem(\n                        "Sentry base URL",\n                        getRouteSentryConfig(route).base_url || "-"\n                    )\n                    : $()\n            )\n            .append(\n                route.source === "sentry"\n                    ? routeDetailsItem(\n                        "Sentry organization",\n                        getRouteSentryConfig(route).organization_slug || "-"\n                    )\n                    : $()\n            )\n            .append(\n                routeDetailsItem(\n                    route.source === "aws_sns"\n                        ? "SNS webhook URL"\n                        : "Webhook URL",\n                    getRouteIntakeUrl(route)\n                )\n            )\n            .append(routeDetailsItem("Maintenance", window.AppMaintenanceBadges.text(route, "-")))\n            .append(routeDetailsItem("Escalation", getRouteEscalationLabel(route)))\n            .append(routeDetailsItem("Team escalation", getRouteTeamEscalationLabel(route)))\n            .append(routeDetailsItem("Notification channel source", getRouteNotificationModeLabel(route)))\n            .append(routeDetailsItem("Channels", asArray(route.channels).map(function (channel) {\n                return channel.name;\n            }).join(", ") || "-"))\n            .append(\n                !["sentry", "aws_sns"].includes(route.source)\n                    ? routeDetailsItem(\n                        "Token prefix",\n                        route.intake_token_prefix\n                    )\n                    : $()\n            )\n            .append(routeDetailsItem("Status", route.enabled ? "Enabled" : "Disabled"))\n            .append(\n                routeDetailsItem(\n                    "Matcher preset",\n                    route.matcher_preset ? formatMatcherPresetOption(route.matcher_preset) : "No preset"\n                )\n            )\n            .append(routeDetailsCode("Additional matchers", route.matchers || {}))\n            .append(routeDetailsCode("Group by", asArray(route.group_by)))\n            .append(routeDetailsItem("Service", route.service_name || route.service_slug || "-"))\n    );\n\n    if (route.source === "aws_sns") {\n        const awsSnsConfig = getRouteAwsSnsConfig(route);\n\n        body.find(".details-list").append(\n            routeDetailsItem(\n                "SNS Topic ARN",\n                awsSnsConfig.topic_arn\n            )\n        );\n    }\n\n    const actions = $("<div>").addClass("details-actions");\n    appendIconActionIfAllowed(actions, route, {\n        required: "write",\n        icon: "fas fa-edit",\n        label: "Edit route",\n        onClick: function () {\n            editRoute(route.id);\n        },\n    });\n    if (!["sentry", "aws_sns"].includes(route.source)) {\n        appendIconActionIfAllowed(actions, route, {\n            required: "write",\n            icon: "fas fa-sync-alt",\n            label: "Regenerate route token",\n            onClick: function () {\n                regenerateRouteToken(route.id);\n            },\n        });\n    }\n    appendIconActionIfAllowed(actions, route, {\n        required: "write",\n        icon: route.enabled ? "fas fa-pause" : "fas fa-play",\n        label: route.enabled ? "Disable route" : "Enable route",\n        className: route.enabled ? "btn-warning" : "btn-success",\n        onClick: function () {\n            if (route.enabled) {\n                disableRoute(route);\n            } else {\n                enableRoute(route);\n            }\n        },\n    });\n    appendIconActionIfAllowed(actions, route, {\n        required: "delete",\n        icon: "fas fa-trash-alt",\n        label: "Delete route",\n        className: "btn-danger",\n        onClick: function () {\n            deleteRoute(route);\n        },\n    });\n\n    if (actions.children().length) {\n        body.append(actions);\n    }\n}\n\nfunction renderRouteDetailsEmpty() {\n    selectedRouteDetailsId = null;\n    $("#route-details-subtitle").text("Select a route");\n    $("#route-details-body").html("<p class=\\"muted\\">Click a route name to inspect matchers, group by, channels and intake token prefix.</p>");\n}\n\nfunction restoreRouteDetails() {\n    if (!routesCache.length) {\n        renderRouteDetailsEmpty();\n        return;\n    }\n\n    if (selectedRouteDetailsId) {\n        const selected = routesCache.find(function (route) {\n            return Number(route.id) === Number(selectedRouteDetailsId);\n        });\n        if (selected) {\n            renderRouteDetails(selected);\n            return;\n        }\n    }\n\n    renderRouteDetails(routesCache[0]);\n}\nfunction getRouteSentryConfig(route) {\n    const integrationConfig = route && route.integration_config\n        ? route.integration_config\n        : {};\n\n    return integrationConfig.sentry || {};\n}\n\nfunction getSentryWebhookUrl(route) {\n    const sentryConfig = getRouteSentryConfig(route);\n    const path = sentryConfig.webhook_path;\n\n    if (!path) {\n        return "";\n    }\n\n    return window.location.origin + path;\n}\n\nfunction getRouteAwsSnsConfig(route) {\n    const integrationConfig = (\n        route && route.integration_config\n            ? route.integration_config\n            : {}\n    );\n\n    return integrationConfig.aws_sns || {};\n}\n\nfunction updateRouteSourceUi() {\n    const source = String(\n        $("#route-source").val() || ""\n    );\n\n    const isSentry = source === "sentry";\n    const isAwsSns = source === "aws_sns";\n    const isWebhook = source === "webhook";\n\n    $("#route-sentry-settings").toggleClass("is-hidden", !isSentry);\n    $("#route-webhook-compatibility-help").toggleClass(\n        "is-hidden",\n        !isWebhook\n    );\n\n    $("#route-aws-sns-settings").toggleClass("is-hidden", !isAwsSns);\n\n    $("#route-aws-sns-topic-arn").prop("required", isAwsSns);\n\n    const hasAwsSnsWebhook = Boolean(\n        String(\n            $("#route-aws-sns-webhook-url").val() || ""\n        ).trim()\n    );\n\n    $("#route-aws-sns-webhook-group").toggleClass(\n        "is-hidden",\n        !isAwsSns || !hasAwsSnsWebhook\n    );\n\n    if (!$("#route-id").val()) {\n        if (source === "sentry") {\n            $("#route-group-by").val(\n                \'["project_slug","issue_id"]\'\n            );\n        } else if (source === "grafana") {\n            $("#route-group-by").val(\n                \'["alertname","grafana_folder","instance"]\'\n            );\n        } else if (source === "rmon") {\n            $("#route-group-by").val(\n                \'["rmon_check_id","rmon_check_type"]\'\n            );\n        } else if (source === "aws_sns") {\n            $("#route-group-by").val(\n                \'["cloudwatch_alarm_arn"]\'\n            );\n        }\n    }\n}\n\nfunction collectRouteIntegrationConfig() {\n    const source = $("#route-source").val();\n\n    if (source === "aws_sns") {\n        return {\n            aws_sns: {\n                topic_arn: String(\n                    $("#route-aws-sns-topic-arn").val() || ""\n                ).trim()\n            }\n        };\n    }\n\n    if (source === "sentry") {\n        const secret = String(\n            $("#route-sentry-webhook-secret").val() || ""\n        ).trim();\n\n        const baseUrl = String(\n            $("#route-sentry-base-url").val() || ""\n        ).trim();\n\n        const organizationSlug = String(\n            $("#route-sentry-organization-slug").val() || ""\n        ).trim();\n\n        const sentry = {};\n\n        if (secret) {\n            sentry.webhook_secret = secret;\n        }\n\n        if (baseUrl) {\n            sentry.base_url = baseUrl;\n        }\n\n        if (organizationSlug) {\n            sentry.organization_slug = organizationSlug;\n        }\n\n        return {\n            sentry: sentry\n        };\n    }\n    return {};\n}\n\nfunction updateSentrySecretHelp(route) {\n    const sentryConfig = getRouteSentryConfig(route);\n\n    if (route && sentryConfig.has_webhook_secret) {\n        $("#route-sentry-secret-help").text(\n            "Sentry webhook secret is configured. Leave empty to keep the existing secret."\n        );\n        return;\n    }\n\n    if (route && route.id) {\n        $("#route-sentry-secret-help").text(\n            "Paste Sentry Client Secret here. Until it is configured, Sentry webhooks will be rejected."\n        );\n        return;\n    }\n\n    $("#route-sentry-secret-help").text(\n        "Create the route first to get the Sentry webhook URL. Then create a Sentry Internal Integration and paste its Client Secret here."\n    );\n}\n\nfunction getRouteGroupByValue() {\n    const value = parseJsonInput(\n        "#route-group-by",\n        []\n    );\n\n    if (!Array.isArray(value)) {\n        const message = "Route group by must be a JSON array, for example [\\"alertname\\", \\"instance\\"]. Use [] to disable cross-alert grouping.";\n\n        if (typeof showAppError === "function") {\n            showAppError(message);\n        } else {\n            alert(message);\n        }\n\n        throw new Error(message);\n    }\n\n    return value;\n}\n\nfunction collectRoutePayload() {\n    const mode = $("#route-escalation-mode").val() || "rotation";\n    const usePolicy = mode === "policy";\n\n    return {\n        team_id: Number($("#route-team").val()),\n        name: $("#route-name").val(),\n        source: $("#route-source").val(),\n        escalation_mode: mode,\n        rotation_id: (\n            !usePolicy && $("#route-rotation").val()\n                ? Number($("#route-rotation").val())\n                : null\n        ),\n\n        escalation_policy_id: (\n            usePolicy && $("#route-escalation-policy").val()\n                ? Number($("#route-escalation-policy").val())\n                : null\n        ),\n        channel_ids: ($("#route-channels").val() || []).map(Number),\n        notification_channel_mode: ($("#route-notification-channel-mode").val() || "route_only"),\n        service_id: $("#route-service").val() ? Number($("#route-service").val()) : null,\n        matcher_preset_id: $("#route-matcher-preset").val() ? Number($("#route-matcher-preset").val()) : null,\n        matchers: getMatcherEditorValue("#route-matchers", {}),\n        group_by: getRouteGroupByValue(),\n\n        integration_config: collectRouteIntegrationConfig(),\n\n        enabled: $("#route-enabled").is(":checked")\n    };\n}\n\nfunction saveRoute() {\n    const id = $("#route-id").val();\n    const existing = id ? routesCache.find(function (item) {\n        return Number(item.id) === Number(id);\n    }) : null;\n    if (existing && !canWriteObject(existing)) {\n        showAppError("Team manager or group editor/admin role is required to edit this route.");\n        return;\n    }\n\n    if (id) {\n        apiPut("/api/routes/" + id, collectRoutePayload(), function () {\n            closeAppModal("#route-form-modal");\n            resetRouteForm();\n            refreshRoutes();\n        });\n        return;\n    }\n\n    const payload = collectRoutePayload();\n\n    apiPost("/api/routes", payload, function (response) {\n        closeAppModal("#route-form-modal");\n        resetRouteForm();\n        refreshRoutes();\n\n        if (payload.source === "aws_sns") {\n            showAppSuccess(\n                "AWS SNS route created. Copy the webhook URL from route details."\n            );\n            return;\n        }\n\n        showRouteIntakeDetails(response);\n    });\n}\n\nfunction editRoute(id) {\n    const route = routesCache.find(function (item) {\n        return Number(item.id) === Number(id);\n    });\n    if (!route) {\n        return;\n    }\n    if (!canWriteObject(route)) {\n        showAppError("You do not have permission to edit this route.");\n        return;\n    }\n\n    $("#route-form-title").text("Edit route #" + id);\n    $("#route-id").val(route.id);\n    $("#route-team").val(route.team_id);\n    $("#route-name").val(route.name);\n    $("#route-source").val(route.source);\n    const integrationConfig = (\n        route.integration_config || {}\n    );\n\n    const awsSnsConfig = (\n        integrationConfig.aws_sns || {}\n    );\n\n    const sentryConfig = (\n        integrationConfig.sentry || {}\n    );\n\n    $("#route-aws-sns-topic-arn").val(\n        awsSnsConfig.topic_arn || ""\n    );\n\n    const awsSnsWebhookPath = (\n        awsSnsConfig.webhook_path || ""\n    );\n\n    if (route.source === "aws_sns" && awsSnsWebhookPath) {\n        $("#route-aws-sns-webhook-url").val(\n            window.location.origin + awsSnsWebhookPath\n        );\n\n        $("#route-aws-sns-webhook-group")\n            .removeClass("is-hidden");\n    } else {\n        $("#route-aws-sns-webhook-url").val("");\n\n        $("#route-aws-sns-webhook-group")\n            .addClass("is-hidden");\n    }\n\n    setMatcherEditorValue("#route-matchers", route.matchers || {});\n    $("#route-group-by").val(JSON.stringify(asArray(route.group_by), null, 2));\n    $("#route-enabled").prop("checked", !!route.enabled);\n    $("#route-sentry-webhook-secret").val("");\n    $("#route-sentry-base-url").val(sentryConfig.base_url || "");\n    $("#route-sentry-organization-slug").val(sentryConfig.organization_slug || "");\n\n    updateSentrySecretHelp(route);\n    updateRouteSourceUi();\n\n    loadRouteDependencies(function () {\n        const usePolicy = !!route.escalation_policy_id;\n\n        $("#route-escalation-mode").val(usePolicy ? "policy" : "rotation");\n        $("#route-rotation").val(route.rotation_id || "");\n        $("#route-escalation-policy").val(route.escalation_policy_id || "");\n\n        $("#route-channels").val(asArray(route.channels).map(function (channel) {\n            return String(channel.id);\n        }));\n\n        $("#route-service").val(route.service_id || "");\n        $("#route-notification-channel-mode").val(route.notification_channel_mode || "route_only");\n\n        updateRouteNotificationChannelModeUi();\n        updateRouteEscalationModeUi();\n        updateRouteSourceUi();\n    }, route.matcher_preset_id || (route.matcher_preset ? route.matcher_preset.id : null));\n\n    openAppModal("#route-form-modal");\n}\n\nfunction disableRoute(route) {\n    if (!canWriteObject(route)) {\n        showAppError("You do not have permission to disable this route.");\n        return;\n    }\n\n    const routeName = route.name || ("Route #" + route.id);\n    showAppConfirm({\n        title: "Disable this route?",\n        message: "Disable route \\"" + routeName + "\\"?\\n\\nThe route will stop accepting incoming alerts, but it will stay visible and can be enabled again.",\n        confirmText: "Disable",\n        confirmClass: "btn-warning",\n    }).done(function () {\n        apiPost("/api/routes/" + route.id + "/disable", {}, function () {\n            refreshRoutes();\n        });\n    });\n}\n\nfunction enableRoute(route) {\n    if (!canWriteObject(route)) {\n        showAppError("You do not have permission to enable this route.");\n        return;\n    }\n\n    apiPost("/api/routes/" + route.id + "/enable", {}, function () {\n        refreshRoutes();\n    });\n}\n\nfunction deleteRoute(route) {\n    if (!canDeleteObject(route)) {\n        showAppError("You do not have permission to delete this route.");\n        return;\n    }\n\n    const routeName = route.name || ("Route #" + route.id);\n    showAppConfirm({\n        title: "Delete this route?",\n        message: "Delete route \\"" + routeName + "\\"?\\n\\nThis will remove the route from active route lists and stop alert intake for this route. Historical alerts will be preserved.",\n        confirmText: "Delete",\n        confirmClass: "btn-danger",\n    }).done(function () {\n        apiDelete("/api/routes/" + route.id, function () {\n            if (Number(selectedRouteDetailsId) === Number(route.id)) {\n                selectedRouteDetailsId = null;\n                renderRouteDetailsEmpty();\n            }\n            refreshRoutes();\n        });\n    });\n}\n\nfunction resetRouteForm() {\n    $("#route-form-title").text("Create route");\n    $("#route-id").val("");\n    $("#route-name").val("");\n    $("#route-source").val("alertmanager");\n    setMatcherEditorValue("#route-matchers", {});\n    $("#route-group-by").val(\'["alertname","instance"]\');\n    $("#route-enabled").prop("checked", true);\n    $("#route-rotation").val("");\n    $("#route-channels").val([]);\n    $("#route-escalation-mode").val("rotation");\n    $("#route-escalation-policy").val("");\n    $("#route-service").val("");\n    fillMatcherPresetSelect("#route-matcher-preset", routeMatcherPresetsCache, null);\n    updateRouteMatcherPresetHint();\n    $("#route-aws-sns-topic-arn").val("");\n    $("#route-aws-sns-webhook-url").val("");\n    $("#route-aws-sns-webhook-group").addClass("is-hidden");\n    updateRouteEscalationModeUi();\n    $("#route-sentry-webhook-secret").val("");\n    $("#route-sentry-base-url").val("");\n    $("#route-sentry-organization-slug").val("");\n    $("#route-notification-channel-mode").val("route_only");\n    updateRouteNotificationChannelModeUi();\n    updateSentrySecretHelp(null);\n    updateRouteSourceUi();\n}\n\nfunction getRouteIntakePath(route) {\n    const source = String(route.source || "").toLowerCase();\n\n    if (source === "sentry") {\n        return "/api/integrations/sentry/" + route.id;\n    }\n\n    if (source === "aws_sns") {\n        const awsSnsConfig = getRouteAwsSnsConfig(route);\n\n        return (\n            awsSnsConfig.webhook_path\n            || ("/api/integrations/aws-sns/" + route.id)\n        );\n    }\n\n    if (source === "alertmanager") {\n        return "/api/integrations/alertmanager";\n    }\n\n    if (source === "heartbeat") {\n        return "/api/heartbeats/ping/<heartbeat-token>";\n    }\n\n    if (source === "zabbix") {\n        return "/api/integrations/zabbix";\n    }\n\n    if (source === "webhook") {\n        return "/api/integrations/webhook";\n    }\n\n    return "/api/integrations/" + source;\n}\n\nfunction getRouteIntakeUrl(route) {\n    return window.location.origin + getRouteIntakePath(route);\n}\n\nfunction buildRouteIntakeCurl(route, token) {\n    const url = getRouteIntakeUrl(route);\n    const source = String(route.source || "").toLowerCase();\n\n    if (source === "sentry") {\n        return [\n            "curl -X POST \'" + url + "\' \\\\",\n            "  -H \'Content-Type: application/json\' \\\\",\n            "  -H \'Sentry-Hook-Resource: event_alert\' \\\\",\n            "  -H \'Sentry-Hook-Signature: <generated-by-sentry>\' \\\\",\n            "  -d \'{\\"action\\":\\"triggered\\",\\"data\\":{}}\'"\n        ].join("\\n");\n    }\n\n    if (source === "heartbeat") {\n        return [\n            "curl -fsS -X POST \'" + url + "\' \\\\",\n            "  -H \'Content-Type: application/json\' \\\\",\n            "  -d \'{}\'"\n        ].join("\\n");\n    }\n\n    if (source === "webhook") {\n        return [\n            "# IncidentRelay generic webhook format",\n            "curl -X POST \'" + url + "\' \\\\",\n            "  -H \'Content-Type: application/json\' \\\\",\n            "  -H \'Authorization: Bearer " + (token || "<route-token>") + "\' \\\\",\n            "  -d \'{\\"title\\":\\"Example alert\\",\\"severity\\":\\"critical\\",\\"fingerprint\\":\\"example-1\\"}\'",\n            "",\n            "# PagerDuty Events API v2-compatible format",\n            "curl -X POST \'" + url + "\' \\\\",\n            "  -H \'Content-Type: application/json\' \\\\",\n            "  -d \'{\\"routing_key\\":\\"" + (token || "<route-token>") + "\\",\\"event_action\\":\\"trigger\\",\\"dedup_key\\":\\"example-1\\",\\"payload\\":{\\"summary\\":\\"Example alert\\",\\"source\\":\\"example-host\\",\\"severity\\":\\"critical\\"}}\'"\n        ].join("\\n");\n    }\n\n    return [\n        "curl -X POST \'" + url + "\' \\\\",\n        "  -H \'Content-Type: application/json\' \\\\",\n        "  -H \'Authorization: Bearer " + (token || "<route-token>") + "\' \\\\",\n        "  -d \'{}\'"\n    ].join("\\n");\n}\n\nfunction showRouteIntakeDetails(route) {\n    const source = String(route.source || "").toLowerCase();\n    const token = route.intake_token || "";\n    const isSentry = source === "sentry";\n    const isHeartbeat = source === "heartbeat";\n    const isWebhook = source === "webhook";\n    const url = getRouteIntakeUrl(route);\n\n    $("#route-intake-title").text(\n        isSentry\n            ? "Sentry webhook URL"\n            : (isHeartbeat ? "Heartbeat ping URL pattern" : "Route intake details")\n    );\n\n    $("#route-intake-subtitle").text(\n        isSentry\n            ? "Copy this URL to Sentry Internal Integration. Then paste Sentry Client Secret into this route settings."\n            : (isHeartbeat\n                ? "Heartbeat pings use a per-heartbeat token in the URL. Create or open a heartbeat to copy its real ping URL."\n                : (isWebhook\n                    ? "Use this token as Authorization: Bearer for generic payloads or as routing_key for PagerDuty Events API v2-compatible payloads."\n                    : "Copy this token now. It may not be shown again."))\n    );\n\n    $("#route-intake-url").val(url);\n    $("#route-intake-token").val(token);\n    $("#route-intake-curl").val(buildRouteIntakeCurl(route, token));\n\n    $("#route-intake-token-group").toggleClass("is-hidden", isSentry || isHeartbeat);\n    $("#copy-route-intake-token").toggleClass("is-hidden", isSentry || isHeartbeat);\n\n    $("#route-intake-url-help").text(\n        isSentry\n            ? "Use this URL as Webhook URL in Sentry Internal Integration."\n            : (isHeartbeat\n                ? "Replace <heartbeat-token> with the token from a heartbeat. Do not send Authorization: Bearer for heartbeat pings."\n                : (isWebhook\n                    ? "Generic payloads use Authorization: Bearer. PagerDuty Events API v2-compatible payloads put the same token in routing_key."\n                    : "Send alerts to this URL and pass the token as Authorization: Bearer."))\n    );\n\n    openAppModal("#route-token-box");\n}\n\nfunction closeRouteTokenModal() {\n    closeAppModal("#route-token-box");\n    $("#route-intake-url").val("");\n    $("#route-intake-token").val("");\n    $("#route-intake-curl").val("");\n}\n\nfunction regenerateRouteToken(routeId) {\n    const route = routesCache.find(function (item) {\n        return Number(item.id) === Number(routeId);\n    });\n    if (route && !canWriteObject(route)) {\n        showAppError("You do not have permission to regenerate this route token.");\n        return;\n    }\n\n    showAppConfirm({\n        title: "Regenerate route intake token?",\n        message: "Regenerate route intake token? Existing token will stop working.",\n        confirmText: "Regenerate",\n        confirmClass: "btn-warning",\n    }).done(function () {\n        apiPost("/api/routes/" + routeId + "/intake-token", {}, function (response) {\n            showRouteIntakeDetails(response);\n            refreshRoutes();\n        });\n    });\n}\n\nfunction openCreateRouteModal() {\n    resetRouteForm();\n    $("#route-form-title").text("Create route");\n    loadRouteDependencies();\n    openAppModal("#route-form-modal");\n}\nfunction copyTextFromField(selector) {\n    const value = $(selector).val() || "";\n\n    if (!value) {\n        return;\n    }\n\n    if (navigator.clipboard && navigator.clipboard.writeText) {\n        navigator.clipboard.writeText(value);\n        return;\n    }\n\n    const field = $(selector);\n    field.trigger("select");\n    document.execCommand("copy");\n}\nfunction copyRouteIntakeToken() {\n    copyTextFromField("#route-intake-token");\n}\n\nfunction copyRouteIntakeUrl() {\n    copyTextFromField("#route-intake-url");\n}\n\nfunction copyRouteIntakeCurl() {\n    copyTextFromField("#route-intake-curl");\n}\n\nfunction initRoutesTableSorting() {\n    bindSortableTableHeaders(\n        "#routes-table-view",\n        routesSortState,\n        routesSortColumns,\n        renderRoutesTable\n    );\n}\n\nfunction getFilteredRoutes() {\n    const query = String($("#routes-search").val() || "").trim().toLowerCase();\n    const source = String($("#routes-source-filter").val() || "");\n    const status = String($("#routes-status-filter").val() || "");\n\n    const filtered = routesCache.filter(function (route) {\n        if (source && route.source !== source) {\n            return false;\n        }\n        if (status === "enabled" && !route.enabled) {\n            return false;\n        }\n        if (status === "disabled" && route.enabled) {\n            return false;\n        }\n        if (!query) {\n            return true;\n        }\n        return getRouteSearchText(route).indexOf(query) !== -1;\n    });\n\n    return sortTableData(filtered, routesSortState, routesSortColumns);\n}\n\n$(document).on("change", "#route-team", function () {\n    loadRouteDependencies();\n});\n$(document).on("change", "#route-source", updateRouteSourceUi);\n$(document).on("input", "#routes-search", renderRoutesTable);\n$(document).on("change", "#routes-source-filter, #routes-status-filter", renderRoutesTable);\n$(document).on("click", "#open-route-create-modal", openCreateRouteModal);\n$(document).on("click", "#save-route", saveRoute);\n$(document).on("click", "#reset-route-form", resetRouteForm);\n$(document).on("click", "#reload-routes", refreshRoutes);\n$(document).on("click", "#close-route-form-modal", function () {\n    closeAppModal("#route-form-modal");\n});\n$(document).on("click", "#close-route-token-modal, #close-route-token-modal-footer", closeRouteTokenModal);\n$(document).on("click", "#copy-route-intake-token", copyRouteIntakeToken);\n$(document).on("click", "#route-form-modal", function (event) {\n    if (event.target === this) {\n        closeAppModal("#route-form-modal");\n    }\n});\n$(document).on("click", "#route-token-box", function (event) {\n    if (event.target === this) {\n        closeRouteTokenModal();\n    }\n});\n$(document).on("keydown", function (event) {\n    if (event.key !== "Escape") {\n        return;\n    }\n    if ($("#route-token-box").hasClass("is-open")) {\n        closeRouteTokenModal();\n        return;\n    }\n    if ($("#route-form-modal").hasClass("is-open")) {\n        closeAppModal("#route-form-modal");\n    }\n});\n$(document).on("change", "#route-rotation", function () {\n    if ($(this).val()) {\n        $("#route-escalation-mode").val("rotation");\n        $("#route-escalation-policy").val("");\n    }\n\n    updateRouteEscalationModeUi();\n});\n$(document).on("change", "#route-escalation-policy", function () {\n    if ($(this).val()) {\n        $("#route-escalation-mode").val("policy");\n    }\n\n    updateRouteEscalationModeUi();\n});\nfunction updateRouteEscalationModeUi() {\n    const mode = $("#route-escalation-mode").val() || "rotation";\n    const usePolicy = mode === "policy";\n\n    $("#route-rotation")\n        .prop("disabled", usePolicy)\n        .closest(".form-group")\n        .toggleClass("is-muted", usePolicy);\n\n    $("#route-escalation-policy")\n        .prop("disabled", !usePolicy);\n\n    $("#route-policy-group").toggleClass("is-hidden", !usePolicy);\n}\n$(document).on("change", "#route-escalation-mode", updateRouteEscalationModeUi);\n$(document).on("click", "#save-service-rule", saveRouteServiceRule);\n\n$(document).on("click", "#reset-service-rule-form", resetServiceRuleForm);\n\n\n$(document).on("click", "#close-route-service-rules-modal", function () {\n    closeAppModal("#route-service-rules-modal");\n});\n\n$(document).on("click", "#route-service-rules-modal", function (event) {\n    if (event.target === this) {\n        closeAppModal("#route-service-rules-modal");\n    }\n});\n$(document).on("click", "#copy-route-intake-url", copyRouteIntakeUrl);\n$(document).on("click", "#copy-route-intake-curl", copyRouteIntakeCurl);\n$(document).on("change", "#route-notification-channel-mode", updateRouteNotificationChannelModeUi);\n$(document).on("change", "#route-matcher-preset", updateRouteMatcherPresetHint);\n$(document).on("change", "#service-rule-matcher-preset", updateServiceRuleMatcherPresetHint);\n', 'plain'), ('f48742fafbc644224b040949a29a8ca99cca9a80295f53f85d6d9622a44ae569', 'dc2f3c6f5791ff34dbf77af0cc40d9e2331a20cc4c49339d1927e90e12c3e8af', 'let routesCache = [];\nlet selectedRouteDetailsId = null;\nlet routesSortState = createTableSortState("id", "desc");\nconst routesSortColumns = {\n    name: {path: "name", type: "text", defaultDirection: "asc"},\n    team: {\n        value: function (route) {\n            return getRouteTeamLabel(route);\n        },\n        type: "text",\n        defaultDirection: "asc"\n    },\n    source: {path: "source", type: "text", defaultDirection: "asc"},\n    rotation: {\n        value: function (route) {\n            return route.escalation_policy_name || route.rotation_name || "";\n        },\n        type: "text",\n        defaultDirection: "asc"\n    },\n    channels: {\n        value: function (route) {\n            return asArray(route.channels).map(function (channel) {\n                return channel.name || "";\n            }).join(", ");\n        },\n        type: "text",\n        defaultDirection: "asc",\n    },\n    enabled: {path: "enabled", type: "boolean", defaultDirection: "desc"},\n};\nlet selectedRouteForServiceRules = null;\nlet routeServiceRulesCache = [];\nfunction getRouteTeamLabel(route) {\n    return route.team_name || route.team_slug || "-";\n}\nfunction getRouteEscalationLabel(route) {\n    if (route.escalation_policy_name) {\n        return i18n.t("routes.escalation.policy", {\n            name: route.escalation_policy_name,\n        });\n    }\n\n    if (route.rotation_name) {\n        return i18n.t("routes.escalation.rotation", {\n            name: route.rotation_name,\n        });\n    }\n\n    return "-";\n}\nlet routeMatcherPresetsCache = [];\nlet routeServiceRuleMatcherPresetsCache = [];\n\nfunction updateRouteMatcherPresetHint() {\n    const preset = findMatcherPresetById(routeMatcherPresetsCache, $("#route-matcher-preset").val());\n    $("#route-matcher-preset-hint").text(matcherPresetAndLocalHint(preset));\n}\n\n\nfunction updateServiceRuleMatcherPresetHint() {\n    const preset = findMatcherPresetById(routeServiceRuleMatcherPresetsCache, $("#service-rule-matcher-preset").val());\n    $("#service-rule-matcher-preset-hint").text(matcherPresetAndLocalHint(preset));\n}\n\nfunction getRouteNotificationModeLabel(route) {\n    const mode = route.notification_channel_mode || "route_only";\n\n    if (mode === "service_policy") {\n        return i18n.t("routes.notification.service_policy");\n    }\n\n    if (mode === "service_policy_plus_route") {\n        return i18n.t("routes.notification.combined");\n    }\n\n    return i18n.t("routes.notification.route_only");\n}\n\nfunction updateRouteNotificationChannelModeUi() {\n    const mode = (\n        $("#route-notification-channel-mode").val()\n        || "route_only"\n    );\n    const routeChannelsDisabled = mode === "service_policy";\n\n    $("#route-channels")\n        .prop("disabled", routeChannelsDisabled)\n        .closest(".form-group")\n        .toggleClass("is-muted", routeChannelsDisabled);\n\n    let helpKey = "routes.notification.route_only_help";\n\n    if (mode === "service_policy") {\n        helpKey = "routes.notification.service_policy_help";\n    } else if (mode === "service_policy_plus_route") {\n        helpKey = "routes.notification.combined_help";\n    }\n\n    $("#route-notification-channel-mode-help").text(i18n.t(helpKey));\n}\n\nfunction getRouteTeamEscalationLabel(route) {\n    if (route.escalation_policy_name) {\n        return i18n.t("routes.escalation.ignored_policy");\n    }\n\n    if (route.team_escalation_enabled) {\n        return i18n.t("routes.escalation.after_reminders", {\n            count: route.team_escalation_after_reminders || 0,\n        });\n    }\n\n    return i18n.t("routes.status.disabled");\n}\nfunction initializeRouteMatcherEditors() {\n    enhanceMatcherEditor("#route-matchers", {\n        label: i18n.t("routes.form.additional_matchers_json"),\n        header: i18n.t("routes.form.additional_matchers"),\n        context: function () {\n            return {\n                scope: "route",\n                teamId: Number($("#route-team").val()) || null,\n                routeId: Number($("#route-id").val()) || null,\n                matcherPresetId: Number($("#route-matcher-preset").val()) || null,\n            };\n        },\n    });\n\n    enhanceMatcherEditor("#service-rule-matchers", {\n        label: i18n.t("routes.form.additional_matchers"),\n        context: function () {\n            const route = getSelectedRouteForServiceRules();\n\n            return {\n                scope: "service_rule",\n                teamId: route ? route.team_id : null,\n                routeId: route ? route.id : null,\n                serviceId: Number($("#service-rule-service").val()) || null,\n                matcherPresetId: Number($("#service-rule-matcher-preset").val()) || null,\n            };\n        },\n    });\n}\n\nfunction loadRoutes() {\n    initializeRouteMatcherEditors();\n    fillTeamSelect("#route-team", false, loadRouteDependencies);\n    initRoutesTableSorting();\n    refreshRoutes();\n}\n\nfunction loadRouteDependencies(callback, selectedMatcherPresetId) {\n    const teamId = $("#route-team").val();\n\n    const rotationSelect = $("#route-rotation");\n    const channelSelect = $("#route-channels");\n    const policySelect = $("#route-escalation-policy");\n    const serviceSelect = $("#route-service");\n    const matcherPresetSelect = $("#route-matcher-preset");\n\n    rotationSelect.empty().append($("<option>").val("").text(i18n.t("routes.form.no_rotation")));\n    channelSelect.empty();\n\n    if (policySelect.length) {\n        policySelect.empty().append($("<option>").val("").text(i18n.t("routes.form.no_policy")));\n    }\n\n    if (serviceSelect.length) {\n        serviceSelect.empty().append($("<option>").val("").text(i18n.t("routes.form.no_default_service")));\n    }\n\n    if (!teamId) {\n        if (typeof callback === "function") {\n            callback();\n        }\n        return;\n    }\n\n    routeMatcherPresetsCache = [];\n    fillMatcherPresetSelect(matcherPresetSelect, [], selectedMatcherPresetId);\n    updateRouteMatcherPresetHint();\n\n    let rotationsLoaded = false;\n    let channelsLoaded = false;\n    let policiesLoaded = !policySelect.length;\n    let servicesLoaded = !serviceSelect.length;\n    let matcherPresetsLoaded = !matcherPresetSelect.length;\n\n    function finishWhenReady() {\n        if (!rotationsLoaded || !channelsLoaded || !policiesLoaded || !servicesLoaded || !matcherPresetsLoaded) {\n            return;\n        }\n\n        if (typeof callback === "function") {\n            callback();\n        }\n    }\n\n    apiGet("/api/rotations?team_id=" + encodeURIComponent(teamId), function (rotations) {\n        rotationSelect.empty().append($("<option>").val("").text(i18n.t("routes.form.no_rotation")));\n\n        asArray(rotations).forEach(function (rotation) {\n            if (!rotation.enabled) {\n                return;\n            }\n\n            rotationSelect.append(\n                $("<option>")\n                    .val(String(rotation.id))\n                    .text(rotation.name)\n            );\n        });\n\n        rotationsLoaded = true;\n        finishWhenReady();\n    });\n\n    apiGet("/api/channels?team_id=" + encodeURIComponent(teamId), function (channels) {\n        channelSelect.empty();\n\n        asArray(channels).forEach(function (channel) {\n            if (!channel.enabled) {\n                return;\n            }\n\n            channelSelect.append(\n                $("<option>")\n                    .val(String(channel.id))\n                    .text(channel.name + " (" + channel.channel_type + ")")\n            );\n        });\n\n        channelsLoaded = true;\n        finishWhenReady();\n    });\n\n    if (policySelect.length) {\n        apiGet("/api/escalation-policies?team_id=" + encodeURIComponent(teamId), function (policies) {\n            policySelect.empty().append($("<option>").val("").text(i18n.t("routes.form.no_policy")));\n\n            asArray(policies).forEach(function (policy) {\n                if (!policy.enabled) {\n                    return;\n                }\n\n                policySelect.append(\n                    $("<option>")\n                        .val(String(policy.id))\n                        .text(policy.name)\n                );\n            });\n\n            policiesLoaded = true;\n            finishWhenReady();\n        });\n    }\n\n    if (serviceSelect.length) {\n        apiGet("/api/services?team_id=" + encodeURIComponent(teamId), function (services) {\n            serviceSelect.empty().append($("<option>").val("").text(i18n.t("routes.form.no_default_service")));\n\n            asArray(services).forEach(function (service) {\n                if (!service.enabled) {\n                    return;\n                }\n\n                serviceSelect.append(\n                    $("<option>")\n                        .val(String(service.id))\n                        .text(service.name + " (" + service.slug + ")")\n                );\n            });\n\n            servicesLoaded = true;\n            finishWhenReady();\n        });\n    }\n    if (matcherPresetSelect.length) {\n        loadMatcherPresetsForTeam(teamId, function (presets) {\n            routeMatcherPresetsCache = presets;\n            fillMatcherPresetSelect(matcherPresetSelect, presets, selectedMatcherPresetId);\n            updateRouteMatcherPresetHint();\n            matcherPresetsLoaded = true;\n            finishWhenReady();\n        });\n    }\n}\n\nfunction refreshRoutes() {\n    apiGet("/api/routes" + selectedTeamQuery(), function (routes) {\n        routesCache = asArray(routes);\n        renderRoutesSummary(routesCache);\n        fillRouteSourceFilter(routesCache);\n        renderRoutesTable();\n        restoreRouteDetails();\n    });\n}\n\nfunction renderRoutesSummary(routes) {\n    routes = asArray(routes);\n\n    const enabled = routes.filter(function (route) {\n        return !!route.enabled;\n    }).length;\n\n    const withEscalation = routes.filter(function (route) {\n        return !!route.rotation_id || !!route.rotation_name ||\n            !!route.escalation_policy_id || !!route.escalation_policy_name;\n    }).length;\n\n    $("#routes-summary-total").text(routes.length);\n    $("#routes-summary-enabled").text(enabled);\n    $("#routes-summary-disabled").text(routes.length - enabled);\n    $("#routes-summary-rotation").text(withEscalation);\n}\n\nfunction fillRouteSourceFilter(routes) {\n    const filter = $("#routes-source-filter");\n    const selected = filter.val();\n    const sources = {\n        alertmanager: true,\n        aws_sns: true,\n        grafana: true,\n        rmon: true,\n        heartbeat: true,\n        zabbix: true,\n        webhook: true,\n        sentry: true,\n        librenms: true\n    };\n\n    asArray(routes).forEach(function (route) {\n        if (route.source) {\n            sources[route.source] = true;\n        }\n    });\n\n    filter.empty();\n    filter.append($("<option>").val("").text(i18n.t("routes.filters.all_sources")));\n    Object.keys(sources).sort().forEach(function (source) {\n        filter.append($("<option>").val(source).text(source));\n    });\n    if (selected && sources[selected]) {\n        filter.val(selected);\n    }\n}\n\nfunction getRouteSearchText(route) {\n    const channels = asArray(route.channels).map(function (channel) {\n        return channel.name + " " + channel.channel_type;\n    }).join(" ");\n\n    return [\n        route.id,\n        route.team_name,\n        route.team_slug,\n        route.name,\n        route.source,\n        route.rotation_name,\n        route.escalation_policy_name,\n        route.intake_token_prefix,\n        route.enabled ? i18n.t("routes.status.enabled") : i18n.t("routes.status.disabled"),\n        channels,\n        route.service_name,\n        route.service_slug,\n        route.notification_channel_mode,\n        route.matcher_preset ? route.matcher_preset.name : "",\n        getRouteNotificationModeLabel(route),\n    ].join(" ").toLowerCase();\n}\n\nfunction renderRoutesCounter(filteredRoutes, allRoutes) {\n    filteredRoutes = asArray(filteredRoutes);\n    allRoutes = asArray(allRoutes);\n    $("#routes-filtered-count").text(filteredRoutes.length);\n    $("#routes-total-count").text(allRoutes.length);\n}\n\nfunction renderRoutesTable() {\n    const tbody = $("#routes-table");\n    const routes = getFilteredRoutes();\n    tbody.empty();\n    renderRoutesCounter(routes, routesCache);\n\n    if (!routes.length) {\n        tbody.append(\n            $("<tr>").append(\n                $("<td>")\n                    .attr("colspan", "8")\n                    .addClass("empty-cell")\n                    .text(i18n.t("routes.table.empty"))\n            )\n        );\n        return;\n    }\n\n    routes.forEach(function (route) {\n        tbody.append(renderRouteRow(route));\n    });\n}\n\nfunction renderRouteRow(route) {\n    const row = $("<tr>");\n\n    row.append(\n        $("<td>")\n            .append(\n                $("<button>")\n                    .attr("type", "button")\n                    .addClass("name-button")\n                    .text(route.name || "-")\n                    .on("click", function () {\n                        renderRouteDetails(route);\n                    })\n            )\n            .append(\n                $("<div>")\n                    .addClass("row-subtitle")\n                    .text(i18n.t("routes.row.number", {id: route.id}))\n            )\n    );\n    row.append($("<td>").append($("<span>").addClass("route-pill").text(getRouteTeamLabel(route))));\n    row.append($("<td>").text(route.source || "-"));\n    row.append($("<td>").text(getRouteEscalationLabel(route)));\n    row.append($("<td>").append(renderRouteChannels(route)));\n    row.append($("<td>").append($("<span>").addClass("token-pill").text(route.intake_token_prefix || "-")));\n    row.append(\n        window.AppMaintenanceBadges.statusCell(\n            renderStatusBadge(\n                route.enabled,\n                i18n.t("routes.status.enabled"),\n                i18n.t("routes.status.disabled")\n            ),\n            route\n        )\n    );\n    row.append($("<td>").addClass("actions-cell").append(renderRouteActions(route)));\n    return row;\n}\n\nfunction renderRouteChannels(route) {\n    const wrapper = $("<div>").addClass("route-channels-list");\n    const mode = route.notification_channel_mode || "route_only";\n    const channels = asArray(route.channels);\n\n    if (mode === "service_policy") {\n        return wrapper.append(\n            $("<span>")\n                .addClass("route-channel-chip")\n                .text(i18n.t("routes.notification.service_policy"))\n        );\n    }\n\n    if (mode === "service_policy_plus_route") {\n        wrapper.append(\n            $("<span>")\n                .addClass("route-channel-chip")\n                .text(i18n.t("routes.notification.service_policy"))\n        );\n    }\n\n    channels.forEach(function (channel) {\n        wrapper.append(\n            $("<span>")\n                .addClass("route-channel-chip")\n                .text(channel.name || channel.id)\n        );\n    });\n\n    if (!wrapper.children().length) {\n        wrapper.append($("<span>").text("-"));\n    }\n\n    return wrapper;\n}\n\nfunction renderRouteActions(route) {\n    const items = [\n        {\n            label: i18n.t("routes.actions.edit"),\n            icon: "fas fa-edit",\n            required: "write",\n            denyMessage: i18n.t("routes.permissions.edit"),\n            onClick: function () {\n                editRoute(route.id);\n            }\n        }\n    ];\n\n    if (!["sentry", "aws_sns"].includes(route.source)) {\n        items.push({\n            label: i18n.t("routes.actions.regenerate_token"),\n            icon: "fas fa-sync-alt",\n            required: "write",\n            denyMessage: i18n.t("routes.permissions.regenerate"),\n            onClick: function () {\n                regenerateRouteToken(route.id);\n            }\n        });\n    }\n\n    items.push(\n        {\n            label: i18n.t("routes.actions.service_rules"),\n            icon: "fas fa-project-diagram",\n            required: "write",\n            denyMessage: i18n.t("routes.permissions.rules"),\n            onClick: function () {\n                openRouteServiceRules(route.id);\n            }\n        },\n        {\n            label: route.enabled\n                ? i18n.t("routes.actions.disable")\n                : i18n.t("routes.actions.enable"),\n            icon: route.enabled ? "fas fa-pause" : "fas fa-play",\n            required: "write",\n            danger: route.enabled,\n            denyMessage: i18n.t("routes.permissions.toggle"),\n            onClick: function () {\n                if (route.enabled) {\n                    disableRoute(route);\n                } else {\n                    enableRoute(route);\n                }\n            }\n        },\n        {\n            label: i18n.t("routes.actions.delete"),\n            icon: "fas fa-trash",\n            required: "delete",\n            danger: true,\n            denyMessage: i18n.t("routes.permissions.delete"),\n            onClick: function () {\n                deleteRoute(route);\n            }\n        }\n    );\n\n    return makeActionMenu({\n        object: route,\n        items: items\n    });\n}\nfunction getSelectedRouteForServiceRules() {\n    if (!selectedRouteForServiceRules) {\n        return null;\n    }\n\n    return routesCache.find(function (route) {\n        return Number(route.id) === Number(selectedRouteForServiceRules);\n    }) || null;\n}\n\nfunction openRouteServiceRules(routeId) {\n    const route = routesCache.find(function (item) {\n        return Number(item.id) === Number(routeId);\n    });\n\n    if (!route) {\n        return;\n    }\n\n    if (!canWriteObject(route)) {\n        showAppError(i18n.t("routes.permissions.rules_denied"));\n        return;\n    }\n\n    selectedRouteForServiceRules = route.id;\n\n    $("#route-service-rules-title").text(\n        i18n.t("routes.rules.title_named", {\n            name: route.name || i18n.t("routes.row.number", {id: route.id}),\n        })\n    );\n    $("#service-rule-team").val(route.team_id);\n    $("#service-rule-route").val(route.id);\n\n    resetServiceRuleForm();\n\n    loadServiceRuleServices(route.team_id, function () {\n        loadServiceRuleMatcherPresets(route.team_id, null, function () {\n            refreshRouteServiceRules();\n            openAppModal("#route-service-rules-modal");\n        });\n    });\n}\n\nfunction loadServiceRuleServices(teamId, callback) {\n    const select = $("#service-rule-service");\n\n    select.empty();\n    select.append($("<option>").val("").text(i18n.t("routes.rules.select_service")));\n\n    if (!teamId) {\n        if (typeof callback === "function") {\n            callback();\n        }\n        return;\n    }\n\n    apiGet("/api/services?team_id=" + encodeURIComponent(teamId), function (services) {\n        select.empty();\n        select.append($("<option>").val("").text(i18n.t("routes.rules.select_service")));\n\n        asArray(services).forEach(function (service) {\n            if (!service.enabled) {\n                return;\n            }\n\n            select.append(\n                $("<option>")\n                    .val(String(service.id))\n                    .text(service.name + " (" + service.slug + ")")\n            );\n        });\n\n        if (typeof callback === "function") {\n            callback();\n        }\n    });\n}\n\nfunction loadServiceRuleMatcherPresets(teamId, selectedPresetId, callback) {\n    routeServiceRuleMatcherPresetsCache = [];\n    fillMatcherPresetSelect("#service-rule-matcher-preset", [], null);\n    updateServiceRuleMatcherPresetHint();\n\n    loadMatcherPresetsForTeam(teamId, function (presets) {\n        routeServiceRuleMatcherPresetsCache = presets;\n        fillMatcherPresetSelect("#service-rule-matcher-preset", presets, selectedPresetId);\n        updateServiceRuleMatcherPresetHint();\n\n        if (typeof callback === "function") {\n            callback();\n        }\n    });\n}\n\nfunction refreshRouteServiceRules() {\n    const route = getSelectedRouteForServiceRules();\n\n    if (!route) {\n        routeServiceRulesCache = [];\n        renderRouteServiceRulesTable();\n        return;\n    }\n\n    apiGet(\n        "/api/services/match-rules?route_id=" + encodeURIComponent(route.id),\n        function (rules) {\n            routeServiceRulesCache = asArray(rules);\n            renderRouteServiceRulesTable();\n        }\n    );\n}\n\nfunction renderRouteServiceRulesTable() {\n    const tbody = $("#route-service-rules-table");\n\n    tbody.empty();\n\n    if (!routeServiceRulesCache.length) {\n        tbody.append(\n            $("<tr>").append(\n                $("<td>")\n                    .attr("colspan", "6")\n                    .addClass("empty-cell")\n                    .text(i18n.t("routes.rules.empty"))\n            )\n        );\n        return;\n    }\n\n    routeServiceRulesCache.forEach(function (rule) {\n        tbody.append(renderRouteServiceRuleRow(rule));\n    });\n}\n\nfunction renderRouteServiceRuleRow(rule) {\n    const row = $("<tr>");\n\n    row.append($("<td>").text(rule.position || 0));\n\n    row.append(\n        $("<td>")\n            .addClass("table-cell-truncate")\n            .attr("title", rule.name || "-")\n            .text(rule.name || "-")\n    );\n\n    row.append(\n        $("<td>")\n            .addClass("table-cell-truncate")\n            .attr("title", rule.service_name || rule.service_slug || "-")\n            .text(rule.service_name || rule.service_slug || "-")\n    );\n\n    const matchersCell = $("<td>");\n    const matcherPreset = rule.matcher_preset;\n\n    if (matcherPreset) {\n        matchersCell.append(\n            $("<div>")\n                .addClass("row-subtitle")\n                .text(\n                    i18n.t("routes.rules.preset", {\n                        name: formatMatcherPresetOption(matcherPreset),\n                    })\n                )\n        );\n    }\n\n    matchersCell.append(\n        $("<code>")\n            .addClass("inline-code")\n            .text(JSON.stringify(rule.matchers || {}))\n    );\n\n    row.append(matchersCell);\n\n    row.append(\n        $("<td>").append(\n            renderStatusBadge(\n                rule.enabled,\n                i18n.t("routes.status.enabled"),\n                i18n.t("routes.status.disabled")\n            )\n        )\n    );\n\n    row.append(\n        $("<td>")\n            .addClass("actions-cell")\n            .append(\n                makeActionMenu({\n                    object: getSelectedRouteForServiceRules(),\n                    items: [\n                        {\n                            label: i18n.t("routes.actions.edit"),\n                            icon: "fas fa-edit",\n                            required: "write",\n                            onClick: function () {\n                                editRouteServiceRule(rule);\n                            }\n                        },\n                        {\n                            label: i18n.t("routes.actions.delete"),\n                            icon: "fas fa-trash",\n                            required: "write",\n                            danger: true,\n                            onClick: function () {\n                                deleteRouteServiceRule(rule);\n                            }\n                        }\n                    ]\n                })\n            )\n    );\n\n    return row;\n}\n\nfunction resetServiceRuleForm() {\n    $("#service-rule-form-title").text(i18n.t("routes.rules.create"));\n    $("#service-rule-id").val("");\n    $("#service-rule-name").val("");\n    $("#service-rule-service").val("");\n    $("#service-rule-position").val("0");\n    $("#service-rule-enabled").prop("checked", true);\n\n    fillMatcherPresetSelect("#service-rule-matcher-preset", routeServiceRuleMatcherPresetsCache, null);\n    updateServiceRuleMatcherPresetHint();\n\n    setMatcherEditorValue("#service-rule-matchers", {});\n}\n\nfunction editRouteServiceRule(rule) {\n    $("#service-rule-form-title").text(i18n.t("routes.rules.edit"));\n    $("#service-rule-id").val(rule.id);\n    $("#service-rule-name").val(rule.name || "");\n    $("#service-rule-service").val(rule.service_id || "");\n    $("#service-rule-position").val(rule.position || 0);\n    $("#service-rule-enabled").prop("checked", rule.enabled !== false);\n\n    const matcherPresetId = rule.matcher_preset_id || (rule.matcher_preset ? rule.matcher_preset.id : null);\n\n    fillMatcherPresetSelect("#service-rule-matcher-preset", routeServiceRuleMatcherPresetsCache, matcherPresetId);\n    updateServiceRuleMatcherPresetHint();\n\n    setMatcherEditorValue("#service-rule-matchers", rule.matchers || {});\n}\n\nfunction collectRouteServiceRulePayload() {\n    return {\n        team_id: Number($("#service-rule-team").val()),\n        route_id: $("#service-rule-route").val() ? Number($("#service-rule-route").val()) : null,\n        service_id: Number($("#service-rule-service").val()),\n        position: Number($("#service-rule-position").val() || 0),\n        name: $("#service-rule-name").val(),\n        description: null,\n        matcher_preset_id: $("#service-rule-matcher-preset").val() ? Number($("#service-rule-matcher-preset").val()) : null,\n        matchers: getMatcherEditorValue("#service-rule-matchers", {}),\n        enabled: $("#service-rule-enabled").is(":checked"),\n    };\n}\n\nfunction saveRouteServiceRule() {\n    const id = $("#service-rule-id").val();\n    const payload = collectRouteServiceRulePayload();\n\n    if (!payload.service_id) {\n        showAppError(i18n.t("routes.rules.service_required"));\n        return;\n    }\n\n    if (!payload.matcher_preset_id && !Object.keys(payload.matchers || {}).length) {\n        showAppError(i18n.t("routes.rules.matcher_required"));\n        return;\n    }\n\n    if (id) {\n        apiPut("/api/services/match-rules/" + id, payload, function () {\n            resetServiceRuleForm();\n            refreshRouteServiceRules();\n        });\n        return;\n    }\n\n    apiPost(\n        "/api/services/" + payload.service_id + "/match-rules",\n        payload,\n        function () {\n            resetServiceRuleForm();\n            refreshRouteServiceRules();\n        }\n    );\n}\n\nfunction deleteRouteServiceRule(rule) {\n    showAppConfirm({\n        title: i18n.t("routes.rules.delete_title"),\n        message: i18n.t("routes.rules.delete_message", {\n            name: rule.name || ("#" + rule.id),\n        }),\n        confirmText: i18n.t("routes.actions.delete"),\n        confirmClass: "btn-danger",\n    }).done(function () {\n        apiDelete("/api/services/match-rules/" + rule.id, function () {\n            refreshRouteServiceRules();\n        });\n    });\n}\nfunction routeDetailsItem(label, value) {\n    return $("<div>")\n        .addClass("details-item")\n        .append($("<div>").addClass("details-label").text(label))\n        .append($("<div>").addClass("details-value").text(value || "-"));\n}\n\nfunction routeDetailsCode(label, value) {\n    return $("<div>")\n        .addClass("details-item")\n        .append($("<div>").addClass("details-label").text(label))\n        .append($("<pre>").addClass("details-code").text(JSON.stringify(value || {}, null, 2)));\n}\n\nfunction renderRouteDetails(route) {\n    selectedRouteDetailsId = route.id;\n\n    $("#route-details-subtitle").text(\n        getRouteTeamLabel(route)\n        + " / "\n        + (\n            route.enabled\n                ? i18n.t("routes.status.enabled")\n                : i18n.t("routes.status.disabled")\n        )\n    );\n\n    const body = $("#route-details-body");\n    body.empty();\n    body.append(\n        $("<div>")\n            .addClass("details-list")\n            .append(routeDetailsItem(i18n.t("routes.details.name"), route.name))\n            .append(routeDetailsItem(i18n.t("routes.details.team"), getRouteTeamLabel(route)))\n            .append(routeDetailsItem(i18n.t("routes.details.source"), route.source))\n            .append(\n                route.source === "sentry"\n                    ? routeDetailsItem(\n                        i18n.t("routes.details.sentry_webhook"),\n                        getSentryWebhookUrl(route)\n                    )\n                    : $()\n            )\n            .append(\n                route.source === "sentry"\n                    ? routeDetailsItem(\n                        i18n.t("routes.details.sentry_secret"),\n                        getRouteSentryConfig(route).has_webhook_secret\n                            ? i18n.t("routes.details.configured")\n                            : i18n.t("routes.details.not_configured")\n                    )\n                    : $()\n            )\n            .append(\n                route.source === "sentry"\n                    ? routeDetailsItem(\n                        i18n.t("routes.details.sentry_base_url"),\n                        getRouteSentryConfig(route).base_url || "-"\n                    )\n                    : $()\n            )\n            .append(\n                route.source === "sentry"\n                    ? routeDetailsItem(\n                        i18n.t("routes.details.sentry_org"),\n                        getRouteSentryConfig(route).organization_slug || "-"\n                    )\n                    : $()\n            )\n            .append(\n                routeDetailsItem(\n                    route.source === "aws_sns"\n                        ? i18n.t("routes.details.sns_webhook")\n                        : i18n.t("routes.details.webhook"),\n                    getRouteIntakeUrl(route)\n                )\n            )\n            .append(\n                routeDetailsItem(\n                    i18n.t("routes.details.maintenance"),\n                    window.AppMaintenanceBadges.text(route, "-")\n                )\n            )\n            .append(routeDetailsItem(i18n.t("routes.details.escalation"), getRouteEscalationLabel(route)))\n            .append(routeDetailsItem(i18n.t("routes.details.team_escalation"), getRouteTeamEscalationLabel(route)))\n            .append(routeDetailsItem(i18n.t("routes.details.notification_source"), getRouteNotificationModeLabel(route)))\n            .append(routeDetailsItem(i18n.t("routes.details.channels"), asArray(route.channels).map(function (channel) {\n                return channel.name;\n            }).join(", ") || "-"))\n            .append(\n                !["sentry", "aws_sns"].includes(route.source)\n                    ? routeDetailsItem(\n                        i18n.t("routes.details.token_prefix"),\n                        route.intake_token_prefix\n                    )\n                    : $()\n            )\n            .append(\n                routeDetailsItem(\n                    i18n.t("routes.details.status"),\n                    route.enabled\n                        ? i18n.t("routes.status.enabled")\n                        : i18n.t("routes.status.disabled")\n                )\n            )\n            .append(\n                routeDetailsItem(\n                    i18n.t("routes.details.matcher_preset"),\n                    route.matcher_preset\n                        ? formatMatcherPresetOption(route.matcher_preset)\n                        : i18n.t("routes.form.no_preset")\n                )\n            )\n            .append(routeDetailsCode(i18n.t("routes.details.additional_matchers"), route.matchers || {}))\n            .append(routeDetailsCode(i18n.t("routes.details.group_by"), asArray(route.group_by)))\n            .append(routeDetailsItem(i18n.t("routes.details.service"), route.service_name || route.service_slug || "-"))\n    );\n\n    if (route.source === "aws_sns") {\n        const awsSnsConfig = getRouteAwsSnsConfig(route);\n\n        body.find(".details-list").append(\n            routeDetailsItem(\n                i18n.t("routes.details.sns_topic"),\n                awsSnsConfig.topic_arn\n            )\n        );\n    }\n\n    const actions = $("<div>").addClass("details-actions");\n    appendIconActionIfAllowed(actions, route, {\n        required: "write",\n        icon: "fas fa-edit",\n        label: i18n.t("routes.actions.edit_route"),\n        onClick: function () {\n            editRoute(route.id);\n        },\n    });\n    if (!["sentry", "aws_sns"].includes(route.source)) {\n        appendIconActionIfAllowed(actions, route, {\n            required: "write",\n            icon: "fas fa-sync-alt",\n            label: i18n.t("routes.actions.regenerate_route_token"),\n            onClick: function () {\n                regenerateRouteToken(route.id);\n            },\n        });\n    }\n    appendIconActionIfAllowed(actions, route, {\n        required: "write",\n        icon: route.enabled ? "fas fa-pause" : "fas fa-play",\n        label: route.enabled\n            ? i18n.t("routes.actions.disable_route")\n            : i18n.t("routes.actions.enable_route"),\n        className: route.enabled ? "btn-warning" : "btn-success",\n        onClick: function () {\n            if (route.enabled) {\n                disableRoute(route);\n            } else {\n                enableRoute(route);\n            }\n        },\n    });\n    appendIconActionIfAllowed(actions, route, {\n        required: "delete",\n        icon: "fas fa-trash-alt",\n        label: i18n.t("routes.actions.delete_route"),\n        className: "btn-danger",\n        onClick: function () {\n            deleteRoute(route);\n        },\n    });\n\n    if (actions.children().length) {\n        body.append(actions);\n    }\n}\n\nfunction renderRouteDetailsEmpty() {\n    selectedRouteDetailsId = null;\n    $("#route-details-subtitle").text(i18n.t("routes.details.select"));\n    $("#route-details-body")\n        .empty()\n        .append(\n            $("<p>")\n                .addClass("muted")\n                .text(i18n.t("routes.details.empty"))\n        );\n}\n\nfunction restoreRouteDetails() {\n    if (!routesCache.length) {\n        renderRouteDetailsEmpty();\n        return;\n    }\n\n    if (selectedRouteDetailsId) {\n        const selected = routesCache.find(function (route) {\n            return Number(route.id) === Number(selectedRouteDetailsId);\n        });\n        if (selected) {\n            renderRouteDetails(selected);\n            return;\n        }\n    }\n\n    renderRouteDetails(routesCache[0]);\n}\nfunction getRouteSentryConfig(route) {\n    const integrationConfig = route && route.integration_config\n        ? route.integration_config\n        : {};\n\n    return integrationConfig.sentry || {};\n}\n\nfunction getSentryWebhookUrl(route) {\n    const sentryConfig = getRouteSentryConfig(route);\n    const path = sentryConfig.webhook_path;\n\n    if (!path) {\n        return "";\n    }\n\n    return window.location.origin + path;\n}\n\nfunction getRouteAwsSnsConfig(route) {\n    const integrationConfig = (\n        route && route.integration_config\n            ? route.integration_config\n            : {}\n    );\n\n    return integrationConfig.aws_sns || {};\n}\n\nfunction updateRouteSourceUi() {\n    const source = String(\n        $("#route-source").val() || ""\n    );\n\n    const isSentry = source === "sentry";\n    const isAwsSns = source === "aws_sns";\n    const isWebhook = source === "webhook";\n\n    $("#route-sentry-settings").toggleClass("is-hidden", !isSentry);\n    $("#route-webhook-compatibility-help").toggleClass(\n        "is-hidden",\n        !isWebhook\n    );\n\n    $("#route-aws-sns-settings").toggleClass("is-hidden", !isAwsSns);\n\n    $("#route-aws-sns-topic-arn").prop("required", isAwsSns);\n\n    const hasAwsSnsWebhook = Boolean(\n        String(\n            $("#route-aws-sns-webhook-url").val() || ""\n        ).trim()\n    );\n\n    $("#route-aws-sns-webhook-group").toggleClass(\n        "is-hidden",\n        !isAwsSns || !hasAwsSnsWebhook\n    );\n\n    if (!$("#route-id").val()) {\n        if (source === "sentry") {\n            $("#route-group-by").val(\n                \'["project_slug","issue_id"]\'\n            );\n        } else if (source === "grafana") {\n            $("#route-group-by").val(\n                \'["alertname","grafana_folder","instance"]\'\n            );\n        } else if (source === "rmon") {\n            $("#route-group-by").val(\n                \'["rmon_check_id","rmon_check_type"]\'\n            );\n        } else if (source === "aws_sns") {\n            $("#route-group-by").val(\n                \'["cloudwatch_alarm_arn"]\'\n            );\n        }\n    }\n}\n\nfunction collectRouteIntegrationConfig() {\n    const source = $("#route-source").val();\n\n    if (source === "aws_sns") {\n        return {\n            aws_sns: {\n                topic_arn: String(\n                    $("#route-aws-sns-topic-arn").val() || ""\n                ).trim()\n            }\n        };\n    }\n\n    if (source === "sentry") {\n        const secret = String(\n            $("#route-sentry-webhook-secret").val() || ""\n        ).trim();\n\n        const baseUrl = String(\n            $("#route-sentry-base-url").val() || ""\n        ).trim();\n\n        const organizationSlug = String(\n            $("#route-sentry-organization-slug").val() || ""\n        ).trim();\n\n        const sentry = {};\n\n        if (secret) {\n            sentry.webhook_secret = secret;\n        }\n\n        if (baseUrl) {\n            sentry.base_url = baseUrl;\n        }\n\n        if (organizationSlug) {\n            sentry.organization_slug = organizationSlug;\n        }\n\n        return {\n            sentry: sentry\n        };\n    }\n    return {};\n}\n\nfunction updateSentrySecretHelp(route) {\n    const sentryConfig = getRouteSentryConfig(route);\n    let key = "routes.form.sentry_secret_new_help";\n\n    if (route && sentryConfig.has_webhook_secret) {\n        key = "routes.form.sentry_secret_configured_help";\n    } else if (route && route.id) {\n        key = "routes.form.sentry_secret_missing_help";\n    }\n\n    $("#route-sentry-secret-help").text(i18n.t(key));\n}\n\nfunction getRouteGroupByValue() {\n    const value = parseJsonInput(\n        "#route-group-by",\n        []\n    );\n\n    if (!Array.isArray(value)) {\n        const message = i18n.t("routes.validation.group_by");\n\n        if (typeof showAppError === "function") {\n            showAppError(message);\n        } else {\n            alert(message);\n        }\n\n        throw new Error(message);\n    }\n\n    return value;\n}\n\nfunction collectRoutePayload() {\n    const mode = $("#route-escalation-mode").val() || "rotation";\n    const usePolicy = mode === "policy";\n\n    return {\n        team_id: Number($("#route-team").val()),\n        name: $("#route-name").val(),\n        source: $("#route-source").val(),\n        escalation_mode: mode,\n        rotation_id: (\n            !usePolicy && $("#route-rotation").val()\n                ? Number($("#route-rotation").val())\n                : null\n        ),\n\n        escalation_policy_id: (\n            usePolicy && $("#route-escalation-policy").val()\n                ? Number($("#route-escalation-policy").val())\n                : null\n        ),\n        channel_ids: ($("#route-channels").val() || []).map(Number),\n        notification_channel_mode: ($("#route-notification-channel-mode").val() || "route_only"),\n        service_id: $("#route-service").val() ? Number($("#route-service").val()) : null,\n        matcher_preset_id: $("#route-matcher-preset").val() ? Number($("#route-matcher-preset").val()) : null,\n        matchers: getMatcherEditorValue("#route-matchers", {}),\n        group_by: getRouteGroupByValue(),\n\n        integration_config: collectRouteIntegrationConfig(),\n\n        enabled: $("#route-enabled").is(":checked")\n    };\n}\n\nfunction saveRoute() {\n    const id = $("#route-id").val();\n    const existing = id ? routesCache.find(function (item) {\n        return Number(item.id) === Number(id);\n    }) : null;\n    if (existing && !canWriteObject(existing)) {\n        showAppError(i18n.t("routes.permissions.edit"));\n        return;\n    }\n\n    if (id) {\n        apiPut("/api/routes/" + id, collectRoutePayload(), function () {\n            closeAppModal("#route-form-modal");\n            resetRouteForm();\n            refreshRoutes();\n        });\n        return;\n    }\n\n    const payload = collectRoutePayload();\n\n    apiPost("/api/routes", payload, function (response) {\n        closeAppModal("#route-form-modal");\n        resetRouteForm();\n        refreshRoutes();\n\n        if (payload.source === "aws_sns") {\n            showAppSuccess(\n                i18n.t("routes.success.sns_created")\n            );\n            return;\n        }\n\n        showRouteIntakeDetails(response);\n    });\n}\n\nfunction editRoute(id) {\n    const route = routesCache.find(function (item) {\n        return Number(item.id) === Number(id);\n    });\n    if (!route) {\n        return;\n    }\n    if (!canWriteObject(route)) {\n        showAppError(i18n.t("routes.permissions.edit_denied"));\n        return;\n    }\n\n    $("#route-form-title").text(i18n.t("routes.form.edit", {id: id}));\n    $("#route-id").val(route.id);\n    $("#route-team").val(route.team_id);\n    $("#route-name").val(route.name);\n    $("#route-source").val(route.source);\n    const integrationConfig = (\n        route.integration_config || {}\n    );\n\n    const awsSnsConfig = (\n        integrationConfig.aws_sns || {}\n    );\n\n    const sentryConfig = (\n        integrationConfig.sentry || {}\n    );\n\n    $("#route-aws-sns-topic-arn").val(\n        awsSnsConfig.topic_arn || ""\n    );\n\n    const awsSnsWebhookPath = (\n        awsSnsConfig.webhook_path || ""\n    );\n\n    if (route.source === "aws_sns" && awsSnsWebhookPath) {\n        $("#route-aws-sns-webhook-url").val(\n            window.location.origin + awsSnsWebhookPath\n        );\n\n        $("#route-aws-sns-webhook-group")\n            .removeClass("is-hidden");\n    } else {\n        $("#route-aws-sns-webhook-url").val("");\n\n        $("#route-aws-sns-webhook-group")\n            .addClass("is-hidden");\n    }\n\n    setMatcherEditorValue("#route-matchers", route.matchers || {});\n    $("#route-group-by").val(JSON.stringify(asArray(route.group_by), null, 2));\n    $("#route-enabled").prop("checked", !!route.enabled);\n    $("#route-sentry-webhook-secret").val("");\n    $("#route-sentry-base-url").val(sentryConfig.base_url || "");\n    $("#route-sentry-organization-slug").val(sentryConfig.organization_slug || "");\n\n    updateSentrySecretHelp(route);\n    updateRouteSourceUi();\n\n    loadRouteDependencies(function () {\n        const usePolicy = !!route.escalation_policy_id;\n\n        $("#route-escalation-mode").val(usePolicy ? "policy" : "rotation");\n        $("#route-rotation").val(route.rotation_id || "");\n        $("#route-escalation-policy").val(route.escalation_policy_id || "");\n\n        $("#route-channels").val(asArray(route.channels).map(function (channel) {\n            return String(channel.id);\n        }));\n\n        $("#route-service").val(route.service_id || "");\n        $("#route-notification-channel-mode").val(route.notification_channel_mode || "route_only");\n\n        updateRouteNotificationChannelModeUi();\n        updateRouteEscalationModeUi();\n        updateRouteSourceUi();\n    }, route.matcher_preset_id || (route.matcher_preset ? route.matcher_preset.id : null));\n\n    openAppModal("#route-form-modal");\n}\n\nfunction disableRoute(route) {\n    if (!canWriteObject(route)) {\n        showAppError(i18n.t("routes.permissions.disable_denied"));\n        return;\n    }\n\n    const routeName = route.name || i18n.t("routes.row.number", {id: route.id});\n    showAppConfirm({\n        title: i18n.t("routes.confirm.disable_title"),\n        message: i18n.t("routes.confirm.disable_message", {\n            name: routeName,\n        }),\n        confirmText: i18n.t("routes.actions.disable"),\n        confirmClass: "btn-warning",\n    }).done(function () {\n        apiPost("/api/routes/" + route.id + "/disable", {}, function () {\n            refreshRoutes();\n        });\n    });\n}\n\nfunction enableRoute(route) {\n    if (!canWriteObject(route)) {\n        showAppError(i18n.t("routes.permissions.enable_denied"));\n        return;\n    }\n\n    apiPost("/api/routes/" + route.id + "/enable", {}, function () {\n        refreshRoutes();\n    });\n}\n\nfunction deleteRoute(route) {\n    if (!canDeleteObject(route)) {\n        showAppError(i18n.t("routes.permissions.delete_denied"));\n        return;\n    }\n\n    const routeName = route.name || i18n.t("routes.row.number", {id: route.id});\n    showAppConfirm({\n        title: i18n.t("routes.confirm.delete_title"),\n        message: i18n.t("routes.confirm.delete_message", {\n            name: routeName,\n        }),\n        confirmText: i18n.t("routes.actions.delete"),\n        confirmClass: "btn-danger",\n    }).done(function () {\n        apiDelete("/api/routes/" + route.id, function () {\n            if (Number(selectedRouteDetailsId) === Number(route.id)) {\n                selectedRouteDetailsId = null;\n                renderRouteDetailsEmpty();\n            }\n            refreshRoutes();\n        });\n    });\n}\n\nfunction resetRouteForm() {\n    $("#route-form-title").text(i18n.t("routes.form.create"));\n    $("#route-id").val("");\n    $("#route-name").val("");\n    $("#route-source").val("alertmanager");\n    setMatcherEditorValue("#route-matchers", {});\n    $("#route-group-by").val(\'["alertname","instance"]\');\n    $("#route-enabled").prop("checked", true);\n    $("#route-rotation").val("");\n    $("#route-channels").val([]);\n    $("#route-escalation-mode").val("rotation");\n    $("#route-escalation-policy").val("");\n    $("#route-service").val("");\n    fillMatcherPresetSelect("#route-matcher-preset", routeMatcherPresetsCache, null);\n    updateRouteMatcherPresetHint();\n    $("#route-aws-sns-topic-arn").val("");\n    $("#route-aws-sns-webhook-url").val("");\n    $("#route-aws-sns-webhook-group").addClass("is-hidden");\n    updateRouteEscalationModeUi();\n    $("#route-sentry-webhook-secret").val("");\n    $("#route-sentry-base-url").val("");\n    $("#route-sentry-organization-slug").val("");\n    $("#route-notification-channel-mode").val("route_only");\n    updateRouteNotificationChannelModeUi();\n    updateSentrySecretHelp(null);\n    updateRouteSourceUi();\n}\n\nfunction getRouteIntakePath(route) {\n    const source = String(route.source || "").toLowerCase();\n\n    if (source === "sentry") {\n        return "/api/integrations/sentry/" + route.id;\n    }\n\n    if (source === "aws_sns") {\n        const awsSnsConfig = getRouteAwsSnsConfig(route);\n\n        return (\n            awsSnsConfig.webhook_path\n            || ("/api/integrations/aws-sns/" + route.id)\n        );\n    }\n\n    if (source === "alertmanager") {\n        return "/api/integrations/alertmanager";\n    }\n\n    if (source === "heartbeat") {\n        return "/api/heartbeats/ping/<heartbeat-token>";\n    }\n\n    if (source === "zabbix") {\n        return "/api/integrations/zabbix";\n    }\n\n    if (source === "webhook") {\n        return "/api/integrations/webhook";\n    }\n\n    return "/api/integrations/" + source;\n}\n\nfunction getRouteIntakeUrl(route) {\n    return window.location.origin + getRouteIntakePath(route);\n}\n\nfunction buildRouteIntakeCurl(route, token) {\n    const url = getRouteIntakeUrl(route);\n    const source = String(route.source || "").toLowerCase();\n\n    if (source === "sentry") {\n        return [\n            "curl -X POST \'" + url + "\' \\\\",\n            "  -H \'Content-Type: application/json\' \\\\",\n            "  -H \'Sentry-Hook-Resource: event_alert\' \\\\",\n            "  -H \'Sentry-Hook-Signature: <generated-by-sentry>\' \\\\",\n            "  -d \'{\\"action\\":\\"triggered\\",\\"data\\":{}}\'"\n        ].join("\\n");\n    }\n\n    if (source === "heartbeat") {\n        return [\n            "curl -fsS -X POST \'" + url + "\' \\\\",\n            "  -H \'Content-Type: application/json\' \\\\",\n            "  -d \'{}\'"\n        ].join("\\n");\n    }\n\n    if (source === "webhook") {\n        return [\n            "# " + i18n.t("routes.intake.generic_example_comment"),\n            "curl -X POST \'" + url + "\' \\\\",\n            "  -H \'Content-Type: application/json\' \\\\",\n            "  -H \'Authorization: Bearer " + (token || "<route-token>") + "\' \\\\",\n            "  -d \'{\\"title\\":\\"Example alert\\",\\"severity\\":\\"critical\\",\\"fingerprint\\":\\"example-1\\"}\'",\n            "",\n            "# " + i18n.t("routes.intake.pagerduty_example_comment"),\n            "curl -X POST \'" + url + "\' \\\\",\n            "  -H \'Content-Type: application/json\' \\\\",\n            "  -d \'{\\"routing_key\\":\\"" + (token || "<route-token>") + "\\",\\"event_action\\":\\"trigger\\",\\"dedup_key\\":\\"example-1\\",\\"payload\\":{\\"summary\\":\\"Example alert\\",\\"source\\":\\"example-host\\",\\"severity\\":\\"critical\\"}}\'"\n        ].join("\\n");\n    }\n\n    return [\n        "curl -X POST \'" + url + "\' \\\\",\n        "  -H \'Content-Type: application/json\' \\\\",\n        "  -H \'Authorization: Bearer " + (token || "<route-token>") + "\' \\\\",\n        "  -d \'{}\'"\n    ].join("\\n");\n}\n\nfunction showRouteIntakeDetails(route) {\n    const source = String(route.source || "").toLowerCase();\n    const token = route.intake_token || "";\n    const isSentry = source === "sentry";\n    const isHeartbeat = source === "heartbeat";\n    const isWebhook = source === "webhook";\n    const url = getRouteIntakeUrl(route);\n\n    let titleKey = "routes.intake.title";\n    let subtitleKey = "routes.intake.route_subtitle";\n    let helpKey = "routes.intake.route_help";\n\n    if (isSentry) {\n        titleKey = "routes.intake.sentry_title";\n        subtitleKey = "routes.intake.sentry_subtitle";\n        helpKey = "routes.intake.sentry_help";\n    } else if (isHeartbeat) {\n        titleKey = "routes.intake.heartbeat_title";\n        subtitleKey = "routes.intake.heartbeat_subtitle";\n        helpKey = "routes.intake.heartbeat_help";\n    } else if (isWebhook) {\n        subtitleKey = "routes.intake.webhook_subtitle";\n        helpKey = "routes.intake.webhook_help";\n    }\n\n    $("#route-intake-title").text(i18n.t(titleKey));\n    $("#route-intake-subtitle").text(i18n.t(subtitleKey));\n\n    $("#route-intake-url").val(url);\n    $("#route-intake-token").val(token);\n    $("#route-intake-curl").val(buildRouteIntakeCurl(route, token));\n\n    $("#route-intake-token-group").toggleClass("is-hidden", isSentry || isHeartbeat);\n    $("#copy-route-intake-token").toggleClass("is-hidden", isSentry || isHeartbeat);\n    $("#route-intake-url-help").text(i18n.t(helpKey));\n\n    openAppModal("#route-token-box");\n}\n\nfunction closeRouteTokenModal() {\n    closeAppModal("#route-token-box");\n    $("#route-intake-url").val("");\n    $("#route-intake-token").val("");\n    $("#route-intake-curl").val("");\n}\n\nfunction regenerateRouteToken(routeId) {\n    const route = routesCache.find(function (item) {\n        return Number(item.id) === Number(routeId);\n    });\n    if (route && !canWriteObject(route)) {\n        showAppError(i18n.t("routes.permissions.regenerate_denied"));\n        return;\n    }\n\n    showAppConfirm({\n        title: i18n.t("routes.confirm.regenerate_title"),\n        message: i18n.t("routes.confirm.regenerate_message"),\n        confirmText: i18n.t("routes.actions.regenerate"),\n        confirmClass: "btn-warning",\n    }).done(function () {\n        apiPost("/api/routes/" + routeId + "/intake-token", {}, function (response) {\n            showRouteIntakeDetails(response);\n            refreshRoutes();\n        });\n    });\n}\n\nfunction openCreateRouteModal() {\n    resetRouteForm();\n    $("#route-form-title").text(i18n.t("routes.form.create"));\n    loadRouteDependencies();\n    openAppModal("#route-form-modal");\n}\nfunction copyTextFromField(selector) {\n    const value = $(selector).val() || "";\n\n    if (!value) {\n        return;\n    }\n\n    if (navigator.clipboard && navigator.clipboard.writeText) {\n        navigator.clipboard.writeText(value);\n        return;\n    }\n\n    const field = $(selector);\n    field.trigger("select");\n    document.execCommand("copy");\n}\nfunction copyRouteIntakeToken() {\n    copyTextFromField("#route-intake-token");\n}\n\nfunction copyRouteIntakeUrl() {\n    copyTextFromField("#route-intake-url");\n}\n\nfunction copyRouteIntakeCurl() {\n    copyTextFromField("#route-intake-curl");\n}\n\nfunction initRoutesTableSorting() {\n    bindSortableTableHeaders(\n        "#routes-table-view",\n        routesSortState,\n        routesSortColumns,\n        renderRoutesTable\n    );\n}\n\nfunction getFilteredRoutes() {\n    const query = String($("#routes-search").val() || "").trim().toLowerCase();\n    const source = String($("#routes-source-filter").val() || "");\n    const status = String($("#routes-status-filter").val() || "");\n\n    const filtered = routesCache.filter(function (route) {\n        if (source && route.source !== source) {\n            return false;\n        }\n        if (status === "enabled" && !route.enabled) {\n            return false;\n        }\n        if (status === "disabled" && route.enabled) {\n            return false;\n        }\n        if (!query) {\n            return true;\n        }\n        return getRouteSearchText(route).indexOf(query) !== -1;\n    });\n\n    return sortTableData(filtered, routesSortState, routesSortColumns);\n}\n\n$(document).on("change", "#route-team", function () {\n    loadRouteDependencies();\n});\n$(document).on("change", "#route-source", updateRouteSourceUi);\n$(document).on("input", "#routes-search", renderRoutesTable);\n$(document).on("change", "#routes-source-filter, #routes-status-filter", renderRoutesTable);\n$(document).on("click", "#open-route-create-modal", openCreateRouteModal);\n$(document).on("click", "#save-route", saveRoute);\n$(document).on("click", "#reset-route-form", resetRouteForm);\n$(document).on("click", "#reload-routes", refreshRoutes);\n$(document).on("click", "#close-route-form-modal", function () {\n    closeAppModal("#route-form-modal");\n});\n$(document).on("click", "#close-route-token-modal, #close-route-token-modal-footer", closeRouteTokenModal);\n$(document).on("click", "#copy-route-intake-token", copyRouteIntakeToken);\n$(document).on("click", "#route-form-modal", function (event) {\n    if (event.target === this) {\n        closeAppModal("#route-form-modal");\n    }\n});\n$(document).on("click", "#route-token-box", function (event) {\n    if (event.target === this) {\n        closeRouteTokenModal();\n    }\n});\n$(document).on("keydown", function (event) {\n    if (event.key !== "Escape") {\n        return;\n    }\n    if ($("#route-token-box").hasClass("is-open")) {\n        closeRouteTokenModal();\n        return;\n    }\n    if ($("#route-form-modal").hasClass("is-open")) {\n        closeAppModal("#route-form-modal");\n    }\n});\n$(document).on("change", "#route-rotation", function () {\n    if ($(this).val()) {\n        $("#route-escalation-mode").val("rotation");\n        $("#route-escalation-policy").val("");\n    }\n\n    updateRouteEscalationModeUi();\n});\n$(document).on("change", "#route-escalation-policy", function () {\n    if ($(this).val()) {\n        $("#route-escalation-mode").val("policy");\n    }\n\n    updateRouteEscalationModeUi();\n});\nfunction updateRouteEscalationModeUi() {\n    const mode = $("#route-escalation-mode").val() || "rotation";\n    const usePolicy = mode === "policy";\n\n    $("#route-rotation")\n        .prop("disabled", usePolicy)\n        .closest(".form-group")\n        .toggleClass("is-muted", usePolicy);\n\n    $("#route-escalation-policy")\n        .prop("disabled", !usePolicy);\n\n    $("#route-policy-group").toggleClass("is-hidden", !usePolicy);\n}\n$(document).on("change", "#route-escalation-mode", updateRouteEscalationModeUi);\n$(document).on("click", "#save-service-rule", saveRouteServiceRule);\n\n$(document).on("click", "#reset-service-rule-form", resetServiceRuleForm);\n\n\n$(document).on("click", "#close-route-service-rules-modal", function () {\n    closeAppModal("#route-service-rules-modal");\n});\n\n$(document).on("click", "#route-service-rules-modal", function (event) {\n    if (event.target === this) {\n        closeAppModal("#route-service-rules-modal");\n    }\n});\n$(document).on("click", "#copy-route-intake-url", copyRouteIntakeUrl);\n$(document).on("click", "#copy-route-intake-curl", copyRouteIntakeCurl);\n$(document).on("change", "#route-notification-channel-mode", updateRouteNotificationChannelModeUi);\n$(document).on("change", "#route-matcher-preset", updateRouteMatcherPresetHint);\n$(document).on("change", "#service-rule-matcher-preset", updateServiceRuleMatcherPresetHint);\n', 'localized')]}
NEW_FILES = {'tests/integrations/test_webhook_pagerduty_v2.py': 'from app.modules.db.models import Alert, AlertGroup\nfrom app.services.integrations.auth import hash_token\nfrom app.services.integrations.normalizers.webhook import normalize_webhook\nfrom tests.factories import create_group, create_route, create_team\n\n\ndef pagerduty_trigger(routing_key, **overrides):\n    payload = {\n        "routing_key": routing_key,\n        "event_action": "trigger",\n        "dedup_key": "database-prod-01",\n        "payload": {\n            "summary": "Production database is unavailable",\n            "source": "db-prod-01",\n            "severity": "error",\n            "component": "postgresql",\n            "group": "production",\n            "class": "database",\n            "custom_details": {\n                "team": "sre",\n                "host": "db-prod-01",\n                "replicas": ["db-prod-02", "db-prod-03"],\n                "message": "Connection checks are failing",\n            },\n        },\n        "links": [\n            {\n                "href": "https://monitoring.example.com/incidents/db-prod-01",\n                "text": "Open monitoring",\n            }\n        ],\n        "client": "Example Monitor",\n        "client_url": "https://monitoring.example.com",\n    }\n    payload.update(overrides)\n    return payload\n\n\ndef test_normalize_webhook_accepts_pagerduty_events_api_v2():\n    alert = normalize_webhook(pagerduty_trigger("secret-route-token"))[0]\n\n    assert alert["source"] == "webhook"\n    assert alert["integration_format"] == "pagerduty_events_api_v2"\n    assert alert["lifecycle_action"] == "trigger"\n    assert alert["status"] == "firing"\n    assert alert["dedup_key"] == "database-prod-01"\n    assert alert["external_id"] == "database-prod-01"\n    assert alert["title"] == "Production database is unavailable"\n    assert alert["message"] == "Connection checks are failing"\n    assert alert["severity"] == "critical"\n    assert alert["team_slug"] == "sre"\n    assert alert["labels"]["source"] == "db-prod-01"\n    assert alert["labels"]["component"] == "postgresql"\n    assert alert["labels"]["group"] == "production"\n    assert alert["labels"]["class"] == "database"\n    assert alert["labels"]["host"] == "db-prod-01"\n    assert alert["labels"]["replicas"] == \'["db-prod-02", "db-prod-03"]\'\n    assert alert["labels"]["event_link"] == (\n        "https://monitoring.example.com/incidents/db-prod-01"\n    )\n    assert alert["payload"]["routing_key"] == "[REDACTED]"\n\n\ndef test_pagerduty_trigger_acknowledge_and_resolve_use_webhook_route(client, db):\n    raw_token = "pagerduty-compatible-routing-key"\n    group = create_group(slug="infra")\n    team = create_team(group, slug="sre")\n    route = create_route(\n        team,\n        source="webhook",\n        token_hash=hash_token(raw_token),\n    )\n\n    trigger_response = client.post(\n        "/api/integrations/webhook",\n        json=pagerduty_trigger(raw_token),\n    )\n\n    assert trigger_response.status_code == 202\n    assert trigger_response.get_json() == {\n        "status": "success",\n        "message": "Event processed",\n        "dedup_key": "database-prod-01",\n    }\n\n    alert = Alert.get(Alert.dedup_key == "database-prod-01")\n    incident = AlertGroup.get_by_id(alert.group_id)\n\n    assert alert.route_id == route.id\n    assert alert.source == "webhook"\n    assert alert.status == "firing"\n    assert alert.payload["routing_key"] == "[REDACTED]"\n    assert incident.status == "firing"\n\n    acknowledge_response = client.post(\n        "/api/integrations/webhook",\n        json={\n            "routing_key": raw_token,\n            "event_action": "acknowledge",\n            "dedup_key": "database-prod-01",\n        },\n    )\n\n    assert acknowledge_response.status_code == 202\n    incident = AlertGroup.get_by_id(incident.id)\n    assert incident.status == "acknowledged"\n\n    resolve_response = client.post(\n        "/api/integrations/webhook",\n        json={\n            "routing_key": raw_token,\n            "event_action": "resolve",\n            "dedup_key": "database-prod-01",\n        },\n    )\n\n    assert resolve_response.status_code == 202\n    incident = AlertGroup.get_by_id(incident.id)\n    alert = Alert.get_by_id(alert.id)\n    assert incident.status == "resolved"\n    assert alert.status == "resolved"\n\n\ndef test_pagerduty_unknown_follow_up_is_successful_noop(client, db):\n    raw_token = "pagerduty-compatible-routing-key"\n    group = create_group(slug="infra")\n    team = create_team(group, slug="sre")\n    create_route(\n        team,\n        source="webhook",\n        token_hash=hash_token(raw_token),\n    )\n\n    response = client.post(\n        "/api/integrations/webhook",\n        json={\n            "routing_key": raw_token,\n            "event_action": "resolve",\n            "dedup_key": "unknown-dedup-key",\n        },\n    )\n\n    assert response.status_code == 202\n    assert response.get_json()["dedup_key"] == "unknown-dedup-key"\n\n\ndef test_pagerduty_routing_key_must_belong_to_webhook_route(client, db):\n    raw_token = "alertmanager-route-token"\n    group = create_group(slug="infra")\n    team = create_team(group, slug="sre")\n    create_route(\n        team,\n        source="alertmanager",\n        token_hash=hash_token(raw_token),\n    )\n\n    response = client.post(\n        "/api/integrations/webhook",\n        json=pagerduty_trigger(raw_token),\n    )\n\n    assert response.status_code == 400\n    assert response.get_json()["error"] == "route_source_mismatch"\n\n\ndef test_pagerduty_payload_requires_valid_routing_key(client):\n    response = client.post(\n        "/api/integrations/webhook",\n        json=pagerduty_trigger("invalid-routing-key"),\n    )\n\n    assert response.status_code == 401\n    assert response.get_json()["error"] == "Route intake token is required"\n\n\ndef test_pagerduty_acknowledge_requires_dedup_key(client, db):\n    raw_token = "pagerduty-compatible-routing-key"\n    group = create_group(slug="infra")\n    team = create_team(group, slug="sre")\n    create_route(\n        team,\n        source="webhook",\n        token_hash=hash_token(raw_token),\n    )\n\n    response = client.post(\n        "/api/integrations/webhook",\n        json={\n            "routing_key": raw_token,\n            "event_action": "acknowledge",\n        },\n    )\n\n    assert response.status_code == 400\n    assert response.get_json()["error"] == "validation_error"\n'}
CATALOG_UPDATES = {'en': {'routes.form.webhook_pd_help': 'Webhook routes accept IncidentRelay generic payloads and PagerDuty Events API v2-compatible trigger, acknowledge and resolve events. Use the route intake token as routing_key.', 'routes.intake.generic_example_comment': 'IncidentRelay generic webhook format', 'routes.intake.pagerduty_example_comment': 'PagerDuty Events API v2-compatible format', 'routes.intake.webhook_subtitle': 'Use this token as Authorization: Bearer for generic payloads or as routing_key for PagerDuty Events API v2-compatible payloads.', 'routes.intake.webhook_help': 'Generic payloads use Authorization: Bearer. PagerDuty Events API v2-compatible payloads put the same token in routing_key.'}, 'ru': {'routes.form.webhook_pd_help': 'Webhook-маршруты принимают обычный формат IncidentRelay и совместимые с PagerDuty Events API v2 события trigger, acknowledge и resolve. Используйте intake token маршрута в поле routing_key.', 'routes.intake.generic_example_comment': 'Обычный формат webhook IncidentRelay', 'routes.intake.pagerduty_example_comment': 'Формат, совместимый с PagerDuty Events API v2', 'routes.intake.webhook_subtitle': 'Для обычного payload используйте токен как Authorization: Bearer, а для формата PagerDuty Events API v2 — как routing_key.', 'routes.intake.webhook_help': 'Обычный payload использует Authorization: Bearer. Совместимый с PagerDuty Events API v2 payload передаёт тот же токен в routing_key.'}}


def digest(content):
    return hashlib.sha256(content.encode("utf-8")).hexdigest()


def main():
    pending = {}
    modes = set()

    for relative_path, variants in FILE_VARIANTS.items():
        path = ROOT / relative_path

        if not path.is_file():
            raise SystemExit(f"ERROR: required file not found: {path}")

        current = path.read_text(encoding="utf-8")
        current_hash = digest(current)
        matched = False

        for source_hash, target_hash, target_content, mode in variants:
            if current_hash == target_hash:
                matched = True
                modes.add(mode)
                break

            if current_hash == source_hash:
                pending[path] = target_content
                matched = True
                modes.add(mode)
                break

        if not matched:
            raise SystemExit(
                f"ERROR: {path} differs from all supported versions. "
                "Attach the current repository archive to rebuild the installer."
            )

    for relative_path, content in NEW_FILES.items():
        path = ROOT / relative_path

        if path.exists():
            current = path.read_text(encoding="utf-8")
            if current != content:
                raise SystemExit(
                    f"ERROR: existing file differs from installer target: {path}"
                )
        else:
            pending[path] = content

    localized_mode = "localized" in modes

    for locale, updates in CATALOG_UPDATES.items():
        path = ROOT / f"app/static/i18n/{locale}/routes.json"

        if not path.exists():
            if localized_mode:
                raise SystemExit(
                    f"ERROR: localized Routes requires an existing catalog: {path}"
                )
            continue

        try:
            data = json.loads(path.read_text(encoding="utf-8"))
        except (OSError, json.JSONDecodeError) as exc:
            raise SystemExit(f"ERROR: cannot read {path}: {exc}") from exc

        changed = False
        for key, value in updates.items():
            if data.get(key) != value:
                data[key] = value
                changed = True

        if changed:
            pending[path] = json.dumps(
                data,
                ensure_ascii=False,
                indent=2,
            ) + "\n"

    for path, content in pending.items():
        path.parent.mkdir(parents=True, exist_ok=True)
        path.write_text(content, encoding="utf-8", newline="\n")
        print(f"updated {path.as_posix()}")

    print(f"Changed files: {len(pending)}")


if __name__ == "__main__":
    main()